/**
 * 能力集合在队列中的 `actionType` 前缀（`set:<setId>`）。
 * **`components/workflow/workflowSectionUiConstants.ts`** 中的 **`SET_ACTION_PREFIX`** 须与此常量一致。
 */
export const WORKFLOW_SET_ACTION_PREFIX = 'set:' as const;
