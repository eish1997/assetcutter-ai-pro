#!/usr/bin/env node
/**
 * Retired: external Agent MCP smoke was removed (CLI-only Agent).
 * Use: npm run agent:cli / npm run agent:init
 */
console.error('[agent-mcp-smoke] removed: MCP product surface is retired. Use `npm run agent:cli` / `npm run agent:init`.');
process.exit(1);
