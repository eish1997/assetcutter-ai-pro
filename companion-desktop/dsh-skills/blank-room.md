# 空房

whenToUse：人在加号租的空房，或说「布置这间房 / 找管家 / 这间空的怎么办」。没进这间房不要当空房办事。

步骤：

1. 先看 handoff 的 `roomId` 和 finger 的 `surface`。`workspace_open_surface` 只用这个 `room-*`。不要改成 `workflow` / `tools` / `connections`。
2. 人在 Plan mode：先问清楚这间房要做什么，再通过 `exit_plan_mode` 交完整 markdown 计划（以 `#` 标题开头）。
3. **禁止假装已改文件或已生成网站。** 盖站施工还没接通。批准计划之后也不要假装写了隔间。
4. 用户明确要去技能 / 工具 / 地图，才切常驻店。
