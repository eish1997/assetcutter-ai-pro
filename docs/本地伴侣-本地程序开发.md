# 本地伴侣 · 本地程序开发说明

本文描述 **本仓库内可运行的本地伴侣进程**（`local-companion/`）：自带 **浏览器界面（用户向称本机管理页）**，当前固定两个产品模块——**插件**、**仓库**——后续可在同一界面扩展更多 Tab 或子路由。

**总架构与插件模型**仍以 [`本地伴侣-存储与计算规范.md`](./本地伴侣-存储与计算规范.md) 为准；**站点中转（Relay）** 协议见 [`本地中转应用开发文档.md`](./本地中转应用开发文档.md)。**产品与技术已决**（安装壳、合并策略、安全模型等）见 **[`本地伴侣-待决策清单与建议.md`](./本地伴侣-待决策清单与建议.md)**；**错误码**见 **[`附录-伴侣错误码.md`](./附录-伴侣错误码.md)**。

---

## 1. 定位

| 项 | 说明 |
|----|------|
| **进程形态** | Node.js（≥20）+ 本机 `http` 服务，仅绑定 **`127.0.0.1`**。 |
| **界面形态** | 内置静态页 `public/index.html`：**总览**（runtime + capabilities）、**插件**、**仓库**；开发期可直接 Node 打开浏览器，亦可用 **`companion-desktop/`** 托盘壳（见 **§6.1**）。 |
| **与主站关系** | 主站「设置 → 本地伴侣」探测 `GET /v1/health`、`GET /v1/capabilities`，成功后可展开 **Relay**、**`compute.seamRepair`** 摘要；「打开本机管理页」即本进程 **`/`**。业务可调用 **`submitCompanionSeamRepairJob`**（`services/companionClient`）。 |

### 1.1 目标交付形态（产品体验，已决）

**产品目标**：**Electron 安装包** + **系统托盘（必选）** + **主窗口（可选）** + **首次配对向导**；**默认不开机自启**；卸载 **默认保留** 本机卷，可选勾选删卷。进程名 **`AssetCutterCompanion`**；安装与数据路径 **英文**（如 `%LOCALAPPDATA%\AssetCutterCompanion\`），Volume **不**默认进 `Program Files`。

**与当前仓库的关系**：`local-companion/` 为 **开发期 Node 宿主**（协议与安装版一致）；Electron 壳 **内嵌/启动该 Node 进程**，管理页 HTML 可继续用 `public/index.html` 或迁入 WebView。仓库内已有 **`companion-desktop/`**（M0：托盘 + 子进程拉起宿主），根目录执行 **`npm run companion-desktop:start`**（须先在各目录 `npm install`）。

**安全（安装包版）**：**Origin 白名单 + 配对 Token**（与 auth-api / bridge 对齐）；开发期 `npm run dev` 可维持宽松 CORS 便于联调（见已决清单 §3）。

---

## 2. 目录结构

```text
companion-desktop/     # Electron 壳（M0：托盘、启动 local-companion、Win 首次向导骨架）
  package.json
  main.cjs
  preload-wizard.cjs
  wizard/index.html
  README.md
local-companion/
  package.json
  tsconfig.json
  README.md
  public/
    index.html          # 总览 / 插件 / 仓库 / 联调
  src/
    main.ts
    httpServer.ts
    httpHandler.ts
    httpReadBody.ts
    openBrowser.ts
    pluginRegistry.ts
    repositoryVolume.ts
    storage/            # 路径安全、manifest、落盘
    compute/jobsStore.ts
```

---

## 3. HTTP API（与探测兼容）

| 方法 | 路径 | 用途 |
|------|------|------|
| `GET` | `/` `/index.html` | 本机管理页 HTML（含 **联调** 页：项目 id 列表、stub.ping、近期 Job） |
| `GET` | `/v1/health` | 存活 |
| `GET` | `/v1/capabilities` | 宿主 + 插件能力 JSON（`protocolVersion` 等） |
| `GET` | `/v1/runtime-status` | 管理页轮询：含 `storage.projectIds`、`compute.recent` |
| `GET` | `/v1/plugins` | 仅插件数组（调试用） |
| `GET` | `/v1/repository/summary` | 仓库卷根、是否存在、一级条目数、浅层文件字节合计 |
| `GET` | `/v1/projects` | `{ projectIds: string[] }`（`volume/projects/` 下合法目录名） |
| `GET` | `/v1/projects/:projectId/manifest` | `layoutVersion: 1` 与 `entries[]`（单写者、与规范 §4.2 对齐） |
| `GET` | `/v1/projects/:projectId/assets/:key` | 返回已落盘 object 原始字节；`Content-Type` 取自 manifest 中该 key 的 `mime` |
| `PUT` | `/v1/projects/:projectId/assets/:key` | 请求体 = 单文件原始字节；落盘为 `assets/<key>/object` 并更新 manifest |
| `GET` | `/v1/projects/:projectId/assets/:key/meta` | 元数据 + `onDisk` |
| `DELETE` | `/v1/projects/:projectId/assets/:key` | 删文件 + 从 manifest 移除 |
| `POST` | `/v1/compute/jobs` | Job 体：`{ "protocolVersion": 1, "type": "stub.ping" \| "seam_repair", "jobId"?: "…", "projectId"?: "…" }`；支持外包一层 `{ "job": { … } }` |
| `GET` | `/v1/compute/jobs` | 最近任务列表（内存） |
| `GET` | `/v1/compute/jobs/:jobId` | 单条任务 |
| `GET` | `/v1/compute/jobs/:jobId/events?afterSeq=0&limit=100` | 任务事件流（`task.accepted` / `reply.delta` / `reply.completed` / `task.failed`），用于闭环展示与断点续读 |
| `GET` | `/v1/compute/jobs/:jobId/stream?afterSeq=0` | SSE 实时事件流（`job.event` / `job.end`） |
| `DELETE` | `/v1/compute/jobs/:jobId` | 从内存移除（尽力取消） |

`POST`/`PUT`/`DELETE` 与 `GET` 使用同一套 CORS（`Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS`）。**单文件上传**大小默认 **100MB 封顶**，可用 **`COMPANION_MAX_UPLOAD_BYTES`** 调整（硬顶 512MB）。**默认**（未配置白名单时）JSON 接口仍 **`Access-Control-Allow-Origin: *`**（或回显 `Origin`），便于 https 开发站跨域探测本机。若设置 **`COMPANION_ALLOWED_ORIGINS`**（逗号分隔的 Origin 列表），则带 `Origin` 头的请求 **不在列表中** 时返回 **403**（`AUTH_ORIGIN_DENIED`）；列表可含 **`http://localhost:*`**、**`http://127.0.0.1:*`** 以匹配任意端口。无 `Origin` 的请求（同源管理页、curl 等）**不**因白名单被拒。

**可选共享 Token**：设置 **`COMPANION_SHARED_TOKEN`**（非空）后，除 **`GET /v1/health`**、**`GET /`**、**`GET /index.html`** 外，所有路径需 **`Authorization: Bearer <与变量值一致>`**，否则 **401**（`AUTH_TOKEN_REQUIRED` / `AUTH_TOKEN_INVALID`）。主站设置「本地伴侣」可保存同名 Token，**`services/companionClient`** 会自动附加该头。**`/v1/capabilities`**、**`/v1/projects`** 等在启用 Token 后亦需携带 Bearer。`GET /v1/capabilities` 与 **`GET /v1/runtime-status`** 的 JSON 中含 **`access`** 摘要（白名单是否启用、是否要求 Bearer），便于管理页展示。

**环境变量（宿主）**：`COMPANION_HTTP_PORT`、`COMPANION_VOLUME_ROOT`、`COMPANION_OPEN_BROWSER` 见上；另 **`COMPANION_RELAY_CMD`**（可选）拉起 Relay 子进程；**`COMPANION_RELAY_CHILD_HTTP_PORT`** 控制子进程 `COMPANION_HTTP_PORT`（默认 `0`，`keep` 不覆盖），见 **§5**；**`COMPANION_ALLOWED_ORIGINS`**、**`COMPANION_SHARED_TOKEN`** 见上（安装包收紧模型，与已决清单 §3 对齐）。**修缝 Adapter**：**`COMPANION_SEAM_REPAIR_URL`**（默认 `http://127.0.0.1:8008/api/repair`）、**`COMPANION_SEAM_REPAIR_TIMEOUT_MS`**（默认 `120000`）。

---

## 4. 两个模块（产品语义）

### 4.1 插件（本地计算等）

- **当前**：`POST /v1/compute/jobs` 已接；`type: "stub.ping"` 在内存中 **直接标记完成**；**`seam_repair`** 已接 **真实 Adapter**：从当前 **`projectId`** 的 Volume 读取 **`inputs`**（`{ objKey, textureKey, maskKey?, outputKey? }` 或规范 §5.1 式 `inputs[]` 角色 `mesh`/`texture`/`mask`），`multipart` 转发至 **WebSeamRepair** **`POST /api/repair`**（默认 **`http://127.0.0.1:8008/api/repair`**，见 **`COMPANION_SEAM_REPAIR_URL`**、**`COMPANION_SEAM_REPAIR_TIMEOUT_MS`**）；若设 **`outputKey`** 则将 PNG 写回 **`PUT` 同一路径语义** 的 asset。  
- **后续**：**WS 进度**（见总规范 §5.3）、scratch 目录与 Job 取消的硬约束；Relay 可通过 **`COMPANION_RELAY_CMD`** 由宿主子进程承载（**§5**），长期再并入单进程。网站侧 **`services/companionClient/relay.ts`** 可从 **`/v1/capabilities.relay`** 读 M1 状态（不直连 WSS）。

#### 新增开源能力时的默认做法（通用）

- **优先“调用”**：在宿主新增一个 Adapter，通过 HTTP/子进程/SDK 调开源项目。  
- **少改上游源码**：除非遇到阻断 bug、必要特性或不可绕过的性能问题。  
- **网站不直连开源项目**：网站只提交 `Job`，由伴侣统一编排输入输出与资产落盘。  
- **文档同步**：每新增一个能力，补 4 件事：任务 `type`、`inputs/params`、环境变量、错误码。

### 4.2 仓库

- **Volume 根**：环境变量 **`COMPANION_VOLUME_ROOT`**；未设置时为 **`~/.assetcutter-companion/volume`**。  
- **项目布局**：`projects/<projectId>/manifest.json`、`assets/<key>/object`、`scratch/` 预留（与总规范 §4.2 一致；**manifest 单进程内单写者**）。  
- **`projectId` / `key`**：仅允许安全字符类 `[a-zA-Z0-9._-]` 等，防路径穿越。  
- **指标**：`/v1/repository/summary` 仍为卷根浅层；**catalog / 跨项目索引** 为 P1+。

---

## 5. 与 A-Driver `local-bridge` 的关系

- **A-Driver** 中的 `apps/local-bridge` 可继续承担 **Relay + WS** 等重逻辑；本包侧重 **主仓库可维护的宿主壳 + 双模块管理页 + 与网站一致的 HTTP 探测面**。  
- **默认端口均为 `18765`**：同一台机器上 **不要** 让两个进程 **同时绑定** 同一端口。推荐：**宿主仅 `local-companion` 占 `18765`**，通过 **`COMPANION_RELAY_CMD`** 拉起 `local-bridge` 子进程，并由宿主注入子进程 **`COMPANION_HTTP_PORT=0`**（默认）关闭子进程自带伴侣 HTTP，仅保留 WS 等（可用 **`COMPANION_RELAY_CHILD_HTTP_PORT`** 覆盖，`keep` 表示不覆盖）。  
- **长期**：单进程合并（pluginhost 加载 relay），见总规范 §11 与已决清单。

---

## 6. 本地开发命令

```bash
# 在 local-companion 目录
npm install
npm run dev          # tsx watch

# 或在网站仓库根目录
npm run local-companion:dev
```

### 6.1 Electron 托盘壳（`companion-desktop`，M0）

**用语（M2）**：对用户与托盘菜单统一称 **本机管理页**（`http://127.0.0.1:<COMPANION_HTTP_PORT>/`）；代码与日志中仍可能出现「控制台」指同一 Web 宿主，含义相同。

模拟「安装后仅托盘、不自动弹浏览器」的体验：

1. **`local-companion/`** 与 **`companion-desktop/`** 各执行一次 `npm install`。  
2. 仓库根：`npm run companion-desktop:start`（或 `cd companion-desktop && npm start`）。  
3. 壳会设置 **`COMPANION_OPEN_BROWSER=0`** 并 `spawn` 系统 **`node --import tsx src/main.ts`**（工作目录为 `local-companion/`）；托盘支持 **「打开主窗口」**（`BrowserWindow` 内嵌加载本机管理页 URL）与 **「打开本机管理页」**（系统浏览器）两个入口，默认地址 `http://127.0.0.1:<COMPANION_HTTP_PORT>/`（默认 `18765`）。  
4. 自定义 Node 可设 **`COMPANION_NODE`**；二次启动同一壳会 **单实例**：若已有主窗口则聚焦，否则打开主窗口（若 **Windows 向导窗口** 仍打开则优先聚焦向导窗口）。

**Windows 首次向导（当前骨架）**：仅 **`win32`** 在首次运行（`userData` 下无 `first-run-complete` 文件）时打开 **`BrowserWindow`**，文案说明托盘与本机管理页；**「完成」** 写入完成标记后关闭窗口。**`userData`** 默认 **`%LOCALAPPDATA%\AssetCutterCompanion\desktop-shell`**。环境变量 **`COMPANION_DESKTOP_SKIP_WIZARD=1`** 跳过向导；**`COMPANION_DESKTOP_FORCE_WIZARD=1`** 每次启动都显示（调试用）。**macOS / Linux** 当前不弹该窗。托盘菜单（**`win32`**）含 **状态行**、**「打开主窗口」**、**「重新启动本地伴侣」**、**「首次设置向导」**；**左键单击托盘图标** 等价于「打开本机管理页」。向导已补齐最小可用配对：界面为 **本机通信密码** 与 **允许的网站地址**（落盘 `userData/pairing-config.json`，对应 **`COMPANION_SHARED_TOKEN`** / **`COMPANION_ALLOWED_ORIGINS`**）；保存时会校验 **密码至少 8 位**、Origin 逗号分隔并自动去重。桌面壳启动 `local-companion` 时会在未显式配置环境变量的前提下自动注入上述变量。当 `local-companion` 启动失败/异常退出，或轮询 `GET /v1/runtime-status` 检测到 **Relay 已配置但未运行**、**状态检查 401（配对密码不一致）**、**`siteAuth.state=not_logged_in`（站点登录态失效）** 时，托盘会弹出异常提示并引导执行重启或打开本机管理页排查；Relay 异常提示会附带 `lastError/lastExitCode/lastSignal` 诊断信息。主窗口若无法加载管理页 URL，会显示内置故障页提示下一步排障动作。正式 **开机自启勾选生效** 等仍按已决清单后续实现。

### 6.2 打包脚本（P1 基础）

- 根脚本：`companion-desktop:pack`、`companion-desktop:dist:win`、`companion-desktop:dist:portable`。  
- 重试脚本：`companion-desktop:dist:retry`（`scripts/companion-desktop-dist.ps1`，支持 `-UseMirror` 注入镜像变量）。  
- 发布预检脚本：`companion-desktop:release:check`（可选加 `-RequireSigning` / `-RequirePublishUrl`）。  
- `companion-desktop/package.json` 已补 `electron-builder` 基础配置（`appId`、`productName`、`nsis/portable` 目标、产物目录 `release/`）。  
- 已实测：`companion-desktop:pack` 可产出 `release/win-unpacked`。  
- 在当前网络环境下，`dist:win` / `dist:portable` 可能因 `electron-builder-binaries`（如 `nsis-resources`）下载失败中断；**代码签名**仍须在发布机构建机上配置 `CSC_LINK` 等（见发布检查清单）。
- 发布流程清单见：`docs/本地伴侣-发布前检查清单.md`。

### 6.3 自动更新（M2 骨架，可选启用）

- 依赖：`companion-desktop` 已引入 **`electron-updater`**。  
- **环境变量 `COMPANION_UPDATE_FEED_URL`**：设为 **generic 更新根 URL**（与 `electron-builder` 发布的 `latest.yml` 同目录，见 [electron-updater generic](https://www.electron.build/auto-update)）。设置后壳会：约 **20s** 后静默检查一次更新；托盘菜单增加 **「检查更新…」**；发现新版本时弹窗询问是否下载，下载完成后可 **退出并安装**。  
- 未设置该变量时：**不**注册更新逻辑，菜单中亦无「检查更新」，避免开发期误连。

---

## 7. 验收清单（P0）

- [x] `npm run dev` 后浏览器能打开 `/`，**插件**、**仓库**、**联调** Tab 有数据。  
- [x] 主站设置「检测连接」在默认地址下返回成功（需本进程已启动）；**「拉取项目 id」** 调用 `GET /v1/projects`（`services/companionClient`）。  
- [x] 修改 `COMPANION_VOLUME_ROOT` 后仓库页路径与 summary 一致（人工回归）。  
- [x] 与 `local-bridge` **不同端口策略**：宿主占 `18765` 时子进程默认 `COMPANION_HTTP_PORT=0`（见 **§5**）。  
- [x] `npm run companion-desktop:start` 后托盘可打开本机管理页、退出时子进程结束（人工回归）。  
- [x] **Windows**：删除 `%LOCALAPPDATA%\AssetCutterCompanion\desktop-shell\first-run-complete` 后重启，应再次出现首次向导；点「完成」后不再出现（人工回归）。

> 验收辅助脚本：`scripts/test-companion-desktop.ps1`  
> 新增场景 `-Scenario p0-acceptance`：交互式收集 P0 验收结果并追加写入 `docs/本地伴侣-P0验收记录.md`。

### 7.1 闭环体验验收（面向最终产品）

- [~] 网站发起站点自动化任务后，用户可见完整链路：`task.accepted` → `reply.delta*` → `reply.completed|failed`（**进行中**：设置页与 **贴图修缝主流程页** 已切到“**SSE 优先 + 失败回退轮询**”；贴图页支持“一键提交到本地伴侣并自动跟踪 jobId”，且在 **`reply.completed` + `outputKey`** 时自动 **`GET` 资产** 拉回修复贴图至 2D/3D 预览；Relay 实时事件与业务主链路自动绑定仍待接入）。  
- [~] Relay 或站点登录异常时，设置页与托盘至少一处有明确提示，且提示中包含下一步动作（**进行中**：贴图修缝主流程页与设置页的 `task.failed/reply.completed` 均已提供“重置游标并重拉（失败态）/打开本机管理页/复制排障信息”动作；托盘已补齐“异常气泡 + 重新启动本地伴侣 + 打开本机管理页”动作，并覆盖 `runtime-status` 下的 Relay 未运行、配对密码不一致（401）与 `siteAuth.state=not_logged_in`（站点登录态失效）场景，且 Relay 提示已附带 `lastError/lastExitCode/lastSignal` 诊断信息；登录态判定当前基于 Relay 错误语义归一，后续可接入更强信号源）。  
- [~] 同一 `taskId` 在断线重连场景不重复渲染最终结果（幂等去重可验证）（**进行中**：事件流以 `seq` 去重；设置页与贴图页的 `jobId/afterSeq/auto` 已提升为 `localStorage` 持久化，并新增跨页面共享 `jobId -> afterSeq` 游标合并；任务到达 `reply.completed/task.failed/task.cancelled` 会自动停跟踪；终态事件幂等落盘 `ac_companion_job_terminal_v1`，刷新后可直接恢复终态卡片；页面重开仍会按已保存 `jobId` 补拉事件以与服务端对齐；与业务结果 UI 的完全幂等绑定仍待补齐）。

---

## 8. TODO（按优先级）

- [x] **P0.14**：Relay 运行状态诊断增强（`lastError/lastExitCode/lastSignal`）并映射到托盘异常提示。  
- [x] **P0.15**：补齐站点登录态错误统一状态字段（`runtime-status.siteAuth`）并接入托盘映射。  
- [x] **P0.16**：首次向导补齐网站配对联调（最小可用：Token + Origin 本地保存并注入宿主环境）。  
- [x] **P0.17**：补完 P0 人工验收项并固化测试脚本场景（主窗口、托盘退出子进程、向导复现）。  
- [~] **P1**：安装包签名/升级链路、卸载保留卷策略、正式版安全策略收口（已完成打包脚本与 `electron-builder` 基础配置）。

### 8.1 执行队列（当前）

- [x] **优先级 2**：安装包签名与升级链路配置骨架（配置项、脚本、文档）。
- [x] **优先级 3**：Origin+Token 与设备会话/可撤销密钥对齐（已补本地可撤销会话占位：`/v1/pairing/session`、`/v1/pairing/revoke`、`AUTH_TOKEN_REVOKED`）。
- [~] **优先级 4**：任务事件流升级到 WS/SSE，并与业务主链路自动绑定（已补 `/v1/compute/jobs/:jobId/stream` SSE 接口与前端 `createCompanionJobEventStream`；设置页/贴图页已接入“**SSE 优先 + 回退轮询**”）。
- [~] **优先级 5**：跨页面/跨会话一致性去重与恢复收口（已将关键游标持久化从 `sessionStorage` 升级到 `localStorage`；跨页游标 `ac_companion_job_cursor_store_v1`；终态快照 `ac_companion_job_terminal_v1`，「重置游标」时同步清除游标与终态缓存）。

---

**文档版本**：随 `local-companion` 迭代更新。
