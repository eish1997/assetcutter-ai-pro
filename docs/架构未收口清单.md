# 架构未收口清单

**创建日期**：2026-07-16  
**用途**：集中记录“架构方向已经定了，但实现还没有完全闭环”的事项，避免多轮改造后遗忘。  
**维护原则**：凡是已经形成架构口径、但状态仍是 `partial`、`partial_gateway`、`adapter_pending`、`待后端`、`待产品化` 的事项，都应登记在这里；完成后把状态改为 `done`，并补验收证据。

## 状态口径

| 状态 | 含义 |
|---|---|
| `open` | 架构已定，尚未开始或只做了文档 |
| `in_progress` | 已有部分实现，但还不能宣称闭环 |
| `blocked` | 需要外部条件，例如 Key、上游能力、产品拍板 |
| `done` | 已有实现、测试/手测、文档同步，能按闭环口径交付 |

## 维护规则

- 不在这里写完整方案，完整方案仍放在对应设计文档；这里写“别忘了收口”的索引。
- 每条必须有：状态、已定架构、未收口点、下一步、关联文档/代码。
- 如果某个入口在 `services/workflowAiPickIndex.ts` 里是 `partial_gateway` 或 `legacy`，这里必须有对应条目。
- 如果某个供应商或模型在管理端可见，但不能“接 Key 后发布并执行”，这里必须有对应条目。
- 如果某个 UI/资产体验已经定了产品化原则，但实现仍靠补丁式分支，也必须登记。

## A. AI 执行路由与 Gateway 闭环

### A1. 能力预设执行仍是 partial_gateway

**状态**：`in_progress`  
**已定架构**：所有普通工作台生成请求应走 `runUnifiedGeneration -> /api/ai/jobs`，供应商只通过 Gateway route/adapter 出货。  
**未收口点**：

- `capability_preset_execute` 在审计表中仍是 `partial_gateway`。
- 生图主路径已走统一 Gateway，但视频、3D、提示词理解、局部重绘辅助链路仍存在混合路径。
- 预设、组覆盖、全局默认模型之间的优先级需要形成单一 resolve 契约。

**下一步**：

- 给能力预设增加统一 route schema：`capability + modality + canonicalModelId + providerId + assetContext`。
- 清理能力执行器里仍直接调用旧 `workflowChat` 的提示词融合路径，明确哪些是临时旧链路。
- 测试覆盖 text/image/video/model3d 的预设提交字段。

**关联**：

- `docs/AI执行路由闭环架构审计.md`
- `services/capabilityExecutor.ts`
- `services/workflowAiPickIndex.ts`
- `services/generation/runUnifiedGeneration.ts`

### A2. Project Agent 当前画面问答上下文仍是 partial

**状态**：`in_progress`  
**已定架构**：`@当前画面 / run_plain_i2t` 应携带当前预览截图、资产 id 和上下文 metadata，经 Gateway text job 执行视觉问答。  
**已完成**：第 4 轮已将截图作为 `inlineData` 进入 `text.generate` Gateway job，并写入 `inputContext`。  
**未收口点**：

- 左侧版本树的父子关系、分叉来源、版本 id 尚未结构化传入。
- 文本、视频、3D、音乐资产仍主要依赖当前截图，缺少“资产语义摘要 + 当前渲染状态 + 版本节点”的统一上下文包。
- Agent 回复 UI 尚未展示“本回答引用了哪个画面/资产版本/job”。

**下一步**：

- 定义 `currentViewContext`：`assetId/versionId/parentVersionId/displayKey/snapshot/semanticSummary`。
- 在 Agent 回复和任务卡展示引用来源与 Gateway job id。

**关联**：

- `components/WorkflowSection.tsx`
- `types.ts` 中 `WorkflowPendingTask.inputContext`
- `services/generation/runUnifiedGeneration.ts`
- `docs/网页端项目Agent侧栏-开发规格.md`

### A3. 分镜表 AI 仍是 partial_gateway

**状态**：`in_progress`  
**已定架构**：分镜文本理解和生图类操作都应通过统一 Gateway 薄层，携带 `storyboardAssetId / rowId / operation / presetId`。  
**已完成**：第 5 轮新增 `storyboardGatewayText`，结构解析、批量解析、结构优化、批量导入 AI 规范化已进入 Gateway 文本 job。分镜重绘/拼图/角色替换已通过能力执行器进入统一生图链路。  
**未收口点**：

- 分镜解析页仍有大量本地规则预解析、字段确认、切格、回填逻辑，需要继续标清哪些是本地处理，哪些是 AI job。
- 视觉检测、拆格、辅助理解路径尚未统一登记 Gateway/local 边界。
- UI 侧还没有完整展示分镜 AI job id、route、provider、模型、费用明细。
- `tests/storyboardTableParse.test.ts` 仍有 2 个既有格式断言失败，需要独立修复，不能混入路由收口。

**下一步**：

- 为分镜所有 AI 操作补一张 operation matrix：`operation -> local/Gateway -> input -> output -> audit`。
- 给分镜结果/历史面板展示 Gateway job trace。
- 单独修复 `compileShotText/compileRedrawPrompt` 格式断言。

**关联**：

- `services/storyboardGatewayText.ts`
- `services/storyboardTableParse.ts`
- `services/storyboardTableBulkAiDetect.ts`
- `services/storyboardTableRedraw.ts`
- `services/storyboardFeedbackSheetRedraw.ts`
- `services/storyboardRoleReplaceRedraw.ts`

### A4. Workflow 3D 仍是 partial_gateway

**状态**：`in_progress`  
**已定架构**：3D 生成应按 `model3d.generate` 统一任务结构，Tripo、混元、方舟 3D 只作为 provider route。  
**未收口点**：

- Tripo 平台 Key 路径已接近可用，但多供应商 3D route 还没有统一执行契约。
- 模型预览、资产缩略图、生成结果落库、任务追踪之间仍有历史兼容逻辑。
- 方舟 Seed3D 已有 Gateway 首版闭环；腾讯混元 3D 仍处于待 adapter/待后端状态。

**下一步**：

- 固化 `model3d.generate` input/output schema。
- 把 3D 结果 artifact、预览图、源模型文件、缩略图生成纳入统一资产结果契约。
- 为 Tripo、Seed3D、混元分别登记 route readiness 和 Key readiness；Seed3D 继续补真实上游字段和多文件 manifest 校验。

**关联**：

- `services/generate3d/`
- `components/workflow/AssetMediaPreviewCenter.tsx`
- `docs/高度3D预览-开发文档.md`

## B. 供应商模型中心与发布闭环

### B0. 方舟补全与全供应商查漏补缺总任务

**状态**：`in_progress`  
**背景**：前面已按“先方舟图片 Seedream，再补方舟视频/3D，再做全供应商查漏”的策略推进；目前只完成了第一轮方舟图片 Seedream 文生图闭环第一版，其余内容仍需登记收口。  
**已完成**：

- 方舟图片 Seedream 第一版：
  - `doubao-seedream-5-0*` 图片模型已标记为 Gateway ready。
  - AI Gateway 已新增 `volcengine-ark-image` provider route。
  - image worker 复用 OpenAI-compatible adapter 调用方舟 `/api/v3/images/generations`。
  - canonical id 已映射到带日期后缀的 Seedream 上游 model id。
  - 返回 URL/base64 已转统一图片 artifact。
  - 当前只声明文生图能力，暂不开放图片编辑。

**未收口总项**：

1. 方舟视频 Seedance 闭环。
2. 方舟 3D Seed3D 闭环。
3. 方舟 Key Smoke Test 分层。
4. 方舟图片/视频/3D 参数 UI。
5. 全供应商 route / adapter / worker 一致性审计。
6. 前端所有入口统一模型来源。
7. 参数能力矩阵。
8. 输出资产标准化。
9. 真实测试分层。
10. 错误提示统一。
11. 文档和测试补齐。

**建议执行轮次**：

| 轮次 | 目标 | 状态 |
|---|---|---|
| 1 | 方舟图片 Seedream 闭环 | `done_first_pass` |
| 2 | 方舟视频 Seedance 闭环 | `done_first_pass` |
| 3 | 方舟 3D Seed3D 闭环 | `done_first_pass` |
| 4 | 全供应商 route/adapter/worker 一致性审计和修复 | `done_first_pass` |
| 5 | 前端参数能力矩阵和预设/组覆盖/输入框统一 | `done_first_pass` |
| 6 | 真实测试分层和后台 UI 优化 | `done_first_pass` |
| 7 | 输出资产标准化查漏补缺 | `done_first_pass` |

**关联**：

- `docs/供应商接入可用闭环开发文档.md`
- `docs/供应商模型发布闭环审计.md`
- `docs/供应商模型中心架构设计.md`
- `shared/aiGatewayModelRoutes.js`
- `server/ai-gateway/`

### B1. 方舟多模态未完全可执行

**状态**：`in_progress`  
**已定架构**：火山方舟是供应商，不是单一模型；其文本、图片、视频、3D 模型应作为 provider model/catalog/route 管理。  
**已完成**：

- Doubao Seed 文本第一版 Gateway ready。
- Seedream 文生图第一版 Gateway ready。
- 方舟模型目录已从 OpenAI 镜像修正为 Doubao/Seed 系列。

**未收口点**：

- Seedream 图像编辑能力未开放或未声明。
- Seedance 视频、Seed3D 已有 Ark async adapter 首版；仍需真实上游字段校验、生成测试和多文件 artifact manifest。
- 方舟 endpointId / 模型 id 映射、不同地域/endpoint 的管理端配置还不完整。

**下一步**：

- 补方舟 endpoint mapping 管理与校验。
- Seedance 视频、Seed3D adapter 继续做真实 Key / 最小生成测试闭环。
- 管理端发布前用 route guard 明确阻断 pending 能力。

**关联**：

- `docs/供应商模型中心架构设计.md`
- `docs/供应商模型发布闭环审计.md`
- `docs/供应商接入可用闭环开发文档.md`
- `shared/aiGatewayModelRoutes.js`

### B1.1 方舟视频 Seedance 待闭环

**状态**：`done_first_pass`
**已定架构**：Seedance 应作为方舟供应商下的视频模型 route，进入统一 `video.generate` Gateway worker，输出标准视频资产。  
**已完成首版**：

- `doubao-seedance-2-0*` 已从 pending rule 移入 Gateway executable rule。
- `provider-router` 新增方舟 video route：`video-worker` + `volcengine-ark-async`。
- `video-worker` 支持 Ark async adapter。
- Ark async adapter 覆盖任务创建、轮询、失败状态、视频 URL artifact、provider key health event。
- published catalog / route guard / availability summary 已能把方舟视频识别为发布后可执行，缺 Key 时显示 `key_missing`。

**未收口点**：

- 需要确认官方任务创建、轮询、失败状态、结果 URL 格式。
- 需要接 `seedance-*` 到 video worker。
- 需要映射文本生视频、图生视频参数：提示词、首帧图、时长、比例、清晰度、运动强度等。
- 需要将输出统一转成视频资产，并写入供应商、模型、任务 id、参数快照。
- 需要支持任务状态回写、失败提示，以及官方支持时的取消能力。
- 前端视频模型选择器需要在发布后可选，但仅在 route/key/adapter ready 时放行。

**下一步**：

- 先补 Seedance route readiness 和 adapter skeleton。
- 再补最小文生视频任务创建/轮询测试。
- 最后接前端视频参数面板和资产回填。

**关联**：

- `services/aiGatewayVideoExecution.ts`
- `server/ai-gateway/`
- `shared/aiGatewayModelRoutes.js`

### B1.2 方舟 3D Seed3D 待闭环

**状态**：`done_first_pass`
**已定架构**：Seed3D 应作为方舟供应商下的 3D 模型 route，进入统一 `model3d.generate` Gateway worker，输出标准模型资产。  
**已完成首版**：

- `doubao-seed3d-2-0` 已从 pending rule 移入 Gateway executable rule。
- `provider-router` 新增方舟 model3d route：`model3d-worker` + `volcengine-ark-async`。
- `model3d-worker` 支持 Ark async adapter。
- Ark async adapter 覆盖任务创建、轮询、失败状态、模型 URL artifact、provider key health event。
- published catalog / route guard / availability summary 已能把方舟 3D 识别为发布后可执行，缺 Key 时显示 `key_missing`。

**未收口点**：

- 需要确认 Seed3D API 是文生 3D、图生 3D，还是两者都有。
- 需要接 `seed3d-*` 到 model3d worker。
- 需要映射输入图、提示词、质量、格式等参数。
- 输出需要转成 3D 模型资产，包含缩略图、模型文件、材质文件。
- 前端 3D 模型选择器需要在发布后可选，但仅在 route/key/adapter ready 时放行。

**下一步**：

- 先确认官方 API 能力和输出格式。
- 固化 `model3d.generate` input/output schema。
- 做 Seed3D 最小任务 adapter test 和 artifact 解析。

**关联**：

- `services/generate3d/`
- `components/workflow/AssetMediaPreviewCenter.tsx`
- `shared/aiGatewayModelRoutes.js`

### B1.3 方舟 Key Smoke Test 分层待完善

**状态**：`done_first_pass`
**已定架构**：后台测试不能都叫“测试”，必须区分凭证检查、真实探活、生成测试。  
**已完成首版**：

- Provider Key Smoke Test 返回 `testLayer=key_smoke`、`createsGenerationTask=false`、`nextAction`。
- 方舟等 OpenAI-compatible 供应商支持低成本 `GET /models` 上游探活；无法真实探活时保留凭证检查语义，不创建生成任务。
- provider key health event 已记录测试模式、上游状态和错误信息。
- 管理端供应商中心已增加 Test Layers 说明，区分 Credential Check、Upstream Probe、Route Check。

**未收口点**：

- 后台 UI 需要清楚区分：
  - `凭证检查`
  - `真实探活`
  - `生成测试`
- Generation Test 仍未做，不应和 Key Smoke / Route Check 混为一谈。

**下一步**：

- 梳理方舟可安全调用的低成本接口。
- 将 provider key health event 写入 test mode、route、upstreamStatus、latencyMs。
- 管理端展示测试类型和风险说明。

**关联**：

- `server/ai-gateway/provider-keys`
- `server/ai-gateway/model-route-test.js`
- `components/admin/AdminProviderKeysPanel.tsx`

### B1.4 方舟参数 UI 待完善

**状态**：`done_first_pass`
**已定架构**：前端参数面板应按模型能力矩阵渲染，不能让用户填写上游不支持或当前 adapter 未映射的参数。  
**已完成首版**：

- 新增 `ModelParameterCapability` 首版能力矩阵。
- Capability Presets 的图片/视频/3D 参数 UI 已按模型能力过滤展示。
- 方舟 Seedance / Seed3D 已声明专属参数能力，Seed3D 会清理 Tripo/Tencent 专属旧参数。

**未收口点**：

- 组覆盖、全局输入框、侧栏对话参数面板仍需完全接入同一套能力矩阵。
- adapter-level 测试还需覆盖“UI 参数 -> Gateway input -> 上游请求体”。

**下一步**：

- 为方舟 Seedream/Seedance/Seed3D 声明参数能力矩阵。
- 将预设、组覆盖、全局输入框、侧栏对话统一读取能力矩阵。
- 给参数映射补 adapter-level 测试。

**关联**：

- `components/CapabilityPresetSection.tsx`
- `components/WorkspaceQuickComposeBar.tsx`
- `components/WorkflowSection.tsx`
- `docs/供应商接入可用闭环开发文档.md`

### B4. 全供应商状态总审计待收口

**状态**：`done_first_pass`
**已定架构**：不能只看供应商级状态，必须按“供应商 -> 模型 -> route -> adapter -> worker -> 前端入口 -> 输出资产”逐层审计。  
**已完成首版**：

- `shared/aiGatewayModelRoutes.js` 已成为前后端共享 route 规则来源。
- 后端 route guard、model availability summary、前端 modelRouteCatalog 已统一读取共享规则。
- 管理端 Workspace model publishing 已按 `ready/key_missing/parameter_pending/adapter_pending/route_not_found` 显示模型级可用性。
- 保存发布清单前会阻断不可用模型，避免普通工作台看到发布但不可执行的模型。

**需要复核的供应商**：

- OpenAI 官方
- ToAPIs OpenAI-compatible
- ToAPIs Gemini 形态
- 即梦
- Tripo
- 腾讯混元
- Gemini AI Studio / Vertex
- VectorEngine
- 火山方舟

**未收口点**：

- 仍缺一张可导出的 provider/model/route readiness matrix，用于发布前和回归时快速审计。
- `adapter 已接但真实生成测试失败` 还没有进入模型级状态。
- ToAPIs Gemini、腾讯混元、VectorEngine、Gemini AI Studio / Vertex 的模型级参数/输出资产仍需逐个补齐。

**下一步**：

- 生成一张 provider/model/route readiness matrix。
- 对照 shared route、worker adapter、前端 published catalog 做一致性检查。
- 把不一致项写入本清单或供应商闭环审计文档。

**关联**：

- `shared/aiGatewayModelRoutes.js`
- `server/ai-gateway/model-availability-summary.js`
- `services/modelRegistry/`
- `docs/供应商模型发布闭环审计.md`

### B5. Route 与 Adapter 一致性检查待自动化

**状态**：`done_first_pass`
**已定架构**：route readiness 必须和真实 worker/adapter 支持一致。  
**已完成首版**：

- 已新增 shared route -> backend plan consistency 聚焦测试。
- 已新增 provider route -> worker adapter 静态一致性测试。
- `createAiGatewayJobPlan` 会在未显式传 provider 时按共享 route 规则推断供应商，避免视频/3D 错路由到其它供应商。

**未收口点**：

- 还需把一致性检查纳入更宽的 CI/发布前检查。
- 真实生成测试失败后的 route 降级/暂停策略仍未自动化。

**下一步**：

- 增加 route/adapter/worker 静态一致性测试。
- 增加发布清单测试：已发布 + gatewayReady 的模型必须能通过 route guard。
- 对前端有效模型 hook 增加后端摘要对齐测试。

**关联**：

- `shared/aiGatewayModelRoutes.js`
- `server/ai-gateway/model-route-guard.js`
- `tests/workflowAiPickIndex.test.ts`

### B6. 前端所有入口统一模型来源待收口

**状态**：`in_progress`  
**已定架构**：所有工作台入口都应读取同一套“已发布 + 可执行”模型清单。  
**未收口入口**：

- 工作台全局输入框
- 侧栏对话
- 预设页
- 组覆盖
- 分镜重绘/拼图
- 视频生成
- 3D 生成
- 后续音乐入口

**未收口点**：

- 部分入口已读 published/effective hooks，但仍有历史 allowlist、默认 registryId、局部硬编码。
- 提交时仍可能沿用 registryId 兼容链路，canonicalModelId 全链路迁移未完全完成。

**下一步**：

- 列出每个入口的 model source 和 submit payload。
- 统一使用 published + gatewayReady 的 effective model rows。
- 提交统一携带 `canonicalModelId / registryId / providerId / capability`。

**关联**：

- `services/modelRegistry/publishedModelCatalog.ts`
- `components/WorkflowSection.tsx`
- `components/CapabilityPresetSection.tsx`

### B7. 参数能力矩阵待落地

**状态**：`done_first_pass`
**已定架构**：每个模型需要声明支持哪些参数，前端按能力显示参数，不再靠硬编码猜。  
**已完成首版**：

- 新增 `services/modelRegistry/modelParameterCapabilities.ts`，声明图片、视频、3D 的首版参数能力。
- Capability Presets 已接入图片/视频/3D 能力矩阵。
- 方舟 Seedance / Seed3D、腾讯混元 3D、Tripo 3D 已有基础参数差异。

**未收口点**：

- 图片编辑、局部编辑、质量、风格等高级参数仍未完全能力化。
- 视频首帧/尾帧、运动强度、清晰度仍需按供应商逐个校验。
- 音乐参数：时长、歌词、风格、参考音频等后续能力尚未接入。
- 组覆盖、全局输入框、侧栏对话仍需统一读取能力矩阵。

**下一步**：

- 设计 `ModelParameterCapability` schema。
- 将参数 UI 绑定到模型能力矩阵。
- adapter 测试覆盖“UI 参数 -> Gateway input -> 上游请求体”。

**关联**：

- `docs/供应商接入可用闭环开发文档.md`
- `components/CapabilityPresetSection.tsx`
- `components/WorkspaceQuickComposeBar.tsx`

### B8. 输出资产标准化仍需查漏

**状态**：`done_first_pass`
**已定架构**：所有生成结果都应转成标准资产，并带来源供应商、模型、任务 id、参数快照。  
**标准输出目标**：

- 文本输出成文字资产。
- 图片输出成图片资产。
- 视频输出成视频资产。
- 3D 输出成模型资产。
- 音乐输出成音频资产。

**已完成首版**：

- 新增 `GeneratedAssetSourceMeta`，AI Gateway 回填资产会记录 `aiGatewayJobId/providerId/modelId/canonicalModelId/registryId/modality/capability/routeId/adapterId/paramsSnapshot`。
- `extractRestorableAiJobArtifacts` 已支持从 job `output/artifacts` 提取图片、视频、3D、音频 URL，以及 text job 的明确文本输出。
- `buildAiJobRestoreAssets` 已能把文字输出回填成 `assetKind: text`，音乐/音频输出回填成 `assetKind: audio`，并保留原有图片、视频、3D 回填链路。
- 已补 `aiJobArtifacts` / `aiJobArtifactRestore` / `workflowAssetVariants` 聚焦测试，覆盖来源元数据、文字资产、音频资产。

**未收口点**：

- 多文件资产、3D 材质包、视频封面、音频波形/封面等 manifest 契约仍需统一。
- 真实上游 artifact 字段形态仍需用方舟/即梦/Tripo/OpenAI/ToAPIs 等供应商逐个校验。
- 非图片大文件的云端/本地伴侣持久化策略仍需继续细化。

**下一步**：

- 定义多文件 artifact manifest：3D 材质包、视频封面、音频封面/波形、通用附件。
- 用真实上游任务结果校验各供应商 artifact 字段形态。
- 明确非图片大文件的本地伴侣优先、云端显式开启策略。

**关联**：

- `services/aiJobArtifacts.ts`
- `services/aiJobArtifactRestore.ts`
- `types.ts`

### B9. 真实测试分层与错误提示待统一

**状态**：`done_first_pass`
**已定架构**：测试层级和错误提示要面向管理员/普通用户分层展示。  
**已完成首版**：

- Provider Key Smoke Test 返回 `testLayer=key_smoke`、`createsGenerationTask=false`、`nextAction`。
- Tripo 继续使用低成本余额探活；OpenAI-compatible 类供应商（OpenAI 官方、ToAPIs、方舟、VectorEngine）支持 `GET /models` 真实上游探活。
- Model Route Test 返回 `testLayer=route_test`、`createsGenerationTask=false`、`nextAction`。
- 供应商中心增加 Test Layers 说明卡，区分 Credential Check、Upstream Probe、Route Check。
- Workspace model publishing 按钮从 Test Route 调整为 Route Check。

**未收口点**：

- 测试层级：
  - Generation Test：真实生成一个最小任务，仍未产品化。
- 错误提示需要统一覆盖：
  - 缺 Key
  - 模型未发布
  - Gateway 待接
  - 参数未映射
  - 上游限流
  - 上游鉴权失败
  - 余额不足
  - 任务超时
- 管理员和普通用户需要不同粒度的可读提示。

**下一步**：

- 建立错误码到用户文案/管理员文案/修复动作的映射表。
- 管理端所有测试按钮继续统一显示测试类型，并补 Generation Test 风险提示。
- 普通用户任务失败提示隐藏敏感实现细节，但给出可行动说明。

**关联**：

- `services/apiErrorMessages.ts`
- `server/ai-gateway/model-route-test.js`
- `components/admin/AdminProviderKeysPanel.tsx`

### B10. 供应商文档和测试补齐规则待制度化

**状态**：`open`  
**已定架构**：每接一个供应商/模型类型，都必须更新闭环审计文档，并补最小测试集。  
**未收口点**：

- adapter 至少应有：
  - 请求体测试
  - 成功响应测试
  - 失败响应测试
  - route guard 测试
  - 发布清单测试
  - 前端模型清单测试
- 当前不同供应商测试覆盖粒度不一致。

**下一步**：

- 为 provider adapter 建测试模板。
- 在 PR/轮次完成清单里强制勾选文档和测试项。

**关联**：

- `docs/供应商接入可用闭环开发文档.md`
- `docs/供应商模型发布闭环审计.md`
- `tests/`


### B2. 供应商模型扫描仍未自动化

**状态**：`open`  
**已定架构**：供应商维护自己的模型清单，平台可每日扫描或手动刷新，再由管理员发布 canonical model。  
**未收口点**：

- 目前多数 provider model 仍是静态目录。
- 每日扫描、diff、状态变更提醒、下架策略尚未做。
- 扫描结果与 canonical model / route / pricing 的合并规则尚未实现。

**下一步**：

- 先做只读扫描记录表：`providerId/modelId/capabilities/status/firstSeen/lastSeen`。
- 再做管理端 diff：新增、变更、下架、需人工映射。
- 最后接发布候选，不自动发布。

**关联**：

- `services/modelRegistry/`
- `server/data/model-ops-config.json`
- `docs/供应商模型中心优化清单.md`

### B3. BYOK、平台 Key、计费边界未完全产品化

**状态**：`in_progress`  
**已定架构**：供应商中心应区分平台 Key、用户 BYOK、目录可见、Gateway 可执行、计费归属。  
**未收口点**：

- 管理端已有能力状态矩阵，但 BYOK 模型是否进入工作台、是否计平台积分、如何提示用户仍未完全闭环。
- Provider Key smoke test 有第一版，但真实生成级 smoke test 还不完整。
- Key 池事件、可用性摘要、发布阻断之间仍需要更清晰联动。

**下一步**：

- 做 provider/model/route 三级 health summary。
- 管理端保存发布时必须显示阻断原因和修复入口。
- BYOK 模型在工作台用明确标识和计费文案区分。

**关联**：

- `components/admin/AdminProviderKeysPanel.tsx`
- `server/ai-gateway/model-availability-summary.js`
- `server/ai-gateway/model-route-test.js`

## C. 多类型资产卡片与预览

### C1. 资产预览产品化仍未完全统一

**状态**：`in_progress`  
**已定架构**：资产卡片只展示统一封面预览和角标；详情预览按资产类型使用统一 viewer shell，版本树在左侧，右侧只显示资产列表缩略预览。  
**已完成**：

- 多类型资产预览已推进多轮，移除了底部版本缩略图、修复部分 3D/文本居中和常驻输入框问题。
- 3D 默认直接进入 3D 预览，卡片/缩略图显示模型缩略图。

**未收口点**：

- 各资产类型 viewer 的顶部 UI、工具区、状态区仍可能不一致。
- 文本资产阅读器、音乐资产、视频资产、资产集/分镜表这类集合资产的展示规范还需要统一。
- 资产集/分镜表是“资产集合”，需要定义集合封面、内部资产树、子资产操作、版本关系。

**下一步**：

- 给每种资产类型定义 viewer contract：`cover / title / metadata / primaryPreview / actions / empty/error/loading`。
- 集合资产定义 `collectionAsset` 契约：集合本身是资产，内部节点引用子资产。
- 用 Playwright 或截图检查关键类型：text/image/video/model3d/music/asset_set/storyboard_table。

**关联**：

- `docs/多类型资产预览产品化重构规格.md`
- `docs/多类型资产卡片与预览改造清单.md`
- `components/workflow/AssetMediaPreviewCenter.tsx`
- `components/WorkflowSection.tsx`

### C2. 资产版本树与分叉关系未完全结构化

**状态**：`in_progress`  
**已定架构**：版本控制应在左侧树状节点中表达，支持从任意版本分叉或生成多个派生版本；底部和右侧不再重复展示版本。  
**未收口点**：

- 版本树 UI 已按方向修过，但数据层对 `parentVersionId / branchFrom / generatedByJobId` 的结构化仍不足。
- Agent 当前画面问答、资产预览、任务结果回填没有统一引用版本节点。
- 集合资产内部的版本关系尚未定义。

**下一步**：

- 定义 `WorkflowAssetVariant` 或等价版本节点数据契约。
- 生成任务结果写入版本节点时记录父节点、来源 job、输入上下文。
- 预览树、Agent 引用、任务时间线都读同一版本节点。

**关联**：

- `docs/多类型资产预览产品化重构规格.md`
- `components/workflow/WorkflowLightboxAssetThumbStrip.tsx`
- `components/WorkflowSection.tsx`

## D. Agent 与 Copilot

### D1. 工作台 Agent 时间轴 UI 已定但还需持续收口

**状态**：`in_progress`  
**已定架构**：侧栏对话不应按普通聊天倒序挤压用户消息，而应以“用户请求 + AI 产出结果”为一组时间轴块。  
**未收口点**：

- 消息组、子任务、生成结果、失败重试、引用资产之间还需要更稳定的数据结构。
- Agent 子运行需要展示 job id、模型、供应商、产物链接。
- 时间轴和全局输入框应作为一等公民常驻，所有资产预览类型都不能遮挡或消失。

**下一步**：

- 固化 `AgentTimelineGroup` 数据结构。
- 将 childRuns、AI Gateway jobs、资产产物绑定到同一组。
- 做跨资产类型预览的常驻输入框回归检查。

**关联**：

- `docs/网页端项目Agent侧栏-开发规格.md`
- `components/WorkflowSection.tsx`
- `components/workflow/quickComposeChat/`

### D2. 本地伴侣 Copilot 与工作台 Agent 边界仍需落地

**状态**：`open`  
**已定架构**：Pi/Hermes/Codex 是外部大脑司机；Copilot 是桌面壳驾驶舱与 `ac.*` 身体入口；工作台 Agent 是项目级业务编排。  
**未收口点**：

- Copilot Chat v1、工具卡片、确认/审计、会话恢复、信任规则仍未实现。
- 与工作台 Agent 的上下文共享和权限边界还没落地。

**下一步**：

- 先做 Copilot Chat v1：流式、消息状态、工具卡片、取消/重试、会话恢复。
- 再接 `ac.*` 工具权限和审计。

**关联**：

- `docs/本地伴侣-Copilot基础设施改造文档.md`
- `docs/本地伴侣-全局Agent规格.md`

## E. 可观测、计费与任务中心

### E1. AI Gateway 任务中心仍缺完整产品闭环

**状态**：`in_progress`  
**已定架构**：AI Gateway job 是执行权威记录，应支持状态、取消、重试、artifact 回填、费用、provider/model/route 可观测。  
**未收口点**：

- 用户侧任务中心已有基础列表和回填，但还缺生成过程和工作台结果之间的完整关联展示。
- 精确 usage event 结算仍有部分 fallback。
- provider/model 级自动熔断、TTL 恢复、一键应用建议尚未完成。

**下一步**：

- 任务卡展示：模型、供应商、route、费用、产物、关联资产。
- provider/model 级 ops control 增加 TTL、建议一键应用、审计事件。
- 精确 usage event 覆盖更多 provider adapter。

**关联**：

- `server/ai-gateway/`
- `services/aiJobsClient.ts`
- `services/aiJobsStore.ts`
- `services/aiJobArtifactRestore.ts`
- `docs/adr/可观测性与计费统一架构-v1.md`

## 收口检查清单

某项可以从本文件移出或标 `done`，必须同时满足：

- 用户路径能跑通，不只是目录/管理端可见。
- 管理端状态、前端选择器、执行 Gateway、adapter、artifact 回填一致。
- 有最小自动化测试或明确手测记录。
- 关联设计文档和 `docs/交接文档.md` 已同步。
- 若涉及 AI 执行入口，`services/workflowAiPickIndex.ts` 状态已更新。
