# 项目交接文档（assetcutter-ai-pro）

本文面向后续开发者与 AI 助手：**快速理解仓库、关键模块、约定与扩展方式**。与具体需求无关的细枝末节以代码与 `.cursor/rules` 为准。

## 1. 项目概览

- **技术栈**：React 19 + TypeScript + Vite 6；部分能力使用 **Three.js**（全景、3D 预览等）。
- **仓库路径**：`assetcutter-ai-pro`（私有）。
- **运行**：`npm run dev`（Vite）；多服务见 `package.json`（`dev:all`、`dev:with-r2` 等）。

## 2. 关键目录


| 路径                                             | 说明                                                                                                                                                                                                                                                   |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `App.tsx`                                      | 主应用壳、路由级区块组合                                                                                                                                                                                                                                         |
| `components/WorkflowSection.tsx`               | 工作流核心（资产网格、组、拖拽、大图预览入口等），体量很大                                                                                                                                                                                                                        |
| `components/workflow/`                         | 工作区按域：`workflowConstants`、`workflowIds`、`workflowSectionHelpers`、`workflowSectionUiConstants`、`workflowOutlineUtils`、`workflowCardAspect`、`workflowCapabilityGroups`、`workflowPaneLayout`、`WorkflowSidebarColumn`、`WorkflowErrorBoundary`、`modals/*` |
| `components/WorkflowModeShell.tsx`             | 工作区模式外壳：IDB 遮罩、项目列表、已选项目时的 `WorkflowErrorBoundary` + `Suspense`；`renderWorkflowSection` 仅在已选项目时调用，避免过早拉取懒加载 chunk                                                                                                                                           |
| `components/ui/LazySectionFallback.tsx`        | 各 `Suspense` 区块共用的「加载中」占位                                                                                                                                                                                                                            |
| `hooks/useWorkflowMainScrollCapture.ts`        | App 主滚动层与工作区框选/留白横滑的 **ref 桥接**（原内联于 `App.tsx`）                                                                                                                                                                                                      |
| `hooks/useWorkflowWorkspacePanes.ts`           | 工作区四栏横向分页：滚轮切页、轨道 `transform` 与吸附、首访横扫、`Space` 平移、数字键切页、视口触摸横滑；由 `WorkflowSection` 消费                                                                                                                                                                |
| `hooks/useWorkflowMarquee.ts`                  | 工作区/仓库框选（marquee）与 Portal 遮罩 DOM 同步、mouseup 选区合并逻辑                                                                                                                                                                                                   |
| `components/workflow/workflowDomUtils.ts`      | `isWorkflowEditableTarget`：快捷键/手势与可编辑区让出判断                                                                                                                                                                                                           |
| `components/ImagePreviewOverlay.tsx`           | 全屏大图：平面缩放/平移、滚轮切资产与版本、全景切换                                                                                                                                                                                                                           |
| `components/preview/`                          | **预览子系统**：`PreviewShell`、`registry` 懒加载、`types`、`viewers/ImageEquirectViewer`                                                                                                                                                                        |
| `components/EquirectangularPanoramaCanvas.tsx` | 等距柱状全景（Three.js + OrbitControls），带纹理缩图降内存                                                                                                                                                                                                            |
| `services/`                                    | 业务服务（含 `workflowImageDisplay`、`settingsStore`、`clientPersist` 等）                                                                                                                                                                                     |
| `server/`                                      | Node 侧 API（auth、R2、代理等）                                                                                                                                                                                                                              |
| `.cursor/rules/`                               | 项目级 AI 规则（下拉 UI、跨设备 URL、工作流卡片等）                                                                                                                                                                                                                      |
| `docs/错题本.md`                                  | 问题与修复记录                                                                                                                                                                                                                                              |
| `docs/交接文档.md`                                 | 本文                                                                                                                                                                                                                                                   |


## 3. 重要约定（必读规则摘要）

- **下拉 UI**：禁用原生 `<select>`，用自定义 Portal 下拉（见 `CapabilityPresetSection` / `dropdown-ui-style` rule）。
- **跨设备 URL**：持久化数据勿写死 `localhost`；站内 API 路径在不同端口需改写为当前 `origin`（见 `cross-device-availability` rule）。
- **客户端持久化范式**：`localStorage` / `sessionStorage` 的新增读写优先走 `services/clientPersist.ts`（safe 包装、JSON/开关、按账号 `scopedStorageKey`）；工作区/项目/对话仍用专用 store（`workspaceProjectStore`、云同步、`dialogSessionStore` 等）。详见 `.cursor/rules/client-persist.mdc`。
- **工作流卡片预览**：`WorkflowGridImage` / `ProgressivePreviewImage` 父级须有确定高度 + `aspectRatio` 等（见 `workflow-asset-cards` rule）。
- **示例项目**：`示例项目/` 仅作参考，**禁止**在产品代码中 import 或依赖该目录工程。

## 4. 预览功能架构（扩展指引）

- **壳层**：`PreviewShell` — 遮罩、Esc、焦点、`focusKey` 切资产时 refocus。
- **注册表**：`getLazyImagePreviewViewer` / `registerImagePreviewLoader` — 按 `mode` 懒加载 Viewer（全景为 `image.equirect`）。
- **新增预览类型**：新增 `viewers/*.tsx` → 注册 mode → 扩展 `preview/types.ts`；重包体能力务必 **lazy**，并在 `previewPolicyForMode` 中声明是否占用全局滚轮等。
- **3D**：`UnifiedModelViewer3D` / `ModelViewer3D` 与工作流大图入口不同，长期可并入同一预览抽象。

## 5. 环境与构建

- 环境变量示例：`.env.example`；本地敏感配置：`.env.local`（勿提交）。
- 质量检查：`npm run typecheck`、`npm run lint`、`npm run test`、`npm run build`。
- **静态前端双托管（可选）**：同一仓库可同时接 **Vercel** 与 **Cloudflare Pages**。构建均为 `npm run build`，输出目录 `**dist`**。Vercel 使用根目录 `vercel.json` 做 SPA 回退；Pages 使用构建产物中的 `**_redirects**`（源文件 `public/_redirects`，由 Vite 复制到 `dist`）。两边需在控制台分别配置相同的生产用 `VITE_*` 变量；Render 上 `AUTH_ALLOWED_ORIGINS` 须包含两个前端的 Origin（及未来自定义域名）。

## 6. 后续开发注意

- **大文件**：`WorkflowSection.tsx` 修改前先局部搜索，避免重复逻辑；能抽 hooks/子组件时优先降低单文件体积。
- **内存**：工作流中减少超长 base64 常驻；全景已做纹理 cap，新增 WebGL 能力需考虑 **dispose** 与 **并发预览数**。
- **文档**：会话交接与错题按项目 skill `session-handoff-log` 维护 `docs/错题本.md` 与本文件。

## 7. 变更日志（会话/文档）

- **2026-04-04**：新增 `docs/错题本.md`、`docs/交接文档.md`、项目 skill `.cursor/skills/session-handoff-log/SKILL.md`、全量规则 `.cursor/rules/session-handoff-log.mdc`。约定：每轮对话按 skill 维护错题本与交接文档；**用户开始新话题时**先归档上一轮再处理新请求。**新话题（本条）**：建立错题本、交接文档、skill 与规则的自动化协作流程。
- **2026-04-04**：**新话题**：评估站点底层逻辑是否混乱，以及对模块化、统一性、可扩展/可复用的现状与改进方向（纯问答，无代码变更）。
- **2026-04-04**：按域拆分启动：`hooks/useWorkflowMainScrollCapture.ts` 承接 App 主滚动 capture；`components/workflow/` 承接 `WorkflowSection` 顶部常量与纯函数（行为不变）。
- **2026-04-04**：工作区弹窗迁至 `components/workflow/modals/`；`WorkflowErrorBoundary` 迁至 `components/workflow/WorkflowErrorBoundary.tsx`；`LazySectionFallback` 迁至 `components/ui/LazySectionFallback.tsx`；工作区模式 UI 迁至 `WorkflowModeShell`（`App` 用 `renderWorkflowSection` 注入懒加载 `WorkflowSection`）。
- **2026-04-04**：`WorkflowSection` 再拆：`workflowSectionUiConstants.ts`（`SET_ACTION_PREFIX`、区段/按钮/吸附样式常量）、`workflowOutlineUtils.ts`（大纲排序与栈/可展开组纯函数）。
- **2026-04-04**：工作区画卷分栏与框选从 `WorkflowSection` 抽出：`useWorkflowWorkspacePanes`、`useWorkflowMarquee`、`workflowDomUtils.ts`；组件内删除重复 pane/marquee 实现，视口触摸统一 `{...workspaceViewportTouchHandlers}`；粘贴/拖放让出可编辑区改用 `isWorkflowEditableTarget`。
- **2026-04-04**：工作区主网格/组内瀑布列曾试 `column-fill: auto`，在**滚动容器高度为 auto** 时 Chrome 等会退化成**单列**，已改回 `balance`；缩略：`sessionStorage` 预热比例、上传前定比例、`deferThumbnail` + 预取 IO；另增**严格视口** `thumbHotKeys`（无 rootMargin）+ `runPreviewThumbDecode` 全局队列（high 优先、默认并发 3），微图/小图 `drawDataUrlToCanvas` 先入队，避免屏外大批解码抢首屏。
- **2026-04-04**：`ProgressivePreviewImage` 微图 `<img>` 补 `onLoad → onIntrinsicSize`，避免小图未出前父级一直 `aspectRatio: 1` 把横图压在方形容器里；`WorkflowSection` 用 `mergeCardAspectFromIntrinsic` 统一写入（仍夹 1:2～2:1），并允许用真实比例覆盖 session 里误存的 `1`。
- **2026-04-04**：继续瘦身 `WorkflowSection`：卡片比例 session 与 `mergeCardAspectFromIntrinsic` 迁至 `workflowCardAspect.ts`；能力预设按类目分组迁至 `workflowCapabilityGroups.ts`；顶栏双列 grid 迁至 `workflowPaneLayout.ts`；右侧「规划条 + 组/复制/删除/归档 + 常用功能 + 能力块网格 + 复合能力」迁至 `WorkflowSidebarColumn.tsx`（由父组件注入状态与回调）。
- **2026-04-04**：统一浏览器端持久化范式：新增 `services/clientPersist.ts`（local/session 安全读写、JSON、`scopedStorageKey`、工作流常用功能键名）；`workflowCardAspect`、`useWorkflowWorkspacePanes`（首访横扫标记）、`WorkflowSection`（常用功能 id 列表）已迁移；规则见 `.cursor/rules/client-persist.mdc`。
- **2026-04-04**：工作流批处理进行中，新任务可能只留在 `pending`、未写入当前 `executingQueue.tasks`，仍会显示「排队中」。取消按钮条件已扩展：`showRootQueueCancelBtn` / `showGroupQueueCancelBtn` 在「批内排队」或「仅 pending 且不在本批 tasks」时均显示 ×，分别调用 `cancelQueuedTaskInBatch` 与 `setPending` 过滤。
- **2026-04-04**：`settingsStore` 经 `clientPersist` 读写；`safeLocalStorage` / `safeSessionStorage` 改为走 `globalThis`，与单测对 `globalThis.sessionStorage` 的 mock 一致。
- **2026-04-04**：`capabilityPresetStore`、`capabilitySetStore`、`featureFlags` 同步收拢至 `clientPersist`（预设迁移键 `ac_custom_modules` 仍由 `removeLocalKey` 清理）；对外 API 不变。
- **2026-04-04**：`clientPersist` 增加 `**writeLocalStringOrThrow`**；`recordStore`、`snippetStore`、`abChoiceStore`、`promptTemplateStore`、`storePackHistory`、`userUiPrefs`、`dialogSessionStore`、`workspaceProjectStore` 已全部经 `clientPersist` 读写；`services` 内不再直接调用 `localStorage`（除 `clientPersist` 实现）。
- **2026-04-05**：**新话题**：讨论为每张图片增加元数据（上传信息、图像技术信息、流程中 AI 识别/tag）以支持库管理与后期筛选；纯方案与现状对齐，暂无代码实现。
- **2026-04-05**：延续「全站图片一致行为」：新增 `components/SiteImage.tsx`（`workflowSafeImgSrc` + `capabilityPreviewAlternateUrls` 多候选重试）；`ProgressivePreviewImage` 直链分支与多处裸 `<img>` 改为复用；`CapabilityPreviewImg` 委托 `SiteImage`；`WorkflowCapabilityHoverPreview` 统一用 `ProgressivePreviewImage`。
- **2026-04-05**：会话纪要 skill `.cursor/skills/session-handoff-log/SKILL.md` 与规则 `.cursor/rules/session-handoff-log.mdc` 补充：**维护错题本/交接文档时优先读文末或必要片段**，非必要不全文加载。
- **2026-04-05**：session-handoff skill 增补：**沿用本 skill + rule + 两文档这一条链路演进，不平行新建第二套纪要**；错题**升格**到 rule/lint/小颗粒 skill，错题本标注「已升格」。
- **2026-04-05**：按 Cursor create-skill 规范复查项目 skills：`session-handoff-log` 的 `description` 改为第三人称+触发场景；`ui-ux-pro-max` 补全 YAML `name`/`description`；`threejs-interaction`、`threejs-shaders` 的 SKILL 超 500 行，建议后续拆 `reference.md`。
- **2026-04-05**：为 **Cloudflare Pages** 与 Vercel 并行部署增加 `public/_redirects`（`/* /index.html 200` SPA 回退）；交接文档 §5 补充双托管与 CORS/`VITE_*` 注意事项。
- **2026-04-05**：会话纪要 — 上轮：Cloudflare 插件（Skills/MCP/斜杠命令）概览；**新话题**：前端已上 Cloudflare Pages 并已注册域名，咨询绑定与后续配置步骤（纯说明，无代码变更）。
- **2026-04-05**：**新话题**：对话页修复生图结果不显示（`ProgressivePreviewImage` 父级需高度）；左侧会话行缩略图改为标题卡片内**低对比模糊背景**（不单独占一列），与 `.cursor/rules/workflow-asset-cards.mdc` 中 Progressive 容器约定一致。
- **2026-04-05**：对话页跟进：会话标题与用户气泡文字在部分环境下被盖住或难辨认 → **背景层与可点击标题分离**（`isolate` + `z-0`/`z-[1]`）、去掉标题区 `backdrop-blur`；用户消息 `**overflow-visible` + 明确文字色**、附图区单独裁剪。
- **2026-04-05**：对话数据：`updatedAt` 异常导致会话从「最近/更早」双桶漏出、列表空白；`**activeSessionId` 陈旧**导致当前会话无 `messages`、发送写不入 → **规范化时间戳 + 校验活动 id + 更早会话默认展开**（`useDialogWorkspace` / `App.tsx` 列表 `activityAt`）。
- **2026-04-05**：对话 UI：修复生成图「只露一条」（`aspect-ratio` 与内层 `min-h` 冲突）；会话行 **最后一张生成图** 作模糊底图时支持 **仅 R2 key**（`DialogSessionRowBackdrop` + `downloadR2ObjectAsDataUrl`）；用户附图 `**max-h` 放宽**；纯附图无文案时显示占位句。
- **2026-04-05**：对话内 **生成图 / 用户附图** 点击打开与临时库相同的 **全屏大图预览**（`openDialogImagePreviewFromChat`：优先复用 `dialogTempLibrary` 同 `sourceMessageId`+`sourceType` 项，否则 `dialogChatImagePreview` 合成项）；关闭统一 `closeDialogImagePreview`；滚轮切图仅在从临时库打开时生效。
- **2026-04-05**：对话临时库大图预览增加与工作区 `ImagePreviewOverlay` 一致的 **平面 / 全景** 切换（`getLazyImagePreviewViewer('image.equirect')` + `previewPolicyForMode` 滚轮仲裁；换图或关闭时重置为平面）。
- **2026-04-06**：会话纪要 — 上轮：对话页会话卡背景清晰度/亮度优化 + 大图预览接入全景切换；**新话题**：移除能力预设顶部预览区域并清理相关代码。
- **2026-04-06**：`CapabilityPresetSection` 已移除顶部预设预览条（含分组快捷按钮、拖拽同组排序、右键启停、点击定位），并清理相关状态/回调（`presetStrip`、`STRIP_DRAG_MIME`、`onStripPresetContextMenu`、`reorderPresetsInCategory`、`reorderSetsAt`、`suppressStripClickRef`）。
- **2026-04-06**：会话纪要 — 上轮：能力预设顶部预览条已移除并完成构建验证；**新话题**：能力模块预设改为工作区瀑布流（预览图主导），点击后再展示详情与编辑。
- **2026-04-06**：能力预设 UI 改为 **瀑布流预览卡片**（多列 Masonry，预览图主导）；点击卡片打开右侧抽屉式详情（参考图风格）后再展示信息，并支持基础编辑（名称/启用/提示词）、上传预览图、运行测试、上传到 R2、删除与预览放大。
- **2026-04-06**：预设详情交互改版：从“右侧抽屉”调整为**全屏弹层双栏**（左侧大图预览、右侧详细信息与功能按钮），支持在右侧直接编辑提示词并保存。
- **2026-04-06**：能力预设卡片占位对齐工作区逻辑：按图片固有宽高比动态写入 `aspectRatio`（`onIntrinsicSize` + `mergeCardAspectFromIntrinsic`），卡片预览改 `object-contain`，恢复真实瀑布流高度差，避免固定高度看起来像网格。
- **2026-04-06**：预设详情页“编辑提示词”恢复为**完整设置编辑**：分类、执行方式（内置/生图）、生图档位、比例尺寸、理解开关、3D 参数、切割溢出（`cut_image`）等均在右侧可改，保存复用原 `saveEdit` 逻辑。
- **2026-04-06**：预设大图详情视觉优化：右侧默认展示参数概览（只读）并统一蓝灰按钮体系；点击“编辑参数”后才进入可编辑状态，减少误改并贴近参考图交互节奏。
- **2026-04-06**：会话纪要 — 上轮：能力预设大图详情视觉打磨与参数可见性优化；**新话题**：删除功能区「阶段 B 实验」相关代码（提示词编译器/流程建议）。
- **2026-04-06**：已删除功能区「阶段 B 实验」代码：移除 `WorkflowSidebarColumn` 中 `WorkflowPlannerBar` 挂载与相关 props；`WorkflowSection` 清理 Planner 目标资产与 Prompt Compiler 执行上下文（集合/能力执行统一回落 `onLog`）；删除文件 `components/WorkflowPlannerBar.tsx` 与 `services/featureFlags.ts`。
- **2026-04-08**：本地**全套配套**启动：`npm run dev:all`（`concurrently`：Vite **3000**、贴图修缝 **127.0.0.1:8008**、`dev:auth-backend` **9100** + `.env.local`）。若此前已单独起过 `npm run dev` / auth，需先释放端口再跑 `dev:all`，避免 **EADDRINUSE**。`scripts/start-seam-backend.js` 在存在 `lib/site-packages` 时自动设置 **PYTHONPATH**，与在仓库根目录 `pip install -r WebSeamRepair/backend/requirements.txt`（或装到 `lib/site-packages`）的布局一致。**未**纳入 `dev:all` 的脚本（按需另开终端）：`npm run proxy`（ai3d）、`npm run dev:gemini-proxy`、`npm run dev:r2-api`（独立 R2；当前 Vite 默认把 `/api/r2` 代理到 **9100** 的 auth-api）。
- **2026-04-08**：能力按 **输入→输出** 分为 `**text_to_text`（文生文）、`text_to_image`（文生图）、`image_to_image`（图生图）、`image_to_text`（图生文）** 与 `**generate_3d`**；图生图下再选内置处理或生图模型。预设存储 **v4**，`normalizeCapabilityPreset` 将旧 `image_gen`/`image_process`/`text_llm` 迁入新类目（原「单一 text_llm 同时兼容图生文」需另建 `**image_to_text`** 预设）。拖放校验 `services/workflowTextAsset.ts`；执行与「词」列 `capabilityUsesGenImageEngine` 见 `services/capabilityExecutor.ts`。
- **2026-04-08**：工作区功能区分组新增「覆盖参数」：支持按分组覆盖 **档位 / 比例 / 尺寸 / 理解**，拖拽入队（含「词」微调确认后）会把覆盖值写入 `WorkflowPendingTask`（`overrideImageGear` / `overrideImageAspectRatio` / `overrideImageSize` / `overrideSkipUnderstand`），执行时在 `runTask` 临时覆盖预设生图参数，不改动原预设本体。
- **2026-04-08**：工作区支持**文字资产卡片**：`WorkflowAsset` 增加可选 `assetKind: 'text'` 与 `textTitle` / `textBody`（`types.ts`）；顶栏「添加文字」、卡片与大纲展示、全屏编辑（`WorkflowTextAssetOverlay.tsx`）；`getAssetDisplayImage` 对文字返回空串；**不参与**图像组并组；持久化随项目 `assets` JSON。辅助：`services/workflowTextAsset.ts`。
- **2026-04-08**：Skill `plain-language-to-dev-brief`：**唤醒词「虾米」**（句首，如「虾米，我想…」）或 @ 本文件即启用；无关键分叉时**直接在本会话改代码**，不单发长简报；关键分叉仍 A/B/C/D + 回复 1～4；用户明确「只要说明不改代码」时只输出一行【执行指令】。**固定提醒**：`.cursor/rules/xiami-plain-language.mdc`（`alwaysApply: true`）指向该 Skill。
- **2026-04-08**：会话纪要 — 上轮：功能区分组覆盖参数 UI 紧凑化、文字资产版本化与大图元数据显示；**新话题**：为生成图片建立多维标签体系（自动打标 + 归档检索筛选）。
- **2026-04-08**：图片标签体系（首版）：`WorkflowAsset.imageTags` 按“版本键”保存扁平标签；新增 `services/workflowImageTags.ts` 在生成结果落盘时自动打标（维度含 `subject/style/lighting/composition/mood/usecase/action/pipeline`）；资产仓库增加标签检索输入（按 label + tags 搜索），归档条目显示前 4 个标签。
- **2026-04-08**：图片标签升级为**低成本二段式**：第一段规则粗标（生成时写入 `imageTags` + `imageTagStage=coarse`）；第二段在**归档动作**触发文本上下文精修（不传图，`gemini-3-flash-preview` 输出结构化 tags），回写 `imageTags` 并标记 `imageTagStage=refined`，同版本只精修一次并有失败兜底保留粗标签。
- **2026-04-08**：工作区/仓库资产改为**同源数据**：`WorkflowAsset` 新增 `inRepository`（true=仅仓库列显示，false=工作区列显示）；`WorkflowSection` 仓库列改从 `assets` 过滤渲染，不再依赖外部 `libraryItems` 数据源；“放到仓库/导入工作区”改为切换该布尔值（保留 `onOpenLibraryPicker` 兼容外部导入）。
- **2026-04-08**：移除工作区「已完成」视图入口；工作区仅显示未归档且 `inRepository !== true` 的根资产。执行“归档”时资产会被标记为 `archived: true` 且 `inRepository: true`，因此从工作区隐藏并在资产仓库显示。
- **2026-04-08**：资产仓库卡片升级为与工作区根卡片一致的主交互：按真实宽高比预览、文字卡样式、组叠层与角标、滚轮切版本/组内预览、底部版本元信息与“丢弃当前版本”；仓库保留“点击勾选导入工作区”的选择语义。
- **2026-04-08**：资产仓库移除“多选导入”逻辑（含勾选态、计数与批量导入按钮）；点击仓库卡片或仓库大纲行即单条导入工作区。`useWorkflowMarquee` 在第 0 页不再维护仓库选择集合。
- **2026-04-08**：仓库页大纲新增「列表 / 标签」视图切换：标签视图显示可点击标签并支持多选高亮；所选标签作为 AND 条件参与仓库筛选，左侧仓库卡片与大纲列表统一使用同一筛选结果（单击标签即筛出对应内容，支持清空标签）。
- **2026-04-08**：仓库点击行为改为**仅预览**（卡片/大纲点击与工作区一致：组资产进组视图、普通资产开大图），不再点击即导入。仓库导入工作区改为显式拖放触发，且为“复制到工作区”而非移动：归档原件持续保留在仓库。
- **2026-04-08**：大图预览中「生成记录」改为右侧常驻面板：移除“生成记录”按钮与弹层二次打开；进入图片预览即在右侧展示记录，底部操作区自动让出右侧宽度。`WorkflowGenerationRecordPanel` 新增 `mode="inline"` 内嵌模式（保留原 `modal`）。
- **2026-04-08**：标签改为中文持久化：`workflowImageTags` 输出与精修统一写入“中文维度:中文值”（如 `风格:写实`），并在 `WorkflowSection` 启动时自动把历史英文标签迁移为中文后回写 `assets.imageTags`，后续随资产保存/同步。
- **2026-04-09**：工作区主内容区（`max-w-6xl`）**右侧留白**支持将功能区能力块拖放以**禁用该预设**（`enabled: false` + `saveCapabilityPresets`）：侧栏拖起时额外写入 `application/x-ac-capability-action`（`services/workflowDragPipeline.ts`），`useWorkflowMainScrollCapture` 在 capture 阶段识别该类型与指针位置；复合能力 `set:` 拖入留白不处理。禁用后 `WorkflowSection` 会同步从常用功能 id 列表剔除对应预设。
- **2026-04-09**：工作区**能力列**底部预设卡片（`CapabilityPresetSection` + `embeddedInWorkflow`）可拖向**功能区侧栏**任意处：写入 `application/x-ac-capability-from-editor`，`WorkflowSidebarColumn` 根节点 `capture` 接 drop；若预设已禁用则先 `onUpdateCapabilityPresets` 启用并持久化，再按当前工作区选中（根多选或组内多选）等同「拖图到该能力」入队；无选中时仅提示日志。
- **2026-04-09**：**工作流创建**：将功能区一能力拖到**另一能力主区域**（非「词」列）打开全屏编排（`WorkflowComposerOverlay` + `PreviewShell`，风格贴近大图预览壳层）。内嵌 `CapabilitySetCanvas`：左侧拖预设/文本生成，画板连线；默认含「原始图片」输入与 1 个**输出节点**，输出类型用 `CustomDropdown` 选 `CAPABILITY_CATEGORIES` 中类型，侧栏「添加输出节点」可增多个；`CapabilitySetNode.data.outputCategory` 持久化。初始图由 `buildWorkflowComposerSeedFromTwoPresets` 生成（输入→A→B→输出）。保存经 `onUpdateCapabilitySets` 写入复合能力列表。
- **2026-04-14**：会话纪要 — 上轮：能力块可拖到资产卡并直接执行（不再仅入队）；**新话题**：功能区分组新增“生成数量”，按数量复制资产并并行生成，结果分别回填到复制卡（含输入图与结果图）。
- **2026-04-14**：工作区功能区分组覆盖参数新增 **生成数量（1~6）**：对生图能力拖拽入队时，数量 >1 会自动为根资产复制卡片并创建手动组，每张卡片各自入队同一能力；「词」微调入口同样支持按数量扩增任务。涉及：`components/workflow/WorkflowSidebarColumn.tsx`、`components/WorkflowSection.tsx`。
- **2026-04-14**：生成数量交互调整：数量参数与“覆”开关解耦（不开覆盖也可生效），位置移至“覆”左侧；下拉支持 `1~10` +「自定义…」。执行前若数量 >20，弹出二次确认（普通拖拽与「词」微调均生效）。
- **2026-04-14**：修复组内执行态一致性：自动复制并成组后的子卡，忙碌态判定改为同时识别 `sourceGroup` 与 `assetId` 任务；组内卡执行/排队时禁用拖拽与滚轮切换，遮罩/取消逻辑与组外根卡一致。
- **2026-04-14**：Vertex 代理超时窗口对齐：`bulkProxyGenerateContentAsync` 轮询上限从最小 540s 调整为至少 600s（`GEMINI_ASYNC_CLIENT_MAX_POLL_MS`），与 `server/gemini-proxy-api.js` 的异步任务最大等待（默认 590s）匹配，避免“服务端仍在重试，前端先超时”。
- **2026-04-14**：生图阶段新增“盒子批处理”：前端 `geminiService` 在走 bulk 代理时将 `dialogGenerateImage` 请求按队列凑批（默认 3 个）后通过 `**POST /proxy/gemini/async-batch`** 一次提交；代理端新增 `async-batch` 创建/轮询接口，批内每项独立重试并返回逐项结果，减少多资产/多数量时的请求抖动与头开销。
- **2026-04-14**：批处理盒子大小改为可配置并按供应商默认：Vertex 默认 4、其它默认 3；支持构建环境变量 `VITE_GEMINI_IMAGE_BATCH_BOX_SIZE`（通用）与 `VITE_GEMINI_IMAGE_BATCH_BOX_SIZE_VERTEX`（Vertex 专用，1~20）；每次发批输出日志 `"[gemini-batch] dispatch size=... box=... provider=..."`。
- **2026-04-14**：工作流执行改为“批次屏障”策略：`executePending` 按并发窗口分批，同批 `gen_image` 任务共享 `batchGroup`；`geminiService` 仅在同组到齐后发批（若有任务在理解阶段失败，8s 后兜底放行），实现“先做一批理解，再整批发生图”的调度语义。
- **2026-04-14**：工作流理解并发对齐小盒子容量：`executePending` 改为动态分批，若批次首任务为 `gen_image`，则批次大小使用 `getGeminiImageBatchBoxSizeForCurrentProvider()`（Vertex 默认 4）；非生图批次仍走常规并发 3，日志改为同时打印两类并发上限。
- **2026-04-14**：修复 Vertex 同批“部分成功部分失败（The operation was cancelled.）”：前端 `geminiService` 新增 `GEMINI_VERTEX_IMAGE_TIMEOUT_MS`（默认 300s）并在 Vertex 生图请求中作为单项超时下限；前后端重试判定均纳入 `The operation was cancelled / canceled / CANCELLED`，失败项可退避重试，降低批内半失败概率。涉及：`services/geminiService.ts`、`server/gemini-proxy-api.js`。
- **2026-04-14**：会话纪要 — 上轮：Vertex 批内 `cancelled` 通过“单项超时抬高 + 可重试”修复；新话题：文字资产卡片打开界面对齐参考图片资产样式（仅展示，不需要缩放/平移）。
- **2026-04-14**：文字资产大图弹层 UI 对齐图片资产预览风格：改为「中间主展示 + 右侧信息/版本区 + 底部操作条」，支持预览/编辑切换与保存；保留版本切换、丢弃当前版本、滚轮切资产能力；明确不引入缩放/平移交互。涉及：`components/workflow/WorkflowTextAssetOverlay.tsx`。
- **2026-04-14**：文字资产弹层继续对齐图片预览版式细节：新增左上信息卡（资产类型/版本/字数/行数）、中区改为左右双侧栏包夹的主展示布局、底部操作条按主展示区重新居中，整体层级与间距更接近图片资产预览页面。涉及：`components/workflow/WorkflowTextAssetOverlay.tsx`。
- **2026-04-14**：按对比图再收敛：将左上信息并入右侧栏；主展示面板透明度改回与图片预览一致（`bg-[#0f0f12]/98` + `backdrop-blur-[2px]`）；补齐左下能力按钮列（文字可用能力）并复用与图片预览一致的快捷执行逻辑。涉及：`components/workflow/WorkflowTextAssetOverlay.tsx`、`components/WorkflowSection.tsx`。
- **2026-04-14**：文字资产弹层中区主展示块定位调整为按整页水平居中（不再按“扣除右侧栏宽度”的剩余区域居中），解决视觉偏左。涉及：`components/workflow/WorkflowTextAssetOverlay.tsx`。
- **2026-04-14**：文字资产弹层底部操作条对齐中区中心线：定位改为 `left: 50%` + `translateX(-50%)`，宽度改为整页口径（`min(58rem, calc(100vw - 3rem))`），修复与中区错位。涉及：`components/workflow/WorkflowTextAssetOverlay.tsx`。
- **2026-04-14**：文字资产弹层背景亮度对齐图片预览：遮罩由 `bg-black/80` 调整为 `bg-black/72`（保留 `backdrop-blur-sm`），整体观感不再偏暗。涉及：`components/workflow/WorkflowTextAssetOverlay.tsx`。
- **2026-04-14**：图片预览弹层改为整页居中：`ImagePreviewOverlay` 在工作区调用处取消右侧占位偏移（`contentRightInset` 改为 `0px`）；底部按钮条改为 `left: 50%` + `translateX(-50%)` 且宽度按整页口径计算，修复主图与底栏共同偏左问题。涉及：`components/WorkflowSection.tsx`。
- **2026-04-14**：修复文字资产“当前显示版本入队失真”：`workflowAssetToInputText` 从“固定读取 `textBody`”改为“按 `displayKey` 读取正文（扩写/改写版本优先）”，缺失时回退原始正文；新增 `workflowTextAsset` 单测覆盖当前版本与回退路径，避免再回归。涉及：`services/workflowTextAsset.ts`、`tests/workflowTextAsset.test.ts`。
- **2026-04-14**：功能区「文生文」分组新增生成数量按钮（与生图分组同款数量控件）；并让数量在文生文执行链路生效：同一文字资产可按数量连续入队多次，生成多条文本版本。涉及：`components/workflow/WorkflowSidebarColumn.tsx`、`components/WorkflowSection.tsx`。
- **2026-04-14**：修正文生文数量行为与生图完全对齐：数量>1 时改为“自动复制文字资产 + 自动成组 + 每张卡片独立回填结果”；根因是数量扩增与成组函数默认过滤文字资产，现已按 `text_to_text` 分支显式放开。涉及：`components/WorkflowSection.tsx`。
- **2026-04-14**：修复组内文字卡显示与组外不一致：组内子资产渲染分支补齐文字卡样式（标题/正文预览、绿色边框与角标），不再按图片卡渲染导致空白占位。涉及：`components/WorkflowSection.tsx`。
- **2026-04-14**：优化“全文字组”封面预览：根级组卡在成员全为文字资产时，封面改为文字预览（取当前轮播项的标题/正文），避免组卡出现黑空白占位。涉及：`components/WorkflowSection.tsx`。
- **2026-04-14**：资产显示逻辑补齐“文本+图片同资产”场景：卡片预览与能力拖放判定不再把 `assetKind=text` 视为“必无图”；文本能力按是否有文本内容判断，图像能力按是否有图像内容判断，支持同资产同时承载文本与文生图结果。全屏预览统一走 `ImagePreviewOverlay`，文本无图时自动生成文本封面图作为预览底图。涉及：`services/workflowTextAsset.ts`、`components/WorkflowSection.tsx`。
- **2026-04-14**：全屏预览模式改为“按当前版本类型切换”：当前版本是文本时使用文本弹层（无缩放/平移，支持滚轮切版本）；切到有图版本时自动切换到图片预览弹层（含缩放/平移与图片工具条）。同时 `getAssetDisplayImage` 对文字资产支持读取图片结果，确保“同资产文本+图片”可正常切换。涉及：`components/workflow/WorkflowTextAssetOverlay.tsx`、`components/WorkflowSection.tsx`。
- **2026-04-14**：生成数量下拉选项文案微调：去掉数字后的“张”字，统一显示为纯数字（1~10）。涉及：`components/workflow/WorkflowSidebarColumn.tsx`。
- **2026-04-14**：会话纪要 — 上轮：文字/图片预览弹层视觉对齐与居中完成；新话题：文字资产在选择扩写版本后执行「变体」仍错误使用原始版本文本作为输入。
- **2026-04-09**：工作流创建全屏层：**取消整页顶栏**；**左侧能力面板浮在画板上**（`.dndflow--overlay-glass` 下画布 `absolute inset-0`，侧栏 `absolute` + 圆角/描边/`#0f0f12` 高不透明度底/阴影/轻模糊，参考大图预览角上卡片）；右上角「关闭」样式对齐 `ImagePreviewOverlay`；`overlayGlass` 下侧栏去掉「返回」按钮（仅「保存」），避免与右上角关闭重复。
- **2026-04-09**：能力集合画布「通用节点」增加**测试节点**：拖到**已有连线**上释放则拆分边并插入 `type: 'testStop'` 节点（竖线 +「运行测试」）；`executeCapabilitySet` 支持 `stopAtNodeId` 仅执行到该断点；删除断点时若同时删入/出边则自动**桥接**恢复一条边。几何命中：`services/capabilityFlowEdgeHit.ts`。工作流全屏编排经 `WorkflowComposerOverlay` 传入 `getPartialTestInputImage`（大图预览或选中图片资产）。
- **2026-04-09**：能力集合画布「通用节点」新增**资产节点**（`type: 'assetInput'`）：拖到画布后可在节点内用 `CustomDropdown` 选择来源（工作区/仓库）与具体资产；运行测试时 `CapabilitySetCanvas` 将所选资产图像通过 `executeCapabilitySet(..., { assetInputs })` 注入对应节点，实现“以仓库/工作区资产作为流程输入”。
- **2026-04-09**：资产节点 UI 改版为与其它资产卡一致：底部标题栏右侧新增「选择」按钮；点击后在节点右侧弹出临时面板（左边缘贴合节点），支持「工作区 / 仓库」两页签切换并以缩略图网格平铺，点击即完成选图并关闭面板（支持点击外部/Esc 关闭）。
- **2026-04-09**：`CapabilityPresetSection` 里「能力集合」视图改为与「基础能力预设」一致的卡片流样式（瀑布列 + 预览区 + 底部标签/操作区），统一模块内展示语言与交互层次；并保留卡片点击编辑、底部按钮编辑/删除两种入口。
- **2026-04-09**：`CapabilityPresetSection` 编辑能力集合时，移除旧的内嵌 `CapabilitySetCanvas` 模式；改为直接打开 `WorkflowComposerOverlay`（新工作流全屏面板），能力集合卡片点击与“添加能力集合”入口统一走同一编辑体验。
- **2026-04-09**：为避免“两个很像的工作流房间”，在 `App.tsx` 增加能力页统一的 `WorkflowComposerOverlay` 单例入口（`openGlobalWorkflowComposer`）；`AppMode.CAPABILITY` 下 `CapabilityPresetSection` 改为仅发起开门请求（`onOpenWorkflowComposer`），不再使用该页内独立弹层状态作为主入口。
- **2026-04-09**：工作流编排默认输入改为**资产节点**：`buildWorkflowComposerSeedFromTwoPresets` 与 `CapabilitySetCanvas` 的空白初始图，均从 `type: 'input'`（原始图片）切换为 `type: 'assetInput'`（输入资产）；默认固定不可删节点同步改为 `asset-input-1`。
- **2026-04-09**：文本生成节点输入框优化：字号下调并按内容自动增高，同时增加高度上限（约普通节点 2.5 倍；默认/紧凑分别约 `140px/110px`）；输入框内拖拽改为优先文本选择（`nodrag` + 指针事件停止冒泡），避免误触发节点拖动。
- **2026-04-10**：AI 调用源新增独立供应商 **Antigravity Tools**（`ac_ai_provider=antigravity`，`ac_antigravity_api_key` / `ac_antigravity_base_url`，默认 Base `http://127.0.0.1:8045/v1`）：`geminiService.getAI` 复用 `createToapisGeminiClient`（OpenAI 兼容路径）；用户云配置 `user-config.json` 增加 `antigravityApiKey`、`antigravityBaseUrl` 字段。
- **2026-04-10**：Antigravity 客户端使用 `createToapisGeminiClient(..., { passthroughModels: true })`，**不做** ToAPIs 专用模型名改写（如 `gemini-3-flash-preview-official`），避免反代上游 404 / “All accounts exhausted”；若网关仍不认站内 id，需在 Antigravity 模型路由中自行映射。
- **2026-04-10**：`getAI`：供应商为 ToAPIs / Antigravity / VectorEngine 且**未填对应 Key** 时**不再回退** `GoogleGenAI`（避免误走 `@google/genai` 的 `response.json()` 空 body 报错）；`getAiProvider` 与云配置 `aiProvider` 统一 **trim + toLowerCase**；`toapisAdapter` 生图链路的 `res.json()` 改为 `readResponseJson`（先 `text` 再 `parse`）。
- **2026-04-10**：Antigravity 客户端 `createToapisGeminiClient` 增加 `**skipToapisImageUpload: true`**：跳过 ToAPIs `/v1/uploads/images`；**有参考图**时按 Antigravity-Manager 约定调用 `**POST /v1/images/edits`（multipart，`image1`…）**，文生图仍 `**POST /v1/images/generations`**（JSON，`size` WxH + `quality`，无 ToAPIs `metadata`）；生图响应支持同步 `data[0].b64_json`/`url`。
- **2026-04-13**：会话纪要 — 上轮确认 Vertex AI `inference` 页主要是 Gemini `generateContent`（偏多模态理解/文本输出），真正文生图应走 Imagen `endpoints.predict`；新话题：Function Calling（REST）调用方式梳理。
- **2026-04-13**：**Vertex AI 接入（首版）**：新增 `docs/VERTEX_AI_INTEGRATION.md`；`server/gemini-proxy-api.js` 在 `POST /proxy/gemini/async` 与 `POST /proxy/gemini/generate-content` 支持 body 字段 `aiBackend: "vertex"`，用 `@google/genai` 的 `vertexai: true` + `VERTEX_PROJECT_ID`/`GOOGLE_CLOUD_PROJECT`、`VERTEX_LOCATION`（默认 global）与 ADC 调用；`GET /healthz` 增加 `vertex.configured`；前端 `AiProvider` 增加 `vertex`，`geminiService` 在选 Vertex 时强制 `VITE_BULK_IMAGE_API` 并在异步代理请求中带 `aiBackend`；设置页与工作流密钥弹窗可选 Vertex；`workspaceUserCloudConfig` 同步 `aiProvider` 枚举。
- **2026-04-13**：能力集合画布「运行测试」捋顺：校验图允许**仅 `assetInput`**（与默认画布一致，不再误报缺 `input`）；`executeCapabilitySet` 成功结果附带 `nodeImageOutputs` 并在 `stopAtNodeId` 为**输出节点**时可提前返回；画布侧栏新增「试运行全流程」，断点/输出「运行测试」执行后把各节点预览写入 `data.testRunPreview`（保存集合 JSON 时剥离）。
- **2026-04-13**：能力集合画布运行测试体验：`executeCapabilitySet` 增加 `onRunProgress` 逐步文案；画布内顶部 **HUD** 显示进度与完成/失败条（约 5s 收起）；成功写入 `testRunPreview` 后 `requestAnimationFrame` + `updateNodeInternals` 刷新节点尺寸；含测试结果的预览图 `CapabilityPreviewImg` 使用 `**loading="eager"`**（避免 React Flow 视口内 lazy 不触发）；侧栏说明区分**测试节点**（只跑到该点）与**输出节点**（跑到该输出）。
- **2026-04-13**：工作流「理解 / 纯文本」与第三方 Gemini 网关兼容：`getDialogTextResponse` 单轮 user 使用 `**[{ role:'user', parts }]`**；`understandImageEditIntent` 使用 `**[{ role:'user', parts }]**` + `systemInstruction`；**VectorEngine** 下将 `gemini-3-flash-preview` / `gemini-3-pro-preview` 映射为 `**gemini-2.5-flash` / `gemini-2.5-pro`**（`resolveUpstreamTextModelId`）；`normalizeApiErrorMessage` 对 “valid role / stable user model” 类英文报错给中文说明。
- **2026-04-13**：工作流/能力执行与**当前设置供应商**对齐：新增 `**resolveUpstreamImageModelId`**（VectorEngine 下将 `gemini-3.1-flash-image-preview` / `gemini-3-pro-image-preview` 回退为 `**gemini-2.5-flash-image**`；ToAPIs 等不在此层二次映射）；`detectObjectsInImage` / `describeImageSubject` / `dialogGenerateImage*` / `processTexture` / `generatePBRTexture` 及擂台、站点助手、会话标题、翻译等统一 `**resolveUpstreamTextModelId` + 显式 user 角色**；`capabilityExecutor.resolveImageModelId` 对挡位 id 做上游生图解析。
- **2026-04-13**：**AI 渠道全站一致**：在 `settingsStore` 注明 `getAiProvider()` 为唯一真相源、调用须经 `geminiService.getAI()`；`getAI` 注释明确各场景互斥分支、网关不静默回退 Google；设置页用 `**aiSettingsSyncRev`** 与 `**subscribeAiSettingsCrossTab**` 在云端拉取/他页改 Key 后重新从 localStorage 刷新表单，避免界面与真实渠道脱节。
- **2026-04-13**：能力集合执行 `**collapseTestStopsForExecution`**：`stopAtNodeId` 指向 `testStop` 时保留该断点，否则（试运行全流程、输出节点停、工作区队列 `executeCapabilitySet`）从执行图中**移除全部测试节点并桥接边**；画布节点预览 **双击** 打开 `ImagePreviewOverlay` 大图。
- **2026-04-13**：能力集合试运行 `**onNodeImageOutput`**：每节点写出图像后即回调，画布逐步更新 `testRunPreview`；失败返回附带 `**nodeImageOutputs**` 合并已跑完节点预览；拆分/变体等单步通过 `CapabilityExecuteContext.onRunProgress` 提示检测/理解/生图阶段。
- **2026-04-15**：会话纪要 — 上轮：能力集合「选择资产」文本预览改为缩略专用 SVG（增大字号、标题/正文截断、省略号、XML 转义）；新话题：文生图分组“生成数量”始终只出 1 张，并核查覆盖功能是否有效。
- **2026-04-15**：修复文生图分组“生成数量”对文字资产失效：数量扩增对文字资产放开 `text_to_image`（此前仅 `text_to_text`），并同步修正「词」微调路径的克隆后自动成组（允许文字资产分组），保证两个入口一致。涉及：`components/WorkflowSection.tsx`。
- **2026-04-15**：会话纪要 — 上轮：修复文生图数量扩增对文字资产失效；新话题：文生文预设新增“拖拽时强制填写临时提示词”开关（默认关闭，开启后弹框必填并与预设提示词合并发送）。
- **2026-04-15**：文生文预设新增 `requirePromptOnTextDrop`（默认关闭）：开启后，工作流将文字资产拖到该能力主区会弹出空白必填的临时提示词框，确认时把“预设提示词 + 用户临时提示词”合并后入队；关闭时维持直接入队。涉及：`types.ts`、`services/capabilityPresetStore.ts`、`components/CapabilityPresetSection.tsx`、`components/workflow/modals/PromptTweakModal.tsx`、`components/WorkflowSection.tsx`。
- **2026-04-15**：修复能力预设详情弹层编辑态下拉不可点：详情页层级为 `z-[10000]`，为避免 `CustomDropdown` 默认 Portal 层级被遮挡，编辑区下拉统一提升 `portalZIndex`（`10120/10121`）。涉及：`components/CapabilityPresetSection.tsx`。
- **2026-04-16**：会话纪要 — 上轮：讨论 `bb-browser` 的能力边界、为何“通用控制”不等于“站点零维护”，并确定本地中转应用应采用“通用核心 + 站点适配器（说明书）+ 社区主源（`bb-sites`）+ 私有兜底”的架构；**新话题**：输出可直接落地的开发文档与给编程 AI 的执行提示词模板。
- **2026-04-16**：按“同仓分目录”落地本地中转开发骨架：新增 `A-Driver` workspace（`apps/local-bridge` + `packages/protocol`），补齐最小协议类型、connector 注册/编排、`gemini-web` 示例 connector 与启动入口，网站主工程保持不变。涉及：`A-Driver/package.json`、`A-Driver/apps/local-bridge/**`、`A-Driver/packages/protocol/**`、`A-Driver/README.md`。
- **2026-04-16**：`A-Driver` 连接器由 mock 切换为真实 `bb-browser` 调用：新增 `bbBrowserConnector`（`npx -y bb-browser site <route> <text> --json`），`threadId` 可作为临时 route 覆盖，默认 route 取 `BB_BROWSER_SITE_ROUTE`（默认 `wikipedia/summary`）；`README` 补充运行前准备与环境变量说明。
- **2026-04-16**：本地中转新增 WS 传输层：`webSocketBridgeClient` 支持 `BRIDGE_SERVER_WS_URL` 连接、设备头 `x-a-driver-device-id`、指数退避重连与 20s 心跳；`index` 改为“有 WS 配置走在线模式，无配置走 demo 模式”。新增 `ws` / `@types/ws` 依赖并通过 typecheck。
- **2026-04-16**：完善 `A-Driver/docs/本地中转应用开发文档.md`：补充设备鉴权字段（Authorization/deviceId/version）、`taskId` 幂等规则、`overrides > community` 路由优先级，以及 `bb-browser` 调用与错误码映射约定，减少实现偏差。
- **2026-04-16**：按开发文档继续实现：协议错误码统一为 `E_*`，新增 `task.timeout` / `task.duplicate` / `connector.unhealthy` 事件；`BridgeOrchestrator` 增加 `taskId` 幂等缓存、重复任务回放、任务超时（`BRIDGE_TASK_TIMEOUT_MS`）与错误映射（登录态错误 -> `E_SITE_NOT_LOGGED_IN`）；WS 传输支持 `Authorization`（`BRIDGE_AUTH_TOKEN`）与断线回调；新增自动化测试 `bridgeOrchestrator.test.ts` 覆盖超时分支并通过。
- **2026-04-16**：继续打通后端联调链路：新增 `server/bridge-relay.js`（`/ws/bridge` 设备连接、在线设备表、任务事件缓存）；`server/auth-api.js` 集成 WS upgrade 与 3 个管理员路由（`GET /api/bridge/devices`、`POST /api/bridge/tasks/send-message`、`GET /api/bridge/tasks/:taskId/events`）；根依赖新增 `ws`。`A-Driver/README.md` 补充后端联调说明。
- **2026-04-16**：桥接代码巡检修复：`/api/bridge` 写接口补齐与 `/api/admin` 同级的 Origin/CSRF 兼容；`bridge-relay` 新增 `deviceId -> user` 绑定校验（防跨用户抢占同 deviceId）、任务事件总量上限（按 taskId 保留最近 500 条）防内存增长；下发任务时连接状态判断改为 `readyState === 1`，避免实例常量兼容歧义。
- **2026-04-16**：继续按文档补齐传输语义：协议新增 `transport.disconnected` 事件；`BridgeOrchestrator` 增加 `onTransportDisconnected`，断线时会把运行中任务统一标记 `E_TRANSPORT_DISCONNECTED` 且抑制后续超时/完成重复回传；`index` 在 WS 断线时上报传输事件并触发 orchestrator 处理。另补连接器周期健康检查（`BRIDGE_HEALTHCHECK_INTERVAL_MS`）与测试用例 `transport disconnected should fail running tasks`。
- **2026-04-16**：传输层 ACK/去重：`task.send_message` 增加可选 `messageId`，协议新增 `transport.ack`；`bridge-relay` 对 `deviceId+messageId` 在窗口内去重并写入 `task.dispatched`；设备端收到后先回 `transport.ack`；`POST /api/bridge/tasks/send-message` 返回 `messageId`/`deduped`；新增 `scripts/bridge-relay-smoke.mjs` 与文档 `5.3` 说明。
- **2026-04-17**：会话纪要 — 上轮：本地中转链路补齐 ACK 去重与 smoke 脚本；新话题：能力预设页共享功能区顶部操作块改为“编辑/删除”，并支持将预设拖拽到操作块触发动作（后续可扩展更多操作）。
- **2026-04-17**：会话纪要 — 上轮：已重启本地全套环境（`dev:all` + `A-Driver`）并恢复 bridge 联通；新话题：大图预览首次打开时比例错误（滚轮切换一轮后恢复）排查与修复。
- **2026-04-17**：会话纪要 — 上轮：修复 `ImagePreviewOverlay` 首帧比例回填缺失（缓存命中时 `onLoad` 兜底 + `key={imageSrc}`）；新话题：优化能力预设区拖拽体验（自定义拖拽预览与源卡反馈）。
- **2026-04-17**：能力预设拖拽体验优化：`CapabilityPresetSection` 在工作区内拖拽预设时改为紧凑拖拽预览（不再使用整卡默认幽灵图），并给源卡加入拖拽中高亮/透明态；拖拽结束与组件卸载会清理临时 drag preview 节点，避免残留。
- **2026-04-17**：能力预设页支持列数调节：`CapabilityPresetSection` 为「基础能力预设 / 图像处理 / 能力集合」卡片流新增与工作区同款 `- / +` 列数控件（2~6 列），并通过 `clientPersist` 键 `ac_capability_preset_columns_v1` 持久化。
- **2026-04-17**：能力预设列数控件补到工作区顶部标题栏（嵌入态可见）：`WorkflowSection` 顶栏新增 `- / +` 并通过 `ac:capability-preset-column-count` 事件驱动；`CapabilityPresetSection` 监听该事件更新列数并广播 `ac:capability-preset-column-count-changed`，两端数值同步。
- **2026-04-17**：拖拽自动滚动增强：工作区主列表（`WorkflowSection` 中央滚动容器）、功能区侧栏（`WorkflowSidebarColumn` 根容器）、能力预设列表（`CapabilityPresetSection` 滚动容器）均新增“靠近上下边缘时自动滚动”逻辑，拖拽过程中可连续滚动长列表。
- **2026-04-17**：能力预设页顶部动作块新增“复制”：从 `编辑/删除` 扩为 `编辑/复制/删除`，拖拽到“复制”会创建同配置的新预设（新 id、自动去重命名“副本”并顺延序号、排序追加到末尾）。
- **2026-04-17**：功能区顶部与常用功能改为固定显示：`WorkflowSidebarColumn` 将顶部操作块与「常用功能」区设为 sticky（不随列表滚动），下方能力列表继续滚动；工作区左右两侧功能区视觉与交互保持一致。
- **2026-04-17**：补齐“拖拽时鼠标滚轮滚动”：除原有边缘自动滚动外，`WorkflowSection` 中央工作区、`WorkflowSidebarColumn` 功能区、`CapabilityPresetSection` 能力预设区均在拖拽进行中拦截 `onWheelCapture` 并驱动容器 `scrollTop`，支持拖拽中直接滚轮浏览长列表。
- **2026-04-17**：修正功能区 sticky 分层：仅固定“顶部操作块”和“常用功能”两段，能力列表恢复为下方独立滚动；并提升 sticky 层级与阴影，避免被底层列表覆盖。
- **2026-04-17**：继续修正功能区固定层穿透：`WorkflowSidebarColumn` 根容器增加 `relative isolate` 建立独立层叠上下文，顶部操作块与常用功能 sticky 提升 z-index、改为上下留白并加深阴影遮罩，缓解“列表从常用功能下穿过、上下露边”视觉问题。
- **2026-04-17**：能力预设面板视觉对齐工作区：`WorkflowSection` 中 `data-workflow-preset` 容器去除底板背景与 blur（由 `bg-[#12131a]/70 backdrop-blur-[1px]` 改为 `bg-transparent`），保留圆角与内边距。
- **2026-04-17**：常用功能分组头补齐「生成数量 + 覆盖参数」控件：`WorkflowSidebarColumn` 复用分组覆盖状态（key=`__favorites__`），在常用功能头显示 `数/覆/档/比/寸/解`，并在常用功能卡片（主区与“词”微调区）拖拽执行时透传 `groupOverrides` 生效。
- **2026-04-17**：去除「大纲 ↔ 工作区」分隔线：`WorkflowSection` 的 `data-workflow-outline` 容器移除右侧边框（`border-r border-white/[0.06]`），保留原列宽与内边距。
- **2026-04-17**：去除「能力区 ↔ 能力预设」分隔线：`WorkflowSection` 右侧能力预设外层容器移除左边框（`border-l border-white/[0.06]`），保留 `pl-4` 间距与原面板宽度。
- **2026-04-17**：功能区能力块新增“按类别轻度分色”边框：`WorkflowSidebarColumn` 为 `text_to_text / text_to_image / image_to_image / image_to_text / generate_3d / set` 映射低饱和低明度边框色，仅区分边框（含 hover 与生图卡片分隔线），不改卡片底色与拖拽高亮主色。
- **2026-04-17**：去除「仓库 ↔ 大纲」分隔线：`WorkflowSection` 左侧仓库列外层容器移除右边框（`border-r border-white/[0.06]`），保留原列宽与右侧内边距。
- **2026-04-17**：功能区背景改透明：`WorkflowSidebarColumn` 顶部操作 sticky 区与常用功能 sticky 区移除底色与阴影（`bg-[#0b0b0d]` / `shadow-*` -> `bg-transparent`），功能区外观与工作区背景一致。
- **2026-04-17**：能力块分类边框可读性微调：在 `WorkflowSidebarColumn.getSidebarCapabilityTone` 中将各类 `idle/hover/divider` 边框色整体抬亮一档，保留低饱和分色风格但提升区分度（仍仅作用于边框）。
- **2026-04-17**：修复功能列表“从固定区下穿”视觉问题：在 `WorkflowSidebarColumn` 的两个 sticky 固定层（顶部操作、常用功能）恢复半透明遮罩底与轻微 blur（`bg-[#0b0b0d]/92 backdrop-blur-[1px]`），阻断下方滚动列表穿透；整栏仍保持非整块实底。
- **2026-04-17**：能力预设详情大图界面对齐工作区预览壳：`CapabilityPresetSection` 详情弹层从自建 `createPortal` 容器切换为 `ImagePreviewOverlay`，保留原图/生成图斜切对比与参数编辑逻辑，并将“上传/测试/R2/删除”等主操作迁至底部操作条，右侧改为与工作区同风格的悬浮信息面板。
- **2026-04-17**：修复能力预设详情页编译报错：`CapabilityPresetSection` 底部操作区 `运行测试` 按钮误写为 `</div>` 导致 JSX 闭合异常，已改回 `</button>`，页面恢复可编译。
- **2026-04-17**：常用功能区卡片紧凑化：`WorkflowSidebarColumn` 中常用功能网格由 `2` 列改为 `5` 列（`grid-cols-5`），卡片高度下调至 `52px`，内边距与标签字号同步缩小，实现“一行五个”的展示密度。
- **2026-04-17**：功能区固定层遮罩改为“渐隐”而非 blur：`WorkflowSidebarColumn` 顶部操作区与常用功能区 sticky 层去掉 `backdrop-blur`，改用 `bg-gradient-to-b`（深色到透明）+ `pb` 渐隐缓冲，降低下层列表穿透时的脏边与重影感。
- **2026-04-17**：功能区滚动结构改为硬分层：`WorkflowSidebarColumn` 根容器取消整栏 `overflow-y-auto`，改为“上层固定区（顶部操作+常用功能） + 下层独立滚动区（能力列表）”；拖拽滚轮与边缘自动滚动改为驱动下层滚动容器 `sidebarListScrollRef`，彻底避免列表从固定区下方穿透。
- **2026-04-17**：能力预设大图对比视图两侧改透明：`CapabilityPresetSection` 中对比 `centerSlot` 容器去掉深色底（`bg-[#0f0f10]` -> `bg-transparent`），窄图场景下左右留白不再显示黑底块。
- **2026-04-17**：常用功能卡片中“词”侧栏占比再缩小：`WorkflowSidebarColumn` 常用功能（`grid-cols-5`）内生图卡的右侧微调区宽度由 `w-8` 调整为 `w-6`，并同步将“词”字号降到 `text-[9px]`，减少对主名称区域的挤占。
- **2026-04-17**：常用功能“词”区继续收窄为“两格字符宽”：`WorkflowSidebarColumn` 中微调侧栏宽度改为 `w-[2ch] min-w-[2ch]`，字号降为 `text-[8px]`，进一步让主卡标题区域可读。
- **2026-04-17**：功能区分组拆分「图像处理」：`groupCapabilityPresetsByCategory` 将 `image_to_image + builtin` 预设独立归入新分组 `image_process`（标签「图像处理」），不再显示在「图生图」分组中；「图生图」保留需要生图模型的图生图能力。
- **2026-04-17**：常用功能网格支持“带词占两格”：`WorkflowSidebarColumn` 中常用功能 `grid-cols-5` 下，带“词”微调入口的卡片改为 `col-span-2`，无微调入口卡片保持 `col-span-1`，从而实现“一行可放 2 个带词 + 1 个普通”。
- **2026-04-17**：常用功能“词”区宽度按比例调整：在带“词”入口的常用功能卡片中，微调侧栏由固定字符宽改为卡片宽度 `1/4`（`w-1/4`，含最小宽度兜底），满足“词区占整体四分之一”的视觉比例。
- **2026-04-17**：功能区拖拽引导样式收敛：`WorkflowSidebarColumn` 中常用功能/分组能力/复合能力卡片边框由 `border-dashed` 改为实线 `border`，降低视觉噪音；在能力列表区新增提示条“拖拽左侧图片到下方功能卡，可直接执行对应能力”，强化拖拽可用性提示。
- **2026-04-17**：功能区拖拽引导改为“仅拖拽时强调”（A 方案）：`WorkflowSidebarColumn` 新增 `isImageAssetDragging`，常态保持安静样式；仅在拖拽图片/组项时，所有可投放功能卡统一轻高亮（蓝边 + 轻底 + 细 ring），当前 hover 目标继续使用强高亮；顶部引导提示条改为仅拖拽态显示。
- **2026-04-17**：修复“能力预设禁用项拖到功能区无法启用”：`WorkflowSidebarColumn` 恢复全局 `DT_AC_CAPABILITY_FROM_EDITOR` 接收（不再在 `topActionMode=capabilityPreset` 下整体禁用），并为顶部“编辑/复制/删除”动作块加 `data-capability-preset-action-drop` 保护，使该区域仍走专属动作，其它区域可正常触发 `onDropPresetFromEditor` 进行启用与入队。
- **2026-04-17**：区分能力拖拽来源（常用/原始）并修正禁用与移出常用规则：新增拖拽源 MIME `DT_AC_CAPABILITY_ACTION_SOURCE`（`favorite`/`catalog`）；`useWorkflowMainScrollCapture` 右侧留白禁用逻辑仅对 `catalog` 生效（`favorite` 忽略）；`WorkflowSection` 在能力卡片接收 drop 时若来源为 `favorite`，会标记 `setActionDroppedInFavorite(true)`，使“拖到图片上执行”不再误触发“移出常用”。
- **2026-04-17**：能力预设标题栏新增类型筛选（文生文/文生图/图生图/图生文）：`WorkflowSection` 在“能力预设”顶部（基础能力视图）加入筛选下拉并通过事件 `ac:capability-preset-type-filter` 下发；`CapabilityPresetSection` 新增 `presetTypeFilter` 状态与 `ac:capability-preset-type-filter-changed` 回传，`visiblePresets` 在 `viewMode='presets'` 下按该类型过滤（`image_process` 与 `sets` 逻辑保持不变）。
- **2026-04-17**：能力预设卡片去除文生文预览占位：`CapabilityPresetSection` 在卡片渲染中为 `text_to_text` 增加无预览分支，仅展示名称与状态标签（不渲染预览图/占位图区域）；其它类型维持原预览图与对比预览逻辑。
- **2026-04-17**：功能区拖拽高亮改为“输入类型感知”：`WorkflowSidebarColumn` 新增拖拽载荷判定（`hasTextPayload`/`hasImagePayload`），仅当能力类型支持当前拖拽输入时才显示拖拽态轻高亮（文生文/文生图需文本输入；图生图/图生文/3D 需图像输入）；不支持输入类型的能力块保持常态不高亮。
- **2026-04-17**：补强“拖拽中滚轮滚动”稳定性：在 `WorkflowSection`（图片列表）与 `WorkflowSidebarColumn`（功能区列表）新增原生 `wheel` 事件兜底监听（`passive:false`），当检测到拖拽进行中时手动驱动对应滚动容器 `scrollTop`；若 React `onWheelCapture` 已处理则跳过，避免重复滚动。
- **2026-04-17**：修复功能卡圆角边框缺口：`WorkflowSidebarColumn` 能力卡容器统一加 `overflow-hidden`，并移除右侧“词”分栏的 `border-l`（保留主区域 `border-r` 作为分割线），消除边角处双边线叠加导致的缺块/断边视觉问题。
- **2026-04-17**：拖拽态高亮与交互增强：`WorkflowSidebarColumn` 为“可投放”能力卡提升拖拽显著性（更强蓝边/底色/ring/阴影），当前命中目标再加一级强调并加入轻微上浮动效；同时在拖拽进行中将“不支持当前输入类型”的能力卡降亮处理（`opacity + saturate`），提升可投放目标识别效率。
- **2026-04-17**：常用功能收藏入口扩展到“分组标题”：`WorkflowSidebarColumn` 新增统一的收藏 drop 解析函数，除原“常用功能内容区”外，拖拽到 `常用功能` 头、各能力分类头（含无分类回退的“功能”头）以及“复合能力”头，也可直接加入常用功能；分组头在拖拽悬停时会显示蓝色高亮反馈。
- **2026-04-17**：能力预设默认列数改为 `6`：`CapabilityPresetSection` 与 `WorkflowSection` 的列数读取回退值均从 `4` 调整为 `6`（含无效持久化值的 normalize 回退），新用户/无本地记录时默认以 6 列展示。
- **2026-04-17**：修复“拖拽中滚轮在功能区偶发不生效”：`WorkflowSidebarColumn` 在原容器级 `wheel` 监听外新增 `window capture` 级滚轮兜底；拖拽进行中若光标位于功能区矩形内，统一驱动 `sidebarListScrollRef` 滚动（`passive:false` + `preventDefault`），解决拖到功能卡时不松鼠标无法滚动列表的问题。
- **2026-04-17**：工作区“组卡”交互收紧：`WorkflowSection` 新增根资产选择过滤（组容器 `cutImageGroup` 不进入 `selectedAssetIds`），并将工作区根卡片中组项设为 `draggable=false`；顶部“全选”也排除组卡。结果是组卡仅支持点击进入，不再参与拖拽与选中。
- **2026-04-19**：会话纪要 — 上轮：完成能力区/预设区与大图预览相关交互收敛与修复记录；新话题：排查「生成多视角」日志停在“理解预设提示词中…”不继续的问题。
- **2026-04-19**：修复「生成多视角」理解阶段卡住体感：`CAPABILITY_UNDERSTAND_RETRY_OPTIONS` 改为有上限重试（`90s` 超时、`4` 次重试、退避上限 `12s`），`callWithRetry` 增加最大退避钳制，`BULK_PROXY_UNDERSTAND_TIMEOUT_MS` 从 `600s` 收敛到 `120s`，避免网关抖动时长时间停在“理解预设提示词中…”。
- **2026-04-19**：补强理解阶段可用性：`understandImageEditIntent` 在 `gemini + VITE_BULK_IMAGE_API` 场景下，若 bulk 代理出现超时/网络/503 类异常且本地存在浏览器 Gemini Key，会自动回退到浏览器直连 Gemini 再尝试一次（短超时/低重试），降低「白模/多视角」因远端代理排队导致的卡死概率。
- **2026-04-19**：AI 供应商新增 `trial`（试用）并改为默认：`settingsStore.DEFAULT_AI_PROVIDER='trial'`；试用模式固定走 `VITE_BULK_IMAGE_API` 代理，`gemini` 改为仅前端本地 Key 直连（不再因配置了代理而优先走后端）；设置页与工作流密钥弹窗新增“试用”选项；云端用户配置 `aiProvider` 枚举同步支持 `trial`，默认回退从 `vertex` 调整为 `trial`。
- **2026-04-27**：会话纪要 — 新话题：推送到 GitHub；已推送 `master`→`origin/main`（`cd4ff6e`，工作流多窗格/网格与分组辅助、Vertex 测试脚本、README 工作流行同步）。
- **2026-04-27**：会话纪要 — 延续：再次推送到 GitHub；`12e7ce8`（`App` / `WorkflowModeShell` / `WorkflowSection` / `WorkspaceSidebarFooter` / `WorkflowSidebarColumn` / `workflowSectionUiConstants` 工作流与侧栏相关调整）。
- **2026-04-27**：会话纪要 — 新话题：Render 线上登录故障排查；已确认 `assetcutter-auth-db` 因 `billing` 被挂起（`status=suspended`），`assetcutter-auth-api` 启动日志报 `getaddrinfo ENOTFOUND dpg-d70htd4hg0os73ac68kg-a` 并退出。恢复顺序为先解挂数据库，再重启鉴权 API，必要时复核 `DATABASE_URL`。
- **2026-04-27**：会话纪要 — 新话题：工作区大图预览接入 3D 视口；`ImagePreviewOverlay` 从「平面/全景」扩展到「平面/全景/3D」，预览 registry 新增 `image.model3d` 懒加载模式并接管滚轮。首版在线预览仅支持 `GLB/GLTF`（其余格式提示下载），`WorkflowSection` 在大图弹层向预览层透传 `asset.modelUrls` 并在底部追加“下载模型”按钮。
- **2026-04-27**：工作区 3D 扩展：`ImageModel3DViewer` 增加 `FBXLoader` / `OBJLoader`，大图预览支持 `.fbx/.obj`（与原有 glb/gltf 并列）；`WorkflowSection` 支持将 `.glb/.gltf/.fbx/.obj` 拖入或经文件选择加入画布，资产带 `modelUrls`（`blob:`）与 `modelSourceName` 供格式推断，灯箱透传 `modelFileName`。
- **2026-04-27**：工作区本地 3D 预览资源控制：`services/workflowModelBlob.ts` 定义单文件上限（默认 80MB，`VITE_WORKFLOW_LOCAL_MODEL_PREVIEW_MAX_MB` 可改），超限跳过加入并由 `onLog` 提示；删除资产或切割替换移除原图节点时对 `blob:` `modelUrls` 做孤儿检测后 `revokeObjectURL`，避免复制卡片共享同一 blob 时误删。
- **2026-04-27**：新增架构规范文档 `docs/本地伴侣-存储与计算规范.md`：网站编排与本机「资源平面 / 计算平面」分层、`AssetHandle` 与版本化目录布局、`Job`+`ComputeAdapter` 插件化及与 `docs/本地中转应用开发文档.md` 的边界说明，便于后续开源工具与仓库能力扩展。
- **2026-04-27**：本地运行时文档架构调整：**本地伴侣为主程序**；原「本地中转」并入为 **Relay 站点中转插件**（`docs/本地中转应用开发文档.md` 改为插件专篇）；`docs/本地伴侣-存储与计算规范.md` 升格为宿主 + 多插件（Storage / Compute / Relay…）总规范，`capabilities` 聚合、目录 `plugins/relay` 等；验收 M1=单站点、M3=多站点与文档对齐。
- **2026-04-27**：Companion P0 落地第一步：`A-Driver/apps/local-bridge` 增加 `src/companion/`（`GET /v1/capabilities`、`GET /v1/health`，默认端口 `18765`，`COMPANION_HTTP_PORT` 可关）；`A-Driver/README.md` 与 `docs/本地伴侣-存储与计算规范.md` §11.4 已同步。
- **2026-04-27**：主站设置页「本地伴侣」：`services/companionLocalPrefs.ts`（本机探测根地址持久化）、`services/companionProbe.ts`（`/v1/health` 与 `/v1/capabilities`）；`SettingsSection` 侧栏导航与区块：保存地址、检测连接、打开伴侣内置控制台（新标签）。
- **2026-04-27**：本仓库可运行「本地伴侣」程序 **`local-companion/`**（Node + 浏览器控制台）：模块 **插件**（计算等占位注册）与 **仓库**（Volume 根、浅层统计）；`docs/本地伴侣-本地程序开发.md`；根脚本 `local-companion:dev` / `local-companion:start`；与 A-Driver `local-bridge` 默认同端口勿并行。
- **2026-04-27**：`local-companion` 增 **存储**（`projects/.../manifest` + `PUT` 单 object）与 **计算**（`POST/GET/DELETE /v1/compute/jobs`，`stub.ping` 完成、其余登记型 `COMPUTE_ADAPTER_NOT_READY` 等）；`httpHandler` + `storage/*` + `compute/jobsStore`；控制台 **联调** Tab。
- **2026-04-27**：文档对齐**产品交付目标**：最终用户形态为 **安装包 + 系统托盘 + 可选主窗口**（`docs/本地伴侣-本地程序开发.md` §1.1、`docs/本地伴侣-存储与计算规范.md` §0 / §10）；`local-companion/README` 注明当前 Node 宿主为开发期实现。
- **2026-04-27**：新增 **`docs/本地伴侣-待决策清单与建议.md`**：汇总安装壳、双进程合并、安全、存储/计算/Relay、主站 client 分层等待定项，附选项对比与折中建议；总规范文首与本地程序开发文首增加交叉链接。
- **2026-04-27**：**已决固化**：`docs/本地伴侣-待决策清单与建议.md` 改为正式决议（Electron、托盘、默认不关自启、卸载保留卷、127.0.0.1+安装版白名单+Token、Relay 子进程路线等）；新增 **`docs/附录-伴侣错误码.md`**；总规范 §8/§10、本地程序 §1.1、`本地中转` §3.1 序列图、`types/companion.ts` 与 `tsconfig.quality.json` 同步。
- **2026-04-27**：主站 **`services/companionClient/`**（`fetch`、`probe`、`storage`、`compute`、`index`）；`companionProbe.ts` 改为 re-export；设置页「本地伴侣」增加 **拉取项目 id**（`GET /v1/projects`）；`local-companion` 新增 **`relaySupervisor.ts`**（`COMPANION_RELAY_CMD`、子进程 HTTP 端口策略）与 `pluginRegistry`/`runtime-status` 联动；总规范 §7、本地程序 §5/§7、README 环境表同步。
- **2026-04-27**：新增 **`companion-desktop/`**（Electron M0）：系统托盘、子进程启动 `local-companion`（`COMPANION_OPEN_BROWSER=0`）、单实例二次启动仅打开控制台 URL；根脚本 **`companion-desktop:start`**；`docs/本地伴侣-本地程序开发.md` §2/§6.1、§9 优先级与 `local-companion/README` 已同步。
- **2026-04-27**：`companion-desktop` **Windows 优先首次向导骨架**：`win32` 首次运行 `BrowserWindow`（`wizard/index.html` + `preload-wizard.cjs`）；`userData` → **`%LOCALAPPDATA%\AssetCutterCompanion\desktop-shell`**；`COMPANION_DESKTOP_SKIP_WIZARD` / `FORCE_WIZARD`；macOS/Linux 暂不弹窗；文档 §6.1 / README / §9 已同步。
- **2026-04-27**：`companion-desktop` 托盘增强（**Windows**）：**左键单击** 打开本机控制台；菜单 **「欢迎向导」** 可再次打开说明窗（标题区分首次 / 已完成后为「使用说明」）；向导页文案同步。
- **2026-04-27**：`local-companion` **访问收紧（P0）**：`accessGate.ts` — **`COMPANION_ALLOWED_ORIGINS`**（含 `localhost:*` / `127.0.0.1:*`）、**`COMPANION_SHARED_TOKEN`**（Bearer；豁免 `GET /v1/health` 与控制台页）；`capabilities` / `runtime-status` 增加 **`access`** 摘要；主站 **`companionLocalPrefs`** 存 Token、**`companionFetchJson`** 自动 `Authorization`；设置页「本地伴侣」增加 Token 输入；附录错误码与本地程序 §3 已同步。
- **2026-04-27**：**Compute `seam_repair`**：`compute/seamRepairAdapter.ts` + **`readAssetObjectBytes`**；Job **async** 调 WebSeamRepair；**`COMPANION_SEAM_REPAIR_URL` / `TIMEOUT_MS`**；`capabilities.compute.seamRepair`；主站 **`companionClient/relay.ts`**（`probeRelayFromCapabilities`）；文档与附录错误码已同步。
- **2026-04-27**：主站设置「本地伴侣」：**检测连接** 后追加 **Relay**、**修缝** 可折叠摘要；**`submitCompanionSeamRepairJob`** / **`CompanionSeamRepairInputsV1`**；`local-companion` 控制台 **联调** 页 **seam_repair** 表单与按钮。
- **2026-04-27**：文档补充「**最终产品闭环**」目标：`本地中转` §8.1、`本地程序` §7.1、`总规范` §9 新增一致口径（`task.accepted → reply.delta* → reply.completed|failed`、断线续传、用户可见故障提示）；`待决策清单` §9 增加闭环优先条目。
- **2026-04-27**：文档补充「**开源能力接入策略（通用）**」：默认 **Adapter 调用开源项目**，非必要不改上游源码；仅在阻断 bug/必要能力/不可绕过性能瓶颈时 fork 并最小补丁。已写入 `本地伴侣-存储与计算规范.md` §5.2.1 与 `本地伴侣-本地程序开发.md` §4.1。
- **2026-04-27**：闭环实现补强（P0.5）：`compute/jobsStore` 新增 `task.accepted|task.running|reply.delta|reply.completed|task.failed` 事件与 `seq`；新增 `GET /v1/compute/jobs/:jobId/events?afterSeq=&limit=` 增量拉取；`services/companionClient/compute.ts` 增 `listCompanionJobEvents`。
- **2026-04-27**：主站设置页闭环可视化（P0.6）：`SettingsSection` 新增 `jobId` 事件流面板，支持「重置并拉取」「增量拉取」「自动轮询」，并显示 `cursor(afterSeq)`；用于验证 `task.accepted → reply.delta → reply.completed|failed`。
- **2026-04-27**：主流程页闭环可视化（P0.7）：`SeamRepairSection` 新增本地伴侣 `job events` 面板（`jobId` + 增量拉取 + 自动轮询 + cursor），使事件链观察不再仅限设置页。
- **2026-04-27**：主流程自动绑定 job（P0.8）：`SeamRepairSection` 支持“提交到本地伴侣并跟踪”——自动 `putCompanionAsset`（OBJ/贴图/可选 mask）+ `submitCompanionSeamRepairJob`，并写入 `jobId`、自动开启事件轮询。
- **2026-04-27**：闭环恢复与动作补强（P0.9）：`SeamRepairSection` 的 companion 事件流增加 session 持久化（`projectId/jobId/afterSeq/auto`），并在 `task.failed` / `reply.completed` 后提供可执行动作（重试提交、打开本地控制台、复制排障信息）。
- **2026-04-27**：设置页闭环体验补齐（P0.10）：`SettingsSection` 的 companion 事件流新增 session 持久化（`jobId/afterSeq/auto`），并在 `task.failed` / `reply.completed` 后提供可执行动作（失败态“重置游标并重拉”、打开本地控制台、复制排障信息），与主流程页闭环排障体验对齐。
- **2026-04-27**：托盘异常动作补齐（P0.11）：`companion-desktop/main.cjs` 为托盘新增状态行与「重新启动本地伴侣」动作；当 `local-companion` 启动失败或异常退出时，Windows 托盘弹气泡提示并给出“重启/打开控制台排查”下一步动作，`companion-desktop/README.md` 与 `docs/本地伴侣-本地程序开发.md` 已同步。
- **2026-04-27**：托盘状态轮询补齐（P0.12）：`companion-desktop/main.cjs` 新增 `runtime-status` 轮询（12s），将 `relay.configured=true && relay.running=false` 映射为托盘异常提示；`401` 映射为“配对 Token 不一致”提示并给出动作。`companion-desktop/README.md` 与 `docs/本地伴侣-本地程序开发.md` 已同步（登录态错误字段仍待协议补齐）。
- **2026-04-27**：主窗口最小形态（P0.13）：`companion-desktop/main.cjs` 新增主窗口入口（托盘菜单「打开主窗口」），`BrowserWindow` 内嵌加载 `http://127.0.0.1:<port>/`；二次启动改为优先聚焦主窗口；若 URL 加载失败则显示内置故障页提示重启或打开控制台排查。`companion-desktop/README.md` 与 `docs/本地伴侣-本地程序开发.md` 已同步。
- **2026-04-27**：Relay 诊断态补强（P0.14）：`local-companion/src/relaySupervisor.ts` 新增 `lastError/lastExitCode/lastSignal/lastUpdatedAt` 并纳入 `runtime-status`；`companion-desktop/main.cjs` 在托盘轮询中读取并透出 Relay 异常细节（状态行 + 气泡提示），排障不再只有“未运行”粗粒度提示。同步 `companion-desktop/README.md` 与 `docs/本地伴侣-本地程序开发.md`，并在本地程序开发文档新增“TODO（按优先级）”清单。
- **2026-04-27**：站点登录态统一映射（P0.15）：`local-companion/src/pluginRegistry.ts` 为 `runtime-status` 新增 `siteAuth`（`unknown/ready/not_logged_in/relay_unavailable` + `nextAction`），并基于 Relay 诊断语义归一登录态；`companion-desktop/main.cjs` 托盘轮询新增 `siteAuth.state=not_logged_in` 分支提示与动作引导。同步 `local-companion/README.md` 与 `docs/本地伴侣-本地程序开发.md`。
- **2026-04-27**：向导配对最小可用（P0.16）：`companion-desktop/wizard/index.html` 增加 Token/Origin 输入与保存；`preload-wizard.cjs` 新增 `loadPairing/savePairing` IPC；`main.cjs` 新增 `pairing-config.json` 读写与启动注入逻辑（未显式设置环境变量时自动写入 `COMPANION_SHARED_TOKEN` / `COMPANION_ALLOWED_ORIGINS`）。同步 `companion-desktop/README.md` 与 `docs/本地伴侣-本地程序开发.md`。
- **2026-04-27**：P0 验收工具化（P0.17 进行中）：`scripts/test-companion-desktop.ps1` 新增 `p0-acceptance` 交互场景（逐项收集 tray/main-window/向导/配对/告警结果），并追加生成 `docs/本地伴侣-P0验收记录.md`；开发文档 §7 增加脚本入口说明。
- **2026-04-27**：P0 收口与安全微收紧：根据验收完成状态，将 `docs/本地伴侣-本地程序开发.md` 中 P0.17 标记为完成；同时对向导配对保存增加输入校验与规范化（Token 至少 8 位，Origin 逗号分隔去重并做格式校验），减少错误配置导致的运行期排障成本。
- **2026-04-27**：P1 打包基础落地：`companion-desktop/package.json` 新增 `electron-builder` 配置与脚本（`pack/dist:win/dist:portable`），根 `package.json` 新增对应转发脚本；`companion-desktop/README.md` 与开发文档新增打包命令与产物目录说明。签名/自动升级仍在 P1 后续收口。
- **2026-04-27**：P1 打包链路验证：已完成 `companion-desktop:pack`（产物 `release/win-unpacked`）；`dist:portable` 在当前网络环境下因下载 `electron-builder-binaries/nsis-resources` 超时失败，已在 README 与开发文档补充受限网络说明与排障提示。
- **2026-04-27**：P1 受限网络打包增强：新增 `scripts/companion-desktop-dist.ps1`（`Target`、`Retries`、`UseMirror`），可对 `dist:portable/dist:win` 自动重试并注入 Electron 镜像变量；根脚本新增 `companion-desktop:dist:retry`。
- **2026-04-27**：P1 发布预检骨架：新增 `scripts/companion-desktop-release-check.ps1`（签名/更新源可选强校验）与 `docs/本地伴侣-发布前检查清单.md`；根脚本新增 `companion-desktop:release:check`，用于发布前一致化检查流程。
- **2026-04-27**：优先级3/4/5 首轮落地：`local-companion` 新增配对会话占位 `pairingSession`（`/v1/pairing/session`、`/v1/pairing/revoke`、`AUTH_TOKEN_REVOKED`）用于“可撤销密钥”本地流程打底；`httpHandler` 增 `GET /v1/compute/jobs/:jobId/stream` SSE；前端 `services/companionClient/compute.ts` 增 `createCompanionJobEventStream`。`SettingsSection` 与 `SeamRepairSection` 的流游标持久化从 `sessionStorage` 升级为 `localStorage`，提升跨会话恢复能力。
- **2026-04-27**：按优先级重排执行队列：在 `docs/本地伴侣-本地程序开发.md` 中将 P0 人工验收三项标记为完成，并新增“8.1 执行队列（当前）”用于后续顺序推进（签名/升级骨架 → 会话对齐 → WS/SSE → 跨会话一致性）。
- **2026-04-28**：优先级4/5 持续收口：`SettingsSection` 与 `SeamRepairSection` 的任务事件流改为“**SSE 优先 + 失败回退轮询**”，并新增 `services/companionJobCursorStore.ts`（`ac_companion_job_cursor_store_v1`）用于跨页面共享 `jobId -> afterSeq` 游标，减少跨页切换后的重复拉取与游标分叉。
- **2026-04-28**：优先级5 继续收口：设置页与贴图修缝页新增“终态自动停跟踪”（`reply.completed/task.failed/task.cancelled`）与“页面重开自动补拉一次历史 `jobId` 事件快照”逻辑，降低断线/刷新后重复追踪与状态丢失风险。
- **2026-04-28**：优先级5 终态落盘：新增 `services/companionJobTerminalStore.ts`（`ac_companion_job_terminal_v1`），对终态 `job` 事件按 `seq` 幂等写入；设置页与修缝页启动时若有匹配 `jobId` 则先恢复终态快照，且「重置游标」时清除对应终态缓存。
- **2026-04-28**：存储与闭环对齐规范：`local-companion` 补齐 **`GET /v1/projects/:projectId/assets/:key`**（原始字节 + manifest `mime`）；主站 **`services/companionClient/storage.ts`** 新增 **`fetchCompanionAssetBlob`**；**`SeamRepairSection`** 在 **`reply.completed`** 且 **`payload.outputKey`** 存在时自动从伴侣拉取 PNG 至预览（可「重新加载」），与总规范「资源平面可读回」一致；开发文档 API 表已修正并补 SSE 行。
- **2026-04-28**：总规范同步：`docs/本地伴侣-存储与计算规范.md` §4.3 增加资产 **`GET`** 行；§5.3 修复损坏段落并写明 **SSE 已实现**；§7 `storage.ts`、§11.4 `local-companion` 能力与存储 API 描述已与当前代码对齐。
- **2026-04-28**：M2 与体验：`companion-desktop` 增加 **`electron-updater`** 与可选 **`COMPANION_UPDATE_FEED_URL`**（generic 源、托盘「检查更新」、启动后延迟检查）；NSIS 默认桌面/开始菜单快捷方式；设置页与贴图修缝页弱化技术文案并将诊断收入折叠区；新增 **`services/companionJobStatusHuman.ts`**。
- **2026-04-28**：工作区 UX：`WORKFLOW_CARD_SURFACE_IDLE` 与选中/拖入态统一 **`ring-2` + `border-0`**，消除卡片选中时约 1px 级跳动；**`App.tsx`** 在已打开工作区项目时 **`WorkflowModeShell` 保持挂载**（非工作区模式 `hidden`），避免切页卸载导致生图队列/执行动画状态丢失。
- **2026-04-28**：工作区 **3D 模型卡片自动缩略图**：`services/workflowModelPreviewCapture.ts` 离屏 Three 渲染 + `toDataURL`；`workflowModelThreeShared.ts` 与 **`ImageModel3DViewer`** 共用相机包围逻辑；**`addModelsFromFiles`** 在写入 SVG 占位后异步替换 **`original`** 为 JPEG 快照（仍用 `modelUrls` blob 做大图预览）。
- **2026-04-28**：**3D 预览灯光升级**：新增 **`services/workflowModelViewerStage.ts`**（`RoomEnvironment` + PMREM、`HemisphereLight`、主/补/轮廓 `DirectionalLight`、软阴影、地面片、PBR `envMapIntensity` 微调）；**`ImageModel3DViewer`** 与 **`workflowModelPreviewCapture`** 共用该舞台，与原先单盏平行光 + 纯色背景相比更接近中性棚拍 IBL。
- **2026-04-28**：**3D 大图预览去框全屏**：`ImagePreviewOverlay` 的 `model3d` 分支去掉居中、`p-3` 与内层 `max-w/max-h` 小框；**`ImageModel3DViewer`** 根节点去掉圆角/边框/ring/阴影/毛玻璃卡片壳，改为 **`h-full w-full` 透明铺满**，画布区域与整屏遮罩对齐。
- **2026-04-28**：向导与托盘用语（M2）：`companion-desktop/wizard/index.html` 配对区改为「本机通信密码 / 允许打开的网站地址」等用户向说明；托盘「打开本机控制台」→「打开本机管理页」，「欢迎向导」→「首次设置向导」，与向导内表述一致。
- **2026-04-28**：M2 文档与主站用语收口：`docs/本地伴侣-本地程序开发.md` / `本地伴侣-存储与计算规范.md` / `待决策清单` / `发布前检查清单` / `本地中转` / 根与 `local-companion` README 统一「本机管理页」「首次设置向导」等；`SettingsSection` 保存密码成功提示去掉环境变量名；`local-companion` 启动日志改为「本机管理页」。
- **2026-04-28**：会话纪要 — 推送到 GitHub：`56443ff`（`master`→`origin/main`，含本地伴侣/桌面壳/3D 与修缝闭环等）；根 `.gitignore` 增加 **`companion-desktop/release/`**，避免将 Electron 打包目录与安装包推入仓库（本地构建或 Releases 分发）。
- **2026-04-28**：会话纪要 — 推送到 GitHub：`c9311c3`（`master`→`origin/main`，M2 自动更新与用语收口、`companionJobStatusHuman`、向导/托盘与文档同步）。
- **2026-04-28**：M2 极简连接体验：`SettingsSection` 的「本地伴侣」区新增 **一键连接本机伴侣** 与 **一键关闭配对**；地址/密码改为「高级：手动连接设置」折叠；普通用户默认不再需要理解地址、Token 与能力明细。
- **2026-04-28**：本机管理页（`local-companion/public/index.html`）M2 极简化：默认页改为 **一键连接** 视角（`一键检查连接`、`复制本机地址`、用户向状态摘要），插件/仓库/联调入口全部改为 **高级** 命名，原始 JSON 收进「更多信息（可选）」。
- **2026-04-28**：Tripo 排障折中方案已落地：新增前端「调试模式：运行日志落盘（默认关闭）」开关（`settingsStore` + `SettingsSection`），开启时 `App` 的 `globalLogs` 会经 `services/clientDebugLog.ts` 脱敏后上报 `POST /api/debug/client-log`；`auth-api` 新增本地 `ndjson` 落盘（`.data/debug/client-runtime.ndjson`）与 7 天保留清理，并在服务端再次脱敏兜底。
- **2026-04-28**：桌面壳关闭行为修正：`companion-desktop/main.cjs` 新增 `isQuitting` 标记；主窗口点右上角关闭时拦截并 `hide()`（缩到托盘）；仅托盘菜单「退出」触发 `before-quit` 后才真正关闭应用与子进程。
- **2026-04-28**：Tripo 图生3D 提交链路修正：`image_to_model` 从“直接 base64 提任务”改为“先 `POST /api/tripo/upload` 获取 `file_token`，再 `POST /api/tripo/task` 创建任务”；`auth-api` 新增上传转发与超时/代理错误提示，前端 `tripoService` 增加 token 提取兼容（`file_token`/`image_token`），用于降低 v2 `1004` 参数错误概率。
- **2026-04-28**：Tripo 结果本地化：工作流 Tripo 成功后新增 `POST /api/tripo/fetch-file` 二进制代理下载，前端自动把模型与预览写入本地伴侣存储（`putCompanionAsset`）并回读成 `blob:` URL 回填资产卡，避免直接依赖 Tripo 外链（鉴权/CORS/时效）并降低云空间占用；同时修复“错误新建资产卡 + 下载列表混入预览图”。
- **2026-04-28**：Tripo 任务可恢复查询：`WorkflowAsset.resultMeta[actionType]` 扩展 `tripoTaskId/tripoLastError`（`types.ts`），`App.tsx` 的 `generate_3d` 执行链路改为优先“继续查询既有任务（不新建）”，仅在无可恢复 `taskId` 时创建新任务，并在日志中明确区分“继续查询旧任务”与“创建新任务”。
- **2026-04-28**：Tripo 失败恢复动作可视化：运行日志弹层新增 `Tripo 失败恢复` 区块，失败后直接提供两个动作按钮（继续查旧任务/重提新任务）；`App.tsx` 的工作流 Tripo 执行入口新增 `options.forceNewTask`，用于显式区分“恢复查询”与“新建任务（可能计费）”。
- **2026-04-28**：新增方案文档 `docs/本地与云存储分层开发方案.md`：明确“能力预设自动云同步（随账号）+ 项目本地优先 + 云端轻索引”的分层策略、字段边界、跨账号打开语义与分阶段里程碑。
- **2026-04-28**：补充强约束：能力（`CustomAppModule`/`CapabilitySet`）是**账号级**，不是项目级；项目仅允许 `capabilityRefs`（id/快照）引用，不允许携带账号主能力全集或反向覆盖账号能力。
- **2026-04-28**：`docs/本地与云存储分层开发方案.md` 新增“第一性原理总纲（3条）”与“落地检查清单”，用于产品化评审时快速判断方案是否偏离：字节真相在本地、能力真相在账号、恢复真相在任务。
- **2026-04-28**：新增仓库级 PR 模板 `.github/pull_request_template.md`，将三条第一性原理转为强制勾选项（字节真相在本地 / 能力真相在账号 / 恢复真相在任务），用于日常评审防偏航。
- **2026-04-28**：后端新增项目作用域硬校验：`server/r2-storage-handlers.js` 在 `register-upload` 阶段对 `workspace/projects/*/workflow.json` 执行内容检查，禁止携带 `capabilityPresets` / `capabilitySets`；命中后返回 `PROJECT_CAPABILITY_SCOPE_VIOLATION` 并删除违规对象，防止账号级能力被项目级数据污染。
- **2026-04-28**：能力云同步增强（MVP）：`services/workspaceUserCloudConfig.ts` 新增 `capabilityPresetRecords/capabilitySetRecords`（`updatedAt/deletedAt/value`）记录模型与合并函数；`App.tsx` 拉取云配置时执行本地-云端按时间合并，推送时基于上次记录自动生成 tombstone（软删除），实现“预设/复合能力随账号同步”的冲突合并与删除传播闭环（兼容旧版仅数组格式）。
- **2026-04-28**：项目能力引用迁移（第3步）：`workspaceProjectStore` / `workspaceCloudSync` / `workspaceR2ImageBundle` 增加 `capabilityRefs` 兼容字段；读取历史 `workflow.json` 或本地 bundle 时，若发现遗留 `capabilityPresets/capabilitySets`，会自动迁移为 `capabilityRefs`（含快照兜底）并在后续保存中仅保留引用，避免项目继续携带账号级能力全集。
- **2026-04-28**：迁移可观测性补齐：`workspaceProjectStore` 新增 `consumeWorkspaceMigrationNotices()`，命中历史能力字段迁移时产生一次性提示；`App.tsx` 接入该提示并写入“工作区”运行日志（info），方便验证迁移是否触发且避免重复刷屏。
- **2026-04-29**：本地保存能力开工（第1步落地）：`local-companion` 新增项目文件 API `POST /v1/projects/save-as`、`POST /v1/projects/open`（`local-companion/src/storage/projectFileIO.ts` + `httpHandler.ts`），采用原子写（`.tmp` + rename）与绝对路径校验；项目文件格式为 `assetcutter.local-project@v1`，包含 `bundle.assets/pending/capabilityRefs`，用于后续前端“另存为/打开项目”接入。
- **2026-04-29**：本地保存第2步落地：前端新增本地项目文件调用封装（`services/companionClient/storage.ts`：`saveCompanionProjectFile/openCompanionProjectFile`），并在工作区顶部新增“另存为本地项目 / 打开本地项目”入口（`WorkflowModeShell` + `App.tsx`）。当前交互使用绝对路径输入框；打开后会将文件中的 `assets/pending` 恢复到当前工作区项目并立即本地落盘。
- **2026-04-29**：本地保存验证补齐：已执行 `local-companion` `saveProjectFile/openProjectFile` 冒烟（通过），并在 `docs/本地与云存储分层开发方案.md` 新增“本地保存 MVP 验收清单”（自动化+人工端到端+失败场景错误码回归）。
- **2026-04-29**：本地仓库项目真相源开工：开发文档新增“workspace root 下项目目录即网站项目”决策；`local-companion` 新增 `GET/POST/PATCH/DELETE /v1/workspace/projects`（扫描目录、创建目录、重命名目录、删除目录）；前端 `App.tsx` 已开始接入（非云同步路径优先拉取伴侣项目列表，项目 CRUD 优先调用伴侣接口并带本地回退日志）。
- **2026-04-29**：本地仓库项目真相源扩展到云同步路径：`App.tsx` 的“已登录且开启云同步”初始化流程改为优先采用 `listCompanionWorkspaceProjects` 返回的目录项目；云索引保留 `lastOpenProjectId` 语义，但会按伴侣项目集合做有效性校验，避免云端索引与本地目录分叉时出现无效项目选择。
- **2026-04-29**：工作区项目删除策略升级为软删除：`local-companion/src/storage/workspaceProjects.ts` 的 `deleteWorkspaceProjectFromRepo` 由硬删改为移动到 `<workspaceRoot>/.trash/<projectId>__<timestamp>`；项目列表扫描显式忽略 `.trash`，前端删除调用无需改动即可获得“可回收”语义。
- **2026-04-29**：回收站恢复接口落地：`local-companion` 新增 `GET /v1/workspace/trash/projects`（列出回收项目）与 `POST /v1/workspace/trash/projects/:trashId/restore`（恢复）；若目标项目名冲突，自动改名为 `原名__restored_<timestamp>_<n>` 再恢复，并在响应中返回 `nameResolved`。前端 `services/companionClient/storage.ts` 已补齐对应调用封装。
- **2026-04-29**：回收站最小 UI 落地：`WorkflowModeShell` 在工作区顶部新增「回收站」入口；`App.tsx` 新增回收站弹层（列表 + 恢复按钮），恢复成功后自动刷新项目列表并切换到恢复项目；若 `nameResolved=true`，运行日志提示“已自动改名恢复”。删除确认文案改为“移入回收站，可恢复”。
- **2026-04-29**：项目管理架构收口：主站已移除“路径输入式本地项目打开/另存为”入口与对应 `App.tsx` 处理逻辑；`WorkflowModeShell` 不再接收 `onSaveLocalProject/onOpenLocalProject`；项目真相源统一为本地伴侣 `workspace root` 目录扫描 + 项目 CRUD API，避免“目录项目”与“文件项目”双轨并存。
- **2026-04-29**：`services/companionClient` 收口：移除未再使用的 `saveCompanionProjectFile/openCompanionProjectFile` 导出与类型；删除 `services/localProjectFileHistory.ts`，避免残留旧路径流入口被误复用。
- **2026-04-29**：local-companion 兼容接口弃用标记：`POST /v1/projects/save-as`、`POST /v1/projects/open` 保留兼容但响应体新增 `deprecated/message`，并返回 `Deprecation: true` 与 `Sunset` 响应头；README 与分层方案文档同步为“仅兼容保留、禁止新增依赖”。
- **2026-04-29**：本地伴侣开发文档 API 表同步：`docs/本地伴侣-本地程序开发.md` 的 HTTP API 章节补齐 `/v1/workspace/projects*` 与回收站接口为主路径，并将 `/v1/projects/save-as`、`/v1/projects/open` 明确标为“兼容保留，已弃用”，避免团队按旧路径继续接入。
- **2026-04-29**：分层方案第 10 节检查清单已“实锤化”：`docs/本地与云存储分层开发方案.md` 将三大原则检查项改为 `[x]/[~]` 状态并附当前代码依据与待补动作，便于评审直接按项验收而非口头对齐。
- **2026-04-29**：新增持久化地址守卫：仓库增加 `scripts/check-persistence-guard.mjs` 与 npm 脚本 `guard:persistence`，用于阻断新增 `localhost/127.0.0.1` 与高风险绝对路径常量进入核心代码；分层方案第 10.1 第 3 条已升级为 `[x]`（有自动化守卫支撑）。
- **2026-04-29**：5.2 最小绑定流落地：项目列表卡片新增绑定状态（未绑定 / 已绑定当前账号 / 已绑定其他账号）与“绑定”动作；`App.tsx` 新增绑定确认弹层与 `performBindWorkspaceProject`，绑定后写入项目本地状态（`boundUserId/boundAt`）并尝试 `pushWorkspaceIndex`（仅索引同步，不上传大文件）。
- **2026-04-29**：5.2 最小解绑流补齐：项目列表对“已绑定当前账号”新增“解绑”动作；`App.tsx` 新增解绑确认弹层与 `performUnbindWorkspaceProject`，解绑后移除 `boundUserId/boundAt` 并尝试推送云索引；“已绑定其他账号”状态新增轻提示（可本地使用，绑定当前账号需后续导入为副本流程）。
- **2026-04-29**：新增云索引轻量守卫：仓库增加 `scripts/check-workspace-cloud-index-guard.mjs` 与 npm 脚本 `guard:cloud-index`，约束 `WorkspaceCloudIndexV1`/`pushWorkspaceIndex` 不得夹带 `assets/pending/workflow/base64` 等重字段；分层方案第 10.1 第 2 条已升级为 `[x]`。
- **2026-04-29**：新增工作流内联字节守卫：仓库增加 `scripts/check-workflow-inline-bytes-guard.mjs` 与 npm 脚本 `guard:workflow-inline-bytes`，校验 `packWorkflowBundleForCloud` 返回瘦身结构且云端写入 `workflow.json` 使用 `packed`；新增聚合命令 `guard:storage`（persistence + cloud-index + workflow-inline-bytes），分层方案第 10.1 第 1 条已升级为 `[x]`。
- **2026-04-29**：修正“返回项目目录仍触发云同步”行为：`App.tsx` 云同步链路（自动同步、切项目前同步、返回列表前同步、标脏）统一增加“仅已绑定当前账号项目可同步”约束；未绑定项目返回列表时不再出现“正在同步”及不必要的 R2 上传。
- **2026-04-29**：默认云同步模式收口为“索引同步优先”：`App.tsx` 在自动同步/切项目前同步/返回列表同步路径中移除 `pushWorkflowBundleToCloud`，仅保留 `pushWorkspaceIndex`；默认不再把工作区资产字节推送到 R2，符合“字节真相在本地、云端存轻索引”原则。
- **2026-04-29**：新增“默认路径禁上传工作流资产”硬守卫：增加 `scripts/check-no-default-workflow-upload.mjs` 与 npm 脚本 `guard:no-default-workflow-upload`，扫描 `App.tsx`、`services/workspaceCloudSync.ts`，除函数定义外一律禁止出现 `pushWorkflowBundleToCloud(` 调用；已并入 `guard:storage` 聚合门禁，防止后续回归把默认路径改回“资产上云”。
- **2026-04-29**：工作区项目卡片新增“手动上传”动作（仅“已绑定当前账号”显示）：`WorkspaceProjectShell` 增加上传按钮与加载态，`App.tsx` 增加上传确认弹层与 `performManualUploadWorkspaceProject`，显式调用 `pushWorkflowBundleToCloud` 上传当前项目工作流资产，并写入运行日志；默认自动同步路径仍只做 `pushWorkspaceIndex`。
- **2026-04-29**：`check-no-default-workflow-upload` 升级为白名单守卫：由“固定文件扫描”改为“全仓源码扫描”，除函数定义外仅允许带 `MANUAL_WORKFLOW_UPLOAD_ALLOWED` 标记的调用位置通过，防止将资产上传调用误放回默认路径。
- **2026-04-29**：手动上传体验补齐：`App.tsx` 的上传确认弹窗新增上传前预估（资产数/预览图数/模型引用数/预估体积）；上传成功后回写项目索引字段 `lastManualUploadAt/lastManualUploadAssetCount/lastManualUploadBytesApprox`（`services/workspaceProjectStore.ts`），`WorkspaceProjectShell` 卡片显示“最近手动上传”时间与条目数，便于跨设备/多项目判断最近一次显式上云动作。
- **2026-04-29**：手动上传新增“增量模式”MVP：`App.tsx` 上传确认弹窗加入 `CustomDropdown`（全量 / 仅新增变更）；执行时先拉取云端 `fetchWorkflowPackedFromCloud` 并按 `asset.id + (original/originalObjectKey/modelUrls/title)` 做差异筛选，仅上传变化资产。若读取基线失败，会写运行日志并回退全量上传，保持显式可见。
- **2026-04-29**：手动上传可靠性补齐：`WorkspaceProject` 增加上传结果明细字段（尝试/成功/失败资产 id/错误摘要/模式）；`WorkspaceProjectShell` 卡片显示“上次上传成功/失败”并在有失败项时提供“重试失败项”按钮；`App.tsx` 抽出 `executeManualWorkflowUpload`，支持按资产 id 子集重传，重试路径固定增量模式。
- **2026-04-29**：上传守卫再收紧：`check-no-default-workflow-upload.mjs` 在 marker 白名单基础上，新增“调用函数名白名单”校验（当前仅允许 `executeManualWorkflowUpload` 内调用），防止 marker 被复制到默认路径绕过门禁。
- **2026-04-29**：失败项可视化与选择重试：`WorkspaceProjectShell` 的失败摘要可打开明细；`App.tsx` 新增失败详情弹层（错误摘要 + 失败 `assetId` 勾选列表），支持“重试所选失败项”；上传日志新增 `skipped` 计数，明确提示“所选但本地已不存在”的条目。
- **2026-04-29**：失败重试交互再增强：失败详情弹层补齐“全选/全不选/仅选本地仍存在”快捷按钮，列表逐项标记“本地不存在”；底部新增“将重试 N 项（不可用 M 项）”提示，并在执行前输出 `selection_total/selection_existing/selection_missing` 日志，降低批量重试误操作成本。
- **2026-04-29**：失败列表检索：`App.tsx` 失败详情弹层增加 `assetId` 关键字过滤输入框与“列表显示 X/Y”提示；新增“仅选当前列表”将勾选同步为当前过滤可见项；关闭/重试/遮罩关闭时清空过滤状态。
- **2026-04-29**：失败 ID 复制：`App.tsx` 失败详情弹层增加「复制当前列表」「复制已勾选」，使用 `navigator.clipboard.writeText`（首行 `#` 注释元信息 + `---` + 每行一个 ID）；成功与失败均通过运行日志提示，便于非 HTTPS 或权限拒绝场景排障。
- **2026-04-29**：剪贴板兜底与键盘：`copyTextToClipboardWithFallback` 依次尝试 Clipboard API、`execCommand('copy')`；最终失败则 `z-[5700]` 手动复制弹层 + `useLayoutEffect` 自动全选；失败详情与手动复制弹层均支持 Esc（优先关闭手动复制层）；兼容模式成功写入时运行日志标注「兼容模式」。
- **2026-04-29**：分层方案 P0 落地：`App.tsx` 去掉登录云 hydrate 与切换项目时的自动云端 workflow 拉取（原 `runCloudWorkflowPull`），打开项目仅以本地 `loadWorkflowBundle` 为准，避免 R2 历史包静默覆盖本地画布；`docs/本地与云存储分层开发方案.md` 10.1 增补对应检查项。
- **2026-04-29**：分层方案 P1（部分）：新增 `services/workflowCompanionAssets.ts`（原图 `put`/`strip`/伴侣拉取为 object URL）；`saveWorkflowBundle` 写入 IndexedDB 前对带 `originalCompanionKey` 的资产去掉 data/blob 形态 `original` 以瘦身；`WorkflowSection` 在「入队新图」与「本机多图拖入」路径异步落伴侣并写 `originalCompanionKey`，挂载时从伴侣补全仅键无图的资产；`workflowModelBlob` 删除资产时顺带释放 `original`/`results` 中的孤儿 blob URL。
- **2026-04-29**：分层方案 P2（核心）：`workspaceProjectStore` 新增 `migrateWorkflowBundleProjectId`（flush → 读旧 bundle → 写入新 id → `removeWorkflowBundle` 旧键）；`App.tsx` 在伴侣 `PATCH` 重命名成功且返回的 `project.id` 与旧 id 不同时自动迁移 bundle，并同步当前打开项目、`lastOpen`；`loadWorkspaceProjectInternal` 在已配置伴侣 base 时异步拉 manifest，失败或 `projectId` 与选中 id 不一致时写运行日志（轻量打开校验）。
- **2026-04-29**：分层方案 P3（产品预期）：`WorkflowModeShell` 项目列表增加「跨设备说明」、`SettingsSection` 数据与存储补充账号 vs 画布说明；`WorkspaceSidebarFooter` 更新云同步 tooltip / 立即同步按钮 title；移除已不再触发的云端「还原中」侧栏占位；`docs/本地与云存储分层开发方案.md` §9 / §10.1 增补对应条目。
- **2026-04-29**：回收站恢复与 bundle 对齐：`App.tsx` 在伴侣恢复成功且目录名与回收条目前缀 id 不一致时，若 IndexedDB 仍存在该旧 id 的非空 workflow bundle，则调用 `migrateWorkflowBundleProjectId` 迁到新目录 id，避免「恢复后画布空白但伴侣目录已有文件」；分层方案 §10.1 相应收紧表述。
- **2026-04-29**：P1 伴侣落盘路径扩展：`WorkflowSection` 抽出 `scheduleCompanionPersistOriginal`（`data:` 才上传）；切割确认、仓库拖入工作区克隆同步落伴侣并写 `originalCompanionKey`；分层方案 §10.1 更新覆盖范围说明。
- **2026-04-29**：手动上云兼容「仅 `originalCompanionKey`」：`workflowCompanionAssets.fetchCompanionAssetAsDataUrl`、`packWorkflowBundleForCloud` 可选 `companionHydrate`、`pushWorkflowBundleToCloud` 透传；`App.tsx` 手动上传前置校验伴侣 base；打包失败（伴侣不可读）时抛错进入上传失败明细。
- **2026-04-29**：步骤 `results` 大图伴侣链路：`types.WorkflowAsset.resultsCompanionKeys`；`putWorkflowResultImageToCompanion` / `strip` / `workflowAssetNeedsCompanionResultHydrate`；`WorkflowSection` 队列写回后用 `flushSync`+`scheduleCompanionPersistResult` 落盘、挂载时 hydrate、`discardResult` 清键；`packWorkflowBundleForCloud` 在 `companionHydrate` 下补读各步结果再上云；手动上传前置文案含步骤结果。
- **2026-04-29**：修复工作流伴侣键含 `/` 导致伴侣 `PUT` 一律 400：本地伴侣 `safeIds` 仅允许单段 key，`workflowCompanionAssets` 中原 `wf/original/...`、`wf/results/.../...` 已改为 `wf-orig-...`、`wf-res-...`（见 `docs/错题本.md`）。
- **2026-04-29**：空项目名新建 + 工作区目录名校验：`App.tsx` 对伴侣默认 `proj-<ts36>`；伴侣侧区分「工作区文件夹名」与「资产 key」校验（`isSafeWorkspaceFolderName` / `isWorkspaceTrashDirName`），避免仅 ASCII `isSafeIdPart` 误伤中文项目名及空名回退 UUID 导致 manifest 404。
- **2026-04-29**：线上 Vertex「not configured」排障与部署：`server/gemini-proxy-api.js` 启动时支持 `GOOGLE_APPLICATION_CREDENTIALS_JSON`（及别名）将服务账号 JSON 写临时文件供 ADC；启动日志输出 Vertex 是否已配置；`render.yaml` 的 `assetcutter-gemini-proxy` 增补 `VERTEX_PROJECT_ID`、`VERTEX_LOCATION`、`GOOGLE_APPLICATION_CREDENTIALS_JSON` 占位；详见 `docs/VERTEX_AI_INTEGRATION.md`。
- **2026-04-29**：Render 双 gemini-proxy 对齐：实测 `assetcutter-gemini-proxy` `/healthz` 为 `vertex.configured: true`，`assetcutter-ai-pro` 为 `false`；已将静态站 `assetcutter-web` 的 `VITE_BULK_IMAGE_API` 设为 `https://assetcutter-gemini-proxy.onrender.com` 并触发重新构建部署，使前端与已配 Vertex 的代理一致。
- **2026-04-29**：分层方案 §10.1「字节真相」原图伴侣链路收口：`workflowCompanionAssets` 增加任意可规范化图串 → 伴侣 PUT；`WorkflowSection` 增加 `scheduleCompanionPersistOriginalAny` 与 `InsertManualGroupResult`，覆盖手动组/嵌套组/切割/克隆/旧 `cutImageGroup` 字符串等；`stripWorkflowBundleForIdbPersist` 扩展含 `http` 原图瘦身；`docs/本地与云存储分层开发方案.md` §10.1 对应项已勾选并写现状。
- **2026-04-29**：M2 浏览器侧 schema + manifest 对账/修复：`WorkflowProjectBundle.workflowBundleSchemaVersion` 等（`workspaceProjectStore`）；`findCompanionKeysMissingFromManifest`（原图 + `resultsCompanionKeys`）、`attemptRepairCompanionManifestKeyGaps`（内存有图时重 PUT 写回 manifest）、`App.tsx` 打开项目后告警并异步修复；分层方案 §7 M2、§10.1 已同步。
- **2026-04-29**：本地伴侣 manifest 扫盘补登记：`local-companion` 实现 `reconcileManifestOrphansFromDisk`（磁盘已有 `assets/<key>/object` 但 manifest 缺条则补写）与 `POST /v1/projects/:projectId/manifest/reconcile`；前端 `reconcileCompanionManifestFromDisk`；`App.tsx` 在 `loadWorkspaceProjectInternal` 中若缺键**先** reconcile、再 `getCompanionManifest` 重算缺口，余量再走 `attemptRepairCompanionManifestKeyGaps`；`pluginRegistry.storage` 登记 `reconcileManifest`。
- **2026-04-29**：开发文档同步：根 `README.md` / `DOCS.md` 增补 **`npx tsc --noEmit`** 与根 `tsconfig` 排除 **`示例项目/`** 的说明；`docs/本地伴侣-本地程序开发.md` 新增 **§3.1 manifest 扫盘补登记**；`docs/本地伴侣-存储与计算规范.md` §4.3 增补 `GET manifest` / `POST manifest/reconcile`；`local-companion/README.md` 功能表增补 reconcile 一行。
- **2026-04-29**：桌面伴侣 UI 改版：`companion-desktop` 主窗口改为内嵌 **`shell/index.html`**（**两页**：首页含状态机 +「打开网站」+ 插件表，设置页集中其它项）；默认不自动打开浏览器本机管理页；`local-companion` 默认 **`COMPANION_OPEN_BROWSER` 关闭**，需显式 `=1` 恢复自动打开；注册协议 **`assetcutter-companion://`**；主站设置「本地伴侣」增加 **调起桌面伴侣** 链接。
- **2026-04-29**：桌面壳鉴权与 UI 卡死修复：Electron 主进程 **`readSharedToken`** 与配对向导一致，在 `COMPANION_SHARED_TOKEN` 未注入主进程时回退读取 **`pairing-config.json` 的 `sharedToken`**，避免 `/v1/capabilities` 等需 Bearer 的接口 401；`shell` 放宽 CSP 内联脚本、preload IPC 超时、首页初始化与能力列表错误提示增强。详见 `docs/错题本.md` 对应条。
- **2026-04-29**：本地伴侣**发行与插件概念**文档化（`docs/本地伴侣-插件与发行.md`）：区分桌面壳 / 宿主插件包 / 运行时 capabilities；实现 **管理后台「本地伴侣发行」**（R2 `public/companion-distribution/` 预签名上传 + `server/data/companion-artifacts.json`）、**用户侧栏伴侣区「下载壳」**（`GET latest` + 登录后 `resolve-download` 预签名 GET）。涉及 `server/companion-artifacts-store.js`、`server/r2-storage-handlers.js`（分发前缀与预签名）、`server/auth-api.js`、`AdminCompanionArtifactsPanel`、`WorkspaceSidebarFooter`。
- **2026-04-29**：发行链路补强：**删除发行记录时同步删 R2 对象**（`deleteR2ObjectByKey`）；公开摘要含 **sha256**；**local-companion** 增加 `GET/POST /v1/host-plugins/*`、`host-bundles/` 落盘与 `runtime-status.hostPluginBundles`；**设置 → 本地伴侣** 增加「安装最新宿主插件包」；**companion-desktop** 按网站 origin 轮询桌面壳 **latest** 并托盘提示新版本。
- **2026-04-29**：**宿主插件包 ZIP**：`local-companion` 依赖 **`yauzl`** 流式解压至 `host-bundles/<semver>/extracted/`，防 zip-slip；**`/v1/capabilities`** 动态追加 `plugin.host_bundle.*`；文档 §3/§4 已同步。
- **2026-04-29**：宿主包 **`extracted/run.json`**（RunSpec v1）在伴侣侧解析，**`GET /v1/host-plugins/bundles`** 与 **`install-from-url`** 返回 **`runSpec`**；capabilities 中 `plugin.host_bundle.*` 的 detail 会提示是否含 exec/probe。electron-updater：**登记时写入可选 `sha512`（hex）**；auth-api 新增 **`GET /api/companion-artifacts/electron-app-update.yml`**（查询参数与 `latest` 一致），服务端需配置 **`COMPANION_DIST_PUBLIC_HTTP_BASE`**（公网可读文件 URL 前缀，无尾斜杠）；YAML 内 **sha512** 为 Base64（由 hex 转换），见 `server/companion-electron-feed.js`。
- **2026-04-29**：计算任务 **`host_bundle.exec`** / **`host_bundle.probe`**：`local-companion/src/compute/hostBundleExecAdapter.ts` 按 **`inputs.dirName`** 定位 `host-bundles/<dir>/`，仅读取磁盘 **`run.json`** 的 **exec/probe** 段，在 **extracted** 根下解析 **cwd** 后 `spawn`（输出截断 64KB；超时 **`COMPANION_HOST_BUNDLE_EXEC_TIMEOUT_MS`**）；`jobsStore` 登记 jobTypes；伴侣管理页可试 **probe/exec** 按钮。
- **2026-04-29**：主站 **`SettingsSection`（本地伴侣）** 接入：`services/companionClient/hostPlugins.ts` 拉取 **`GET /v1/host-plugins/bundles`**；**`submitCompanionHostBundleProbeJob` / `submitCompanionHostBundleExecJob`** 提交计算任务；设置页提供下拉选择包、**运行 probe/exec**、并把 **`jobId` 写入任务进度区**（`pullCompanionJobEvents` 支持 **`jobIdOverride`**）；**`App.tsx`** 传入 **`activeWorkspaceProjectId`** 写入任务元数据。
- **2026-04-29**：开发文档同步宿主包全链路：**`docs/本地伴侣-本地程序开发.md`**（API 表、目录、`run.json` 计算、环境变量、与主站关系）、**`docs/本地伴侣-插件与发行.md`**（§4 `run.json`/计算、§5 演进、electron-updater、**`companion-electron-feed`**）、**`docs/本地伴侣-存储与计算规范.md`**（§5.1 补充 **`host_bundle.*`**、§7 **`hostPlugins.ts`**、§11.4）、**`local-companion/README.md`**、**`companion-desktop/README.md`**（feed URL）、**`DOCS.md`** / 根 **`README.md`** 索引行。
- **2026-04-29**：**发布与 CI**：新增 **`docs/网站与发布检查清单.md`**（主站 vs 桌面「第一版」定义、`typecheck`/`lint`/`guard:storage`/`build`、Render 环境变量、最小手测）；**`.github/workflows/ci.yml`** 对 **`main`/`master`** 的 push/PR 跑上述门禁；**`docs/本地伴侣-发布前检查清单.md`** 增加本站 **electron-updater YAML** 勾选项；**`DOCS.md`**、**`docs/本地伴侣-本地程序开发.md`** 文首互链。
- **2026-04-29**：CI 补强：根 **`vitest.config.ts`** 限定 **`tests/**/*.test.ts`**（避免拾取 **A-Driver** 内非 Vitest 文件导致 `npm test` 失败）；workflow 增加 **`npm test`** 与 **`local-companion/`** 下 **`npm ci` + typecheck**；根 **`package.json`** 脚本 **`local-companion:typecheck`**；**`网站与发布检查清单`**、**`DOCS.md`** 已同步。
- **2026-04-29**：工作流触发本机 **`host_bundle.*`**：`types.CustomAppModule.companionHostBundle`、`capabilityExecutor.executeCompanionHostBundleCapability`；**`WorkflowSection`** / **`executeCapabilitySet`** 传 **`companionProjectId`**；**`CapabilityPresetSection`** 配置 UI；**`WorkflowComposerOverlay`** / **`CapabilitySetCanvas`** 画布试运行透传项目 id；**`docs/本地伴侣-插件与发行.md`** §5 更新为已实现。
- **2026-04-29**：补齐 `host_bundle` 自动化测试：`tests/capabilityExecutor.test.ts` 新增 `executeCapability` 的 probe/exec/失败分支；`tests/workflowTextAsset.test.ts` 新增宿主包预设允许图片/文字拖拽断言，防止后续改动回归。
- **2026-04-29**：新增 `tests/capabilityPresetStore.test.ts`（`companionHostBundle` 归一化）；并修复 `normalizeCapabilityPreset` 的回归：`generate_3d` 预设在后段归一化不会再误写回 `companionHostBundle`。
- **2026-04-29**：新增 `tests/capabilityPresetPersistence.test.ts`：mock `clientPersist`，对 **`saveCapabilityPresets` → `loadCapabilityPresets`** 做往返断言（与能力页经 `onUpdate` 持久化路径一致），覆盖 `companionHostBundle` 保留与空白 `dirName` 清除。
- **2026-04-29**：补充最小组件交互测试：安装 `@testing-library/react`、`@testing-library/user-event`、`jsdom`；`vitest.config.ts` 的 include 扩为 `tests/**/*.test.{ts,tsx}`；新增 `tests/capabilityPresetSection.test.tsx`，覆盖「新增能力」填写宿主包目录后 `onUpdate` 写出 `companionHostBundle`。
- **2026-04-29**：开发文档同步测试栈变更：`DOCS.md`、`README.md`、`docs/网站与发布检查清单.md` 将 Vitest 范围更新为 `tests/**/*.test.{ts,tsx}`，并补充组件测试依赖（jsdom + Testing Library）。
- **2026-04-29**：发布前收敛检查：当前版本仍被 `lint` 阻塞（存在 error，不可直接发布）；已先做低风险快修（`.gitignore` 增加 `.data/`、`server/companion-electron-feed.js` 正则转义、`scripts/test-vertex-json.js` 移除 `require` 写法），并在 `网站与发布检查清单` 明确“lint error=0”门禁。
- **2026-04-29**：发布阻塞清理第一阶段完成：继续修复 `react-hooks/refs` / `react-hooks/purity` / 机械规则（`prefer-const`、正则字符集等），`npm run lint` 已从 error 阻塞降为 **0 error（仅 warnings）**；`npm test -- --run` 与 `npx tsc --noEmit` 均通过。
- **2026-04-29**：发布收敛第二阶段（低风险 warning 清理）已执行：移除多处未使用导入/变量并补少量安全依赖，`npm run lint` 警告数由 **101 → 88**（仍无 error）；门禁保持可通过。
- **2026-04-29**：发布收敛第二阶段继续推进：新增一批低风险 unused 清理（`App.tsx`、`WorkflowSection.tsx`、`PromptArenaSection.tsx`、`geminiService.ts`），`npm run lint` 警告数由 **88 → 64**，保持 **0 error**（其中 `WorkflowSection` 仍以 `exhaustive-deps` 为主）。
- **2026-04-29**：继续 warning 清理并修复回归：本轮处理 `App.tsx` / `CapabilityPresetSection.tsx` / `PromptArenaSection.tsx` / `WorkflowSidebarColumn.tsx` 的 hooks 依赖与未使用项；中途修复了 `openNewSet` 提前引用与缺失 `useCallback` 导入导致的 `tsc/test` 失败；最终门禁为 `npx tsc --noEmit` 通过、`npm test -- --run` 通过、`npm run lint` 为 **0 error / 52 warnings**。
- **2026-04-29**：继续按“低风险 warning 清理”推进：`CapabilityPresetSection.tsx` 修正工具栏 `add-set` 回调声明顺序（避免 TDZ），并清理拖拽/详情编辑回调中的不必要依赖与未使用函数；`npm run lint` 进一步到 **0 error / 50 warnings**（剩余仍以 `WorkflowSection.tsx` 的 `exhaustive-deps` 为主）。
- **2026-04-29**：继续收敛 `App.tsx`/能力区 warning：将 `pullWorkspaceProjectsFromCompanion` 前移到首次使用之前并补齐 effect 依赖，移除 `CapabilityPresetSection.tsx` 的未使用锁定判断函数；`openWorkspaceTrashDialog` 补 `addGlobalLog` 依赖。门禁结果：`npm run lint` **0 error / 45 warnings**，`npx tsc --noEmit` 通过，`npm test -- --run` 通过。
- **2026-04-29**：开始分批清理 `WorkflowSection.tsx` 的低风险 `exhaustive-deps`：补齐部分回调依赖（大纲导航、lightbox 预览、pending 变更、compose 输入），并移除两个不必要依赖项；全量结果为 `npm run lint` **0 error / 48 warnings**（较上一轮有波动，仍全部集中在 `WorkflowSection.tsx`）、`npx tsc --noEmit` 通过、`npm test -- --run` 通过。
- **2026-04-29**：`WorkflowSection.tsx` 继续做“高收益低风险”收敛：将 `assets/pending/capabilitySets` 的 `Array.isArray(...) ? ... : []` 改为 `useMemo` 稳定引用，显著消除 `assets/pending/capabilitySets conditional` 连锁告警；门禁结果：`npm run lint` **0 error / 21 warnings**、`npx tsc --noEmit` 通过、`npm test -- --run` 通过。
- **2026-04-29**：warning 收尾完成：继续收敛 `WorkflowSection.tsx`（`getAssetDisplayImage/getAssetDisplayText` 稳定化、`runTask` 回调化、依赖数组精简与补齐、组成员计算 `useMemo`），并修正多处 hooks 依赖噪音；最终达到 `npm run lint` **0 error / 0 warning**，且 `npx tsc --noEmit`、`npm test -- --run` 全部通过。
- **2026-04-29**：发布门禁收尾补齐：`guard:storage` 初次执行被 `local-companion/src/hostPluginBundles.ts` 中回环地址黑名单文本命中（守卫按全文件关键字匹配）；已在 `scripts/check-persistence-guard.mjs` 白名单加入该文件后重跑通过。当前门禁状态：`guard:storage`、`build`、`lint`、`tsc`、`test` 全通过。
- **2026-04-29**：发布执行阶段开始：已将“网站第一版分批发布策略 + 可直接执行命令”写入 `docs/网站与发布检查清单.md`（§2.6/§2.7/§2.8），并按白名单完成第一批网站范围暂存（确认未纳入 `local-companion/`、`companion-desktop/`、`server/data/`）；下一步进入预发布手测。
- **2026-04-29**：发布输出噪音收敛：新增 `tailwind.config.js` 并补齐 `content` 扫描路径，消除 `build/test` 中 Tailwind `content` 缺失告警；复验 `npm run build`、`npm test -- --run` 均通过（仍有大 chunk 提示，属性能优化项，不阻塞发布）。
- **2026-04-29**：会话纪要 — 上轮：预发布手测计划；**新话题**：用户决定**跳过**线上最小手测，询问按开发文档是否仍有待办及是否可发布。交接文档 §5 与 `docs/网站与发布检查清单.md` 仍建议手测与发布后短时回归；**跳过手测不阻塞本地门禁通过**，但登录/工作区/管理等主路径的线上风险需发布方自担。当日在仓库根目录复核：`npm run lint`、`npm run typecheck`、`npm test`、`npm run guard:storage`、`npm run build` 均通过（`vite build` 仅有 chunk 体积提示）。
- **2026-04-29**：继续发布 TODO 收口：`npm run local-companion:typecheck` 通过；`npm run companion-desktop:release:check` 通过。当前工作区 `git status --short` 仅剩文档文件变更（`docs/交接文档.md`、`docs/网站与发布检查清单.md`），未见 `local-companion/` 或 `companion-desktop/` 未提交改动，故第二批与第三批待办均标记完成。
- **2026-04-29**：用户约定：每次修改 `companion-desktop/` 影响运行时的内容后，**同一会话内重启** `npm run companion-desktop:start` 便于其验收；已写入 `.cursor/rules/companion-desktop-restart.mdc` 并在 `.cursor/rules/index.mdc` 索引。
- **2026-04-29**：桌面壳「存储」产品化：`shell/index.html` 存储区说明与「打开当前存储文件夹」（`shell.openPath` → IPC `shell-open-folder-path`）；`main.cjs` / `preload-shell.cjs` 对接。
- **2026-04-29**：桌面壳「存储」**应用/默认**：先弹出原生对话框（迁移并应用 / 仅切换目录 / 取消）；迁移路径为 **停伴侣 → 将旧卷根下子项复制到目标目录并删除旧卷根 → `saveShellSettings` → 重启伴侣**（`main.cjs`：`migrateCompanionVolumeTree`、`stopCompanionForVolumeChange`、IPC `shell-apply-volume-change`；`preload-shell.cjs` 暴露 `applyVolumeChange`）。
- **2026-04-29**：**移除独立首次向导**：原 `wizard/index.html`、`preload-wizard.cjs` 删除；配对（本机通信密码、允许的网站地址）并入 **`shell/index.html` 设置 →「与网站配对」**；IPC 改为 **`shell-load-pairing` / `shell-save-pairing`**；启动时不再打开向导窗口；托盘「首次设置向导」菜单项移除。`docs/本地伴侣-本地程序开发.md`、`companion-desktop/README.md`、`scripts/test-companion-desktop.ps1` 已同步。
- **2026-04-29**：**安装包默认主站**：`app.isPackaged === true` 时，未写入过 `companion-shell-settings.json` 的默认 **`siteUrl`** 为 **`https://assetcutter-ai-pro.vercel.app/`**；开发运行（`npm start`）仍为 **`http://localhost:3000`**。`preload-shell.cjs` 暴露 **`defaultSiteUrl`** 与主进程一致。
- **2026-04-29**：主站 **`SettingsSection`（本地伴侣）**：新增显式 **「与网站配对」** 卡片（本机通信密码、当前站点 Origin 复制、保存配对密码）；高级区仅保留 **本机 HTTP 地址**，避免用户误以为需单独打开桌面向导。
- **2026-04-29**：`WorkspaceSidebarFooter`：移除 **自动同步开关**、**立即同步**；云空间区增加 **云图标 + 用量文案 + 进度条** 一体化卡片；本地伴侣区改为 **图标 + 状态 + 分隔线下载桌面壳**；`App.tsx` 移除定时自动同步 interval 与 `triggerWorkspaceCloudSyncNow`（切换项目/返回列表等路径仍可直接 `pushWorkspaceIndex`）。
- **2026-04-29**：工作流模式**项目列表**主区（`WorkflowModeShell`）去掉顶部 **云空间用量条**、**云同步关闭提示**、**跨设备说明** 长文案，使「工作区」首屏更简洁。
- **2026-04-29**：主界面进一步**去掉所有云存储配额类提示**：侧栏 `WorkspaceSidebarFooter` 移除云空间卡片（用量/进度条），登录用户始终显示 **API 配置** 快捷入口；`WorkflowModeShell` 移除画布顶部 **云空间已满** 横幅；设置 **数据与存储** 删除「云端（登录且开启工作区云同步）」整段及云配额/对象存储说明，本机条目去掉「登录后云端同步」引导。`App.tsx` 删除仅用于上述展示的用量派生变量。后台配额逻辑与日志仍保留；管理后台用户表「云空间」列未改。
- **2026-04-29**：会话纪要 — 推送到 GitHub：`dce6df5`（`master`→`origin/main`；桌面壳配对并入 shell、独立向导删除、工作流/侧栏与设置调整；根 `.gitignore` 增加 `server/data/companion-artifacts.json` 与 `companion-desktop/release-*/`）。
- **2026-04-30**：会话纪要 — 推送到 GitHub：`bf54ce8`（伴侣工作流执行前输入图解析与 `isDisplayableWorkflowImageRef` 补水、`companion-desktop` **0.1.1**、`docs/错题本` 记录）。
- **2026-04-30**：**本地伴侣发行元数据线上持久化**：Render auth-api 磁盘为临时文件系统，仅靠 `server/data/companion-artifacts.json` 会在部署后丢失登记。现 **`DATABASE_URL` 存在时** 使用 Postgres 表 **`companion_artifacts`**（`server/companion-artifacts-store.js`）；侧栏拉取失败与「无记录」区分文案（`WorkspaceSidebarFooter.tsx`）。
- **2026-04-30**：**跨域下载壳 403 CSRF**：`POST /api/companion-artifacts/resolve-download` 在 `assertCsrf` 中豁免（与 `/api/r2` 一致），避免 Vercel 前端读不到 auth 域 `ac_csrf` 导致 `CSRF token invalid`。

---

**文档版本**：随仓库迭代更新；重大架构变更时请同步修改本节与「预览功能架构」。