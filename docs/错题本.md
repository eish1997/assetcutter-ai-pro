# 错题本

> **读法（省 token）**：日常只读本文件 **「活跃区」**（约 35 条）；更旧或更长的条目在 [`错题本-归档.md`](./错题本-归档.md)，用标题关键词 `grep` 即可。  
> **格式**：一行 = 现象 → 原因 → 修复 + 文件。维护时对齐此格式追加到活跃区顶部；活跃区超过 ~40 条可将最旧几条迁入归档。

---

## 活跃区（倒序）

### 2026-06-26: 移除分镜表顶栏解析/优化预设下拉
解析页改规则切块后，顶栏「结构化解析」「结构化优化」预设选择与 localStorage 同步已无入口；编辑页单镜解析/优化固定用内置 preset · `StoryboardTablePanel.tsx` `WorkflowSection.tsx`

### 2026-06-26: 解析页改为规则切块（仅镜号+时长，原文入库）
去掉 LLM「转换格式」；按行首镜号切块，规则识别时长；镜号+时长齐备才可「创建镜头」；每镜只写 `shotRaw` 整段原文，不拆字段列 · `parseStoryboardRawShotsFromText` `StoryboardTableBulkInput.tsx`

### 2026-06-26: 编辑页仍显示旧长列名且画面/运镜为空
写入/加载链路中 `canonicalizeStoryboardImportFields` 迁到 `storyboardTableParse.ts` 后漏引 `inferFieldKind`，导入运行时抛错，字段未写入标准列；旧 catalog 与 `【运镜标签】` 内容也未对齐 · 补 `inferFieldKind`；修复 `applyStoryboardBulkImport` 结尾；运镜标签保留标签名；加载/写入时 `mergeStoryboardCatalogToStandardFieldLabels` 合并长列 → `画面`/`运镜` · `storyboardTableParse.ts` `storyboardTableBulkImport.ts` `storyboardTableAsset.ts`

### 2026-06-26: 分镜格式转换 45s 客户端超时（非模型慢）
`bulk_normalize` 走 gemini-async 排队+轮询+大 JSON 输出，但 `workflowChat` 默认 `GEMINI_REQUEST_TIMEOUT_MS=45s` 先掐断；审计可见 `>45000ms` · 分镜 bulk 提至 `STORYBOARD_BULK_LLM_TIMEOUT_MS=300s`；格式转换默认 `gemini-2.5-flash`；`getDialogTextResponse` 按 model 选通道 · `storyboardTableBulkAiDetect.ts` `geminiService.ts`

### 2026-06-26: 解析页改为 LLM 整篇格式转换（固定 8 列）
不再「解析字段 + 生成分镜文本」两步；整篇 LLM 只改结构不改内容，输出固定表头 `镜头号 | 时长 | 景别 | 焦距 | 画面 | 运镜 | 对白 | 备注` · `convertStoryboardParsePageFormatWithLlm` `StoryboardTableBulkInput.tsx`

### 2026-06-26: 解析页 LLM 误用单镜 preset 指令导致慢/格式错
解析页 `parseStoryboardParsePageWithLlm` 拼接 `preset.instruction`（单镜 `fields` JSON），与批量 `rows` 解析器不一致，长脚本易慢或返回格式不对 · 固定 `DEFAULT_STORYBOARD_BULK_PARSE_INSTRUCTION` + 解析页 7 字段 hint；preset 仅路由模型；UI 提示「整篇一次 LLM」 · `storyboardParsePageCore.ts` `StoryboardTableBulkInput.tsx`

### 2026-06-26: 解析页宽泛镜号把纯数字误判为时长
`041`、`001` 等镜号在 `extractBroadShotNoFromLine` 中被 `DURATION_TOKEN_RE` 当成时长 token 跳过，导致「无镜号」→ 时长正则要求秒/帧后缀或小数，纯整数不再匹配 · `storyboardParsePageCore.ts`

### 2026-06-26: 拼图/角色替换手动切分框确认后多裁
调整切分框弹窗里框选正确，确认切割后结果比框小、比例不对 → 手动路径仍 `refineFeedbackCollageCropBox` 二次内缩 + `trimImageDataUrlContentBounds` 再裁白边 → 手动确认仅用 `clampStoryboardSheetSplitBox` 精确裁切、跳过 trim；弹窗初始框改 `feedbackCollageLayoutToManualAdjustBoxes` · `storyboardFeedbackCollageSplit.ts` `StoryboardTablePanel.tsx`

### 2026-06-26: 分镜编辑页网格裁切与拼图改图回填丢边
编辑页中间网格 `object-cover` 裁切显示，右侧详情 `object-contain` 全图；反馈拼图改图/角色替换拼 canvas 时 `measureFeedbackCollageImageDrawRect` 把竖图高度 cap 到 4:3 格内，切分回填丢上下边 → 网格改 `object-contain`；拼图绘制与 layout 切分框统一 `measureImageContainedDrawRect`（object-contain） · `StoryboardEditCanvasGrid.tsx` `storyboardCompositeFrameRender.ts`

### 2026-06-24: 工具独立窗口「窗口 API 未就绪」
`electron-builder` `files` 未包含 `preload-tool-window.cjs`，安装包内 preload 缺失 → `window.companionToolWindow` 未注入 · `companion-desktop/package.json`

### 2026-06-24: 小工具云下载 URL 主机未在白名单（续）
`publicInstallUrl` 主机常与 `build-constants` auth-api 不一致（如 Vercel `/api/r2` 代理）→ 发行目录 `install-from-url` 在已登记 sha256/bytes 时启用 `allowCatalogInstallHost`；壳子进程另注入 siteUrl 主机到信任列表 · `bundleInstallCore.ts` `shellToolBundles.ts` `hostPluginBundles.ts` `main.cjs`

### 2026-06-24: 小工具云下载 URL 主机未在白名单
发行 `publicInstallUrl` 走 auth-api `/api/r2/...` 代理（如 `assetcutter-auth-api.onrender.com`），伴侣 `install-from-url` 仅放行 R2 域 → 自动信任 `COMPANION_AUTH_API_ORIGIN` / `COMPANION_DIST_PUBLIC_HTTP_BASE` 主机；桌面壳拉起子进程时注入 `build-constants` 的 auth 域 · `bundleInstallCore.ts` `main.cjs`

### 2026-06-24: 小工具架 IPC 超时 / 安装原子性 / 卸载未关窗
安装 ZIP 或运行示例工具（`timeoutMs: 600000`）在 15s 被 preload IPC 截断报「IPC 超时」；`commitShellToolStagingPackage` 在 cp 成功后写 manifest 失败会留下无 manifest 的残缺目录；有更新时点击卡片会打开旧版；卸载后独立工具窗口仍存活；`openPath` 动作无实现 → preload `api` 透传 `opts.timeoutMs`（上限 600s）；manifest 写入纳入同一 try 并回滚；同 toolId 安装互斥；卸载调 `closeToolWindow`；有更新时卡片主点击不打开（仅「更新」按钮）；`openPath` 调 `openFolderPath` · `preload-shell.cjs` `preload-tool-window.cjs` `shellToolBundles.ts` `tools-page.js` `tool-window.js` `main.cjs` `httpHandler.ts`

### 2026-06-23: 规整网格分镜拼图只识别成一整块（续）
上轮网格回退仍失败：① 单框被标 `ready` 缓存不再重识别；② `detectAutoGrid` 细黑线检测失效且坐标系混用 → **均匀网格拟合**（扫描 2–8 列行，按网格线暗度选最佳如 5×4）；与视觉/自动宫格多路取最多格；整图单框不算可用缓存并强制重识别；可填 `layoutCols×layoutRows` 直切 · `storyboardSheetVisionSplit.ts` `gridDetector.ts` `storyboardSheetPreview.ts` `StoryboardTablePanel.tsx`

### 2026-06-23: 规整网格分镜拼图只识别成一整块
5×4 等带黑色网格线的分镜条，Gemini 常只返回 1 个大框 → 检测若 `isCollapsedStoryboardSheetVisionDetect` 则对原图 `detectAutoGrid` 按网格线切格；再二次读顶部「121 | 4s」镜号；prompt 强调禁止整图单框 · `storyboardSheetVisionSplit.ts` `gridDetector.ts`

### 2026-06-23: 分镜拼图无镜号无法切分 / 镜号位置不固定识别差
无表内镜号时生成拼图不可切分；视觉 prompt 假定镜号在固定角标；混合文字（景别+镜号+运镜）解析弱 → 增强 prompt 先读字再猜镜号；`inferShotNoFromMixedText` 从混合文本推断；无镜号时按阅读顺序切分并 `autoCreateRows`；生成拼图有图即可切分 · `storyboardSheetVisionSplit.ts` `storyboardSheetPreview.ts` `StoryboardTablePanel.tsx`

### 2026-06-23: 分镜拼图切分重复视觉识别
上传/生成拼图后已自动识别，点「切分」仍二次调 Gemini 或弹框重识别 → ① 批量生图 `onChunkReady` 未排队后台识别；② 切分路径对 `generated` 源仍调 `detectStoryboardSheetPanels`；③ 有 `splitDraftBoxes` 仍强制打开调整框 → 统一 `scheduleSheetPreviewSplitDetect`（上传+生图）；`splitDetectStatus: ready` 且有框时切分直接 `splitStoryboardSheetFromBoxes`；识别状态持久化并合并防伴侣同步冲掉缓存 · `StoryboardTablePanel.tsx` `storyboardSheetPreview.ts` `StoryboardSheetPreviewStrip.tsx`

### 2026-06-19: ToAPIs/VectorEngine 流式与 registryId 计量缺口
网站助手等 `generateContentStream` 无用量记录；`wrapGeminiClientWithChannelMetering` 用上游 `args.model` 记 SKU，与站内 registryId 不一致；`__meteringRegistryId` 若透传上游可能污染请求 → 流式末包 `stream_options.include_usage` + channel wrap 汇总记账；`buildGeminiConfig` 注入 `__meteringRegistryId` 并在 wrap/代理/strip 层剥离；Postgres correlation 查询加 `meta_json IS NOT NULL` · `emitGeminiChannel.ts` `openaiAdapter.ts` `toapisAdapter.ts` `geminiService.ts` `usage-billing-store.js`

### 2026-06-19: 用量有记录但估算价全为「—」/ BYOK
管理端与用户设置页明细有 SKU、计量为 0 in/0 out 或 1 张，估算列「—」或误标 BYOK → `recordUsageEvent.withByokMeta` 只要 `getUserApiKey()` 有值就清空 `costUsdEst` 并打 `byok:true`，即使用户实际走本站 `gemini-async` 代理；token 未回传时计量也显示 0 → 仅 `meta.byok===true` 时清空估算；`gemini-async:` 幂等键不因历史误标显示自备 Key；UI 用价目表算参考价回填展示；计量列标「未回传 token」· `recordUsageEvent.ts` `usageApi.ts` `AdminUsagePanel.tsx` `UsageSettingsPanel.tsx`

### 2026-06-19: 用户设置 AI 用量空白 / 管理端有记录
用户设置页无明细、管理端 `/admin/usage` 有条目 → ① 管理端走 `/api/admin/*`（Vite 已代理），用户页走 `/api/usage/*`、记账 POST `/api/usage/events` **曾未配置 Vite 代理** → 浏览器打 `localhost:3000` 直接 **404**，数据写不进库；补 `vite.config.ts` 的 `/api/usage`、`/api/workflow`。② 管理端可见的可能是单测 `user-proj-filter` 或全用户列表，非本人。③ 批量生图 `async-batch` 曾漏记账（已补 `recordGeminiProxyUsage`）· `vite.config.ts` `geminiService.ts` `UsageSettingsPanel.tsx`

### 2026-06-19: 设置页打开即黑屏
点侧栏「设置」整页黑屏、React 崩溃 → `SettingsSection.tsx` 使用 `lazy()` / `<Suspense>` 懒加载 `UsageSettingsPanel`，但未从 `react` 导入，运行时报 `ReferenceError: lazy is not defined` → 补 `import { Suspense, lazy } from 'react'`；用量面板数据做空值归一化；设置模式下不再挂载隐藏的工作流壳 · `SettingsSection.tsx` `UsageSettingsPanel.tsx` `App.tsx`

### 2026-06-19: 工作台黑屏 / 鉴权初始化卡住
页面全黑或一直「鉴权初始化中」→ `.env.local` 配了 `DATABASE_URL=127.0.0.1:55432` 但 Docker Postgres 未起，auth-api `initAuthStore` 连 PG 失败后 `process.exit(1)`，Vite 代理 `/api/auth/me` 为 ECONNREFUSED；另：`gemini-proxy-api.js` 曾误删 `proxyVertexGenerateContent` 函数头（已修）→ 启动 Docker：`npm run pg:dev:script-hub`；或临时注释 `DATABASE_URL`；已加固 `auth-store` PG 失败回退 `auth-db.json` · `auth-store.js` `gemini-proxy-api.js`

### 2026-06-17: 能力商店 catalog 503 / 预设加载失败
`商店源加载失败：/api/r2/capability-store/catalog` 503 → 本机 auth-api 以裸 `node server/auth-api.js` 启动，未加载 `.env.local` 的 `R2_*`，`isR2Configured()` 为 false；直接 `npm run dev:auth-backend` 又会带上 `DATABASE_URL` 连 55432，Docker 未起则进程退出 → 用 `node scripts/start-auth-r2-json.mjs`（R2 + JSON 库 + 9100）；或先 `npm run pg:dev:script-hub` 再 `dev:auth-backend` · `auth-api.js` `r2-storage-handlers.js` `vite.config.ts`

### 2026-06-05: 管理后台用户详情页 Not found
用户列表点「详情」显示红色 Not found → 9100 上 auth-api 未重启，无 `GET /api/admin/users/:id` 路由（404 落入兜底）；客户端曾先打 path 再打 `?userId=` → kill 旧进程重启 auth-api；`fetchAdminUser` 改优先 `?userId=`；路由校验放宽为单段 id · `auth-api.js` `adminClient.ts`

### 2026-06-05: 管理后台 RBAC 重启丢权限 / 种子 admin 无法降级
自定义 admin 矩阵权限、种子账号降级在 auth-api 重启后恢复 → `seedSystemRoles` 每次 DELETE+重插 permissions；`migrateLegacyStaffUsers` / `assignSeedAdminSuperRole` 无条件提升 super → 系统角色仅首次创建时 seed 权限；种子/迁移仅在 `staff_role_id IS NULL` 时写入；`resolveStaffContext` 要求 `role=admin` · `admin-roles-store.js` `auth-api.js`

### 2026-06-04: Kumiko 实验室「仅 Kumiko」+ 列×行仍只出 1 格
影视分镜无漫画黑框，Kumiko 只能整图 1 格；旧逻辑 `prefer==kumiko` 直接返回不 fallback → 填列×行后仍走 `uniform_grid` fallback；投影加白底分隔带 · `grid_detect.py`

### 2026-06-04: 切分未确认离开分镜表后拼图永久「切分中」
点切分后关调整框/退回工作台，再进分镜表卡片与进度条仍 busy → 面板卸载时 `promptSheetSplitBoxAdjust` Promise 未 resolve，全局 `storyboardSheetSplitSession` 残留 → 挂载清 session + 卸载 resolve 并清 busy；`syncSheetSplitSession` 加 `panelMountedRef` 防卸载后 setState · `StoryboardTablePanel.tsx` `storyboardSheetSplitSession.ts`

### 2026-06-05: 编辑页镜头「清除」无效 / 替换无裁切弹窗
清除后伴侣 hydrate 晚到仍按 rowId 写回 blob；替换走 `assignFrameImagesFromFiles` 跳过裁切 → 清除先乐观清字段 + hydrate 校验 companionKey；替换/拖入/粘贴统一 `beginFrameCropFromFiles` · `storyboardFrameCompanion.ts` `StoryboardTablePanel.tsx`

### 2026-06-05: 改分镜裁切后整站黑屏
非法合并 `openFileForRow` / `applyCroppedFrameToRow` 导致 `StoryboardTablePanel.tsx` 语法错误、主 chunk 加载失败 → 拆回两个函数 · `StoryboardTablePanel.tsx`

### 2026-06-04: 重启后分镜表卡片消失（IDB + autosave 竞态）
刷新后分镜表卡片整块不见 → 首屏空状态 autosave 写回缺 `storyboard_table` 的 bundle；guard 只读 LS/cache 读不到 IDB → `storyboardPersistGuardByKey` + loadComplete 门闩 · `workspaceProjectStore.ts` `App.tsx`

### 2026-06-04: 分镜 OCR `fetch failed`
浏览器 opaque network error 无上下文 → `companionNetworkErrors.ts` 中文映射 + 伴侣 Origin 403 返回 JSON · `companionClient/fetch.ts` `httpHandler.ts`

### 2026-06-04: OCR 探测通过但导入失败
探测用传入 baseUrl，执行仍固定 `getCompanionLocalBaseUrl()` → 全链路统一 `companionBaseUrl` · `companionOcr.ts` `storyboardCompanionOcrImport.ts`

### 2026-06-04: 分镜导入 `invalid_key`
OCR 键含 `/`，伴侣 Volume 要单段 → `companionOcrAssetKey()` 无斜杠 · `companionOcrKeys.ts`

### 2026-06-04: OCR 无文字 / OneDNN 500
Paddle 3.3+ MKLDNN 与 PIR 不兼容；3.x 返回 `rec_texts` 未解析 → `enable_mkldnn=False` + 归一化修复 · `paddleocr-service/server.py` `companionOcr.ts`

### 2026-06-02: 拼图替换角色按钮无效
不认 `imageCompanionKey` → `storyboardNamedAssetHasImageRef` + 伴侣拉图 · `storyboardRoleReplaceRedraw.ts`

### 2026-06-02: 分镜表卡片消失（初版防护）
incoming 缺 storyboard_table 写回 IDB → `mergePreservingStoryboardTableAssets` + 空包拦截 + sanitize 保留单条 · `workspaceProjectStore.ts` `App.tsx`（见上条 IDB guard 为后续加固）

### 2026-06: 分镜切分 / 预览 / 生图（高频）
| 现象 | 要点 |
|------|------|
| 切分后解析丢图 | 按镜号/顺序合并，保留 `frameImage*` · `storyboardTableBulkImport.ts` |
| 关面板进度/占位消失 | `storyboardSheetSplitSession` / `storyboardSheetGenSession` |
| 缩略图裂图 | 切分 busy 仍 rehydrate；失效 blob 重拉伴侣 · `storyboardSheetPreview.ts` |
| 批量预览中途消失 | 伴侣/IDB 三级存储，LS 只存元数据 · `storyboardSheetPreview.ts` |
| 切分误填全表 | 视觉切分只传本拼图 `taskRows` · `storyboardSheetVisionSplit.ts` |
| 镜号 01 vs 001 | 统一三位 + `storyboardShotNosMatch` · `storyboardTableAsset.ts` |
| 百镜卡顿 | 虚拟列表 + 时间轴 LOD · `useStoryboardVirtualList.ts` |

### 2026-06-03: 登录后黑屏 TDZ
`applyStoryboardRestoresFromSave` 早于 `addGlobalLog` → 调整声明顺序 · `App.tsx`

### 2026-06-05: 分镜 UI / 视频（摘要）
行编辑器 JSX 不匹配黑屏；拼图可变行高；批量生图 Panel 内直连执行；视频播放/时间轴用 `performance.now()` 与全局时长比例；行内输入 focus 被 `onClose` effect 抢走 · 见 `StoryboardTablePanel.tsx` `storyboardVideoTimeline.ts`

### 2026-05: 工作区持久化 / hydrate
| 现象 | 要点 |
|------|------|
| 刷新后资产空/被空包覆盖 | IDB hydrate 后 ref 未同步；autosave 竞态 → ref 同步 + load 门闩 · `App.tsx` |
| 分镜/原图裂图 | 失效 blob 未 hydrate → `companionRasterSlotNeedsHydrate` + strip blob · `workflowCompanionAssets.ts` |
| invalid_key | 伴侣键含 `/` → 单段 `wf-orig-*` · `workflowCompanionAssets.ts` |
| 相对路径落盘失败 | 未走 `resolveCapabilityPreviewSrc` → 规范化前解析 · `workflowCompanionAssets.ts` |
| VGP 步骤树闪烁 | hydrate effect 依赖不稳定 `onLog` → `onLogRef` · `WorkflowSection.tsx` |

### 2026-06-05: Render auth-api「Port scan timeout / No open ports」
- 现象：deploy 失败或全员无法登录；`/healthz` 长期 `ready:false`
- 原因：① `listen` 前 await init → 端口扫描超时；② 曾用 503 `Service starting` 挡全部 API；③ **`migrateSidebarPermissionKeys` → `listAllRoles` → `ensureAdminRbac` 死锁**
- 修复：先 `listen`；API `await startStoreInit()`；迁移内直接查 `admin_roles` 表 · `server/auth-api.js` `admin-roles-store.js`

### 2026-06-05: 管理端「任务执行」查不到记录
- 现象：用户跑工作流队列后，后台任务执行页为空
- 原因：① 跨域 SPA 上报 `POST /api/workflow/task-events` 未 CSRF 豁免 → 403 且前端静默失败 ② Postgres 按用户名筛选时 COUNT 未 JOIN `users` → 带用户筛选查询报错 ③ 服务端未限制 `RUN_TASK_*` 代码
- 修复：auth-api CSRF 豁免 + `requestJson` 上报；COUNT/LIMIT 与 JOIN 对齐；入库仅接受 `RUN_TASK_*`；用户名/ID 双筛 · `auth-api.js` `workflowTaskEventsSync.ts` `workflow-task-events-store.js`

### 2026-06-05: 角色矩阵「用户管理关不掉 / 侧栏项不全」
- 现象：矩阵把用户管理设为 — 保存后仍显示只读；任务执行/系统状态无独立列
- 原因：`matrixToPermissions` 在开启「用量同步/伴侣删除」等时隐式补回 `users.read`/`companion.read`；侧栏新页共用 `audit.read`/`dashboard.read`
- 修复：去掉 reconcile/delete 的隐式补权；新增 `task_events.read`/`system_status.read` 列与迁移；保存后刷新当前用户 `reload()` · `admin-matrix.js` `adminPermissions.ts` `AdminRolesMatrixPanel.tsx`

### 2026-05: 大图 / SAM / 全景
SAM stub 只有圆点 → 生产 `SAM_MODE=sam`；mask 当主图变灰底 → 原图垫底叠 mask；全景贴回用 `panoEquirectThreeMapping.ts`；pointermove 触发整树重绘 → rAF 节流 · 归档 §全景/分割

### 2026-05: Maya / Script Hub
Command Port 探测/执行后 ECONNREFUSED → 成功路径 `socket.end()` 勿 `destroy()`；idle 后等 `close`；MEL 安装改短 python 复制脚本 · `mayaScriptAdapter.ts`

### 2026-05: 生图 / API
Failed to fetch → 查 9002/CORS/同源转发；Vertex OAuth → proxy `vertexai: false`；GPT Image 超时/4K 尺寸 → 600s + 长边 3840 · `geminiService.ts` `openaiAdapter.ts`

### 2026-05: 3D / Tripo
先 upload 拿 file_token；413 → 压缩 JPEG；Vercel fetch-file 404 → `resolveTripoProxyBase`；步骤刷新丢失 → 合并 resultMeta · `tripoService.ts`

### 2026-05: 桌面壳 / 伴侣
spawn node ENOENT → bundle CJS + `ELECTRON_RUN_AS_NODE`；配对 token 401 → 读 pairing-config；18765 EADDRINUSE → shutdown 先 close · `companion-desktop/main.cjs`

### 2026-04: 常见（索引）
Antigravity 404/502/JSON 解析；对话会话 NaN 过滤；批处理 pending 无取消钮；全景 OOM cap 4096；Render auth-db suspended；CSRF 新路由白名单 · **详述 → 归档 §2026-04**

---

## 域索引 → 归档

| 域 | 关键词 | 归档 |
|----|--------|------|
| 分镜表 | 切分、预览、OCR、虚拟列表、视频 | §2026-06 详述 |
| 工作区 | IDB、autosave、hydrate、blob | §2026-05 持久化 |
| 伴侣 | 18765、pairing、落盘键 | §2026-05 桌面壳 |
| 生图 | Vertex、OpenAI、Antigravity、CORS | §2026-05/04 生图 |
| Maya | Command Port、Script Hub | §2026-05 Maya |
| 3D | Tripo、GLB、blob 下载 | §2026-05 Tripo |

---

## 写作约定

- **何时记**：可复现 bug、数据丢失、环境踩坑。
- **一行式**：`现象 → 原因 → 修复 · 文件`
- **升格**：重复错误写入 `.cursor/rules`，此处加「已升格：见 xxx.mdc」。
- **勿记**：密钥、一次性环境噪音（除非有固定规避）。
