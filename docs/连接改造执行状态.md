# 连接改造执行状态

更新时间：2026-08-10 12:07:00
当前阶段：P7
当前任务：P7-005
整体状态：P1 complete / P2-P5 foundation complete / P7 in_progress

## 已完成任务

- P0-001：创建执行状态文件。
- P0-002：工具页默认隐藏旧宿主中心主体，工具架成为默认内容。
- P0-003：左侧导航新增“连接”一级入口。
- P0-004：连接页提供空态和“对话添加连接”入口，入口绑定 CapabilityPackage/software_connection 上下文。
- P0-005：新增 CapabilityPackage 最小 schema 和校验。
- P0-006：新增工具 manifest 到 tool CapabilityPackage 的最小 adapter。
- P0-007：新增软件连接草稿到 software_connection CapabilityPackage 的最小 adapter。
- P0-008：完成 P0 自动验收。
- P1-001：新增能力包草稿本地接口和 Copilot 工具，支持通过对话创建 software_connection 草稿。
- P1-002：连接页读取并显示 software_connection 能力包草稿卡片，不读取旧 62 宿主 catalog。
- P1-003：Photoshop 能力包生命周期接入真实 Adobe ExtendScript 安装、探测和卸载样板。
- P1-004：新增能力包对象上下文接口和 Copilot 工具，连接对象对话可读取 manifest、安装记录、probe 状态和最近事件。
- P1-005：工具创建进入 `CapabilityPackage(type=tool)` 主线，`ac.shell_tool.scaffold` 成功后同步写入工具能力包草稿。
- P1-006：完成 P1 Loop 验收，软件连接和工具均能进入对象级 loop。
- P2-001：新增统一生命周期调度器 `runCapabilityLifecycle(packageId, action, payload)` 和 Copilot 工具 `ac.capability.lifecycle_run`。
- P2-002：新增能力包发布门禁 `ac.capability.publish_gate_check`，明确管理员、版本说明、真实 probe 和本地草稿/云端隔离规则。
- P5-001：新增能力包云端版本存储与激活接口，`publish` 生命周期可创建云端版本，`switch_version` 只激活云端已有版本。
- P5-002：连接页和工具页接入能力包云端版本，管理员可提交云端和选择已有版本，普通用户不显示治理动作。
- P1-007：自建/导入/编辑工具会自动创建或更新 `CapabilityPackage(type=tool)` 草稿，旧工具列表读取时可回填能力包草稿。
- P7-001：新增统一 Copilot 创建入口 `ac.capability.create_draft`，自然语言判断 tool/software_connection；工具创建优先脚手架为本机工具能力包，不再误入 Workbench 文本资产。
- P7-002：新增统一 Copilot 校验和发布入口 `ac.capability.validate_draft`、`ac.capability.publish_cloud`，复用统一生命周期和发布门禁。
- P7-003：更新 Copilot/MCP/Workbench 路由提示，工具/插件创建优先走能力包入口，`create_text_asset` 仅用于普通文本资产。
- P7-004：补充统一创建入口执行端验收，验证自然语言工具创建走 authored tool 脚手架、自然语言软件连接创建走能力包草稿、统一校验/发布走 lifecycle endpoint。
- P7-005：统一创建软件连接后也返回 capability context；补充工具/连接对象上下文隔离验收，验证 session 和 recentEvents 不串线。

## 当前阻塞

- 阻塞任务：无
- 阻塞原因：无
- 需要人工操作：否
- 最近三次尝试：无

## 最近验证

- 命令：`node --check companion-desktop/shell/tools-bridges.js`
- 结果：通过
- 命令：`node --check companion-desktop/shell/tools-page.js`
- 结果：通过
- 命令：`node --check companion-desktop/shell/connection-page.js`
- 结果：通过
- 命令：`node --check companion-desktop/agent-tool-schemas.cjs`
- 结果：通过
- 命令：`npm run local-companion:typecheck`
- 结果：通过
- 命令：`npx vitest run tests/agentToolSchema.test.ts tests/capabilityPackages.test.ts tests/shellHostCenterUi.test.ts tests/shellConnectionPageUi.test.ts`
- 结果：通过，4 个测试文件、20 条测试通过
- 命令：`npx vitest run tests/capabilityPackages.test.ts tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/shellConnectionPageUi.test.ts`
- 结果：通过，4 个测试文件、40 条测试通过
- 命令：`node --check companion-desktop/agent-tool-schemas.cjs`
- 结果：通过
- 命令：`node --check companion-desktop/agent-body-host.cjs`
- 结果：通过
- 命令：`node --check companion-desktop/agent-body-mcp.cjs`
- 结果：通过
- 命令：`node --check companion-desktop/agent-workbench-client.cjs`
- 结果：通过
- 命令：`npm run local-companion:typecheck`
- 结果：通过
- 命令：`npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts`
- 结果：通过，6 个测试文件、66 条测试通过
- 命令：`npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts`
- 结果：通过，7 个测试文件、76 条测试通过
- 命令：`npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts`
- 结果：通过，8 个测试文件、81 条测试通过

## 完成证据

- 新增“连接”一级页面，但默认不展示旧 62 宿主列表。
- 工具页默认进入“工具架”，旧“桥接管理”入口和主体被隐藏，legacy 代码仍保留为参考和恢复池。
- 连接页空态从用户意图开始，点击“对话添加连接”会打开对象对话，并明确要求创建 `CapabilityPackage(type = software_connection)`。
- `CapabilityPackage` 可表达 `software_connection | tool | workflow`，并具备来源、生命周期、对象对话和治理门禁字段。
- 工具和软件连接均已有最小 adapter 测试，证明它们可以进入统一能力包主线。
- Copilot 工具清单新增 `ac.capability.draft_create` 和 `ac.capability.draft_list`。
- `POST /v1/capability-packages/drafts` 会把连接对话创建为本地 `software_connection` 能力包草稿。
- `GET /v1/capability-packages/drafts` 可读取本地能力包草稿列表，为连接页显示草稿卡片做准备。
- 连接页有能力包卡片、对象对话、安装、探测、卸载、删除草稿入口。
- `POST /v1/capability-packages/:id/install` 对 Photoshop 样板调用真实 Adobe ExtendScript 安装器。
- `POST /v1/capability-packages/:id/probe` 对 Photoshop 样板读取真实 heartbeat；未产生 heartbeat 时失败，不把安装成功当连接成功。
- Copilot 工具清单新增 `ac.capability.install`、`ac.capability.probe`、`ac.capability.uninstall`，对象对话可继续生命周期动作。
- `GET /v1/capability-packages/:id/context` 返回单个能力包对象上下文，包含 session、contextPrompt、package 和 recentEvents。
- Copilot 工具清单新增 `ac.capability.context_get`，用于对象对话刷新当前能力包状态。
- 连接页打开对象对话前会实时读取能力包 context，不再只使用卡片上的静态对象。
- `ac.shell_tool.scaffold` 成功后同步创建 `tool` 能力包草稿，避免工具创建只落在工具目录或误入 Workbench 文本资产。
- Photoshop loop 验收：第一次 probe 在未产生 heartbeat 时失败，失败状态写入该连接对象；写入由桥接脚本产生的 heartbeat 后，再次 probe 成功。
- 工具 loop 验收：随机选择工具能力包可记录 `run_failed` 和 `retest_passed` 事件，事件只进入该工具对象上下文。
- Copilot 工具清单新增 `ac.capability.event_append`，用于把运行失败、修复和复测结果写入指定能力包对象。
- `runCapabilityLifecycle()` 支持 `validate/install/run/probe/uninstall/open_conversation`，并对 `publish/switch_version` 返回未启用状态，避免本地草稿自动污染云端版本。
- `POST /v1/capability-packages/:id/lifecycle` 提供统一生命周期 HTTP 入口。
- `ac.capability.lifecycle_run` 成为 Copilot 的统一生命周期入口；工具 `run` 失败会写入该工具对象事件，软件连接 `probe` 仍走真实 heartbeat。
- 能力包发布门禁和云端版本存储已接入生命周期；软件连接发布要求真实 probe，普通用户不能发布或切换云端版本。
- 连接页和工具页都能显示能力包云端标识/版本，并通过能力包云端 API 提交和切换已有版本。
- `ac.capability.create_draft` 成为对话创建能力的统一入口：工具请求会先创建本机 authored tool 并同步为 tool 能力包；连接请求会创建 software_connection 能力包草稿。
- `ac.capability.validate_draft` 通过统一生命周期执行 validate；`ac.capability.publish_cloud` 通过统一生命周期执行 publish，不绕过发布门禁。
- Workbench 文本资产误路由拦截、Codex brain prompt 和 MCP quickstart 均推荐 `ac.capability.create_draft`，降低“工具被创建成文本资产”的概率。
- `tests/agentP2L2.test.ts` 已验证“随机选择工具”类自然语言请求不会调用 Workbench 文本资产路径，而是调用 `/v1/shell-tools/authored/scaffold` 并打开本机工具。
- `tests/agentP2L2.test.ts` 已验证“添加 Spine 连接”类自然语言请求会创建 `/v1/capability-packages/drafts` 的 `software_connection` 草稿，不携带 `templateId`。
- `tests/agentP2L2.test.ts` 已验证 `ac.capability.validate_draft` 与 `ac.capability.publish_cloud` 均路由到 `/v1/capability-packages/:id/lifecycle`，其中发布 action 为 `publish`。
- `ac.capability.create_draft` 创建 software_connection 后会尝试读取 `/v1/capability-packages/:id/context`，让连接创建后可以立即进入对象对话。
- `tests/capabilityPackages.test.ts` 已验证 `random-selector` 工具和 `spine` 连接拥有不同 session：`capability:tool:random-selector` 与 `capability:software_connection:spine`。
- `tests/capabilityPackages.test.ts` 已验证工具事件只进入工具 context，连接事件只进入连接 context，`recentEvents` 和 `contextPrompt` 不互相污染。

## 下一任务

- P8-001：继续清理旧分裂入口，把新增创建/验证/发布说明统一指向能力包，保留 legacy host bridge 作为参考和回收池。
- P8-002：抽出更通用的能力包卡片/动作 schema，减少工具页和连接页各自维护提交、版本、对象对话按钮逻辑。

## 重要约束

- 不恢复旧 62 宿主默认列表。
- 不用 mock probe 冒充真实连接。
- 不把工具创建成 Workbench 文本资产。
- 不自动提交本地草稿到云端。

## P8-001 / P8-002 更新记录

更新时间：2026-08-10 12:13:00

完成内容：

- 新增 `companion-desktop/shell/capability-card-schema.js`，把能力包卡片的标签、云端版本显示、发布按钮、版本切换按钮等判断集中到一套共享 schema。
- `连接` 页面改为使用共享 schema 渲染软件连接卡片标签和动作，避免连接页继续维护独立的提交/版本/对话按钮规则。
- `工具` 页面改为使用共享 schema 判断云端提交和版本切换入口，保留原有按钮样式和交互。
- `index.html` 已在工具页和连接页之前加载共享 schema，确保两个页面使用同一套能力包 UI 事实。
- 新增 `tests/shellCapabilityCardSchema.test.ts`，验证 software_connection 与 tool 的共享标签/动作规则。
- 更新 `tests/shellConnectionPageUi.test.ts` 和 `tests/shellToolCloudPublishUi.test.ts`，验收目标从页面内联条件调整为共享 schema 规则。

验证结果：

- `npm run local-companion:typecheck` 通过。
- `node --check companion-desktop/shell/capability-card-schema.js` 通过。
- `node --check companion-desktop/shell/connection-page.js` 通过。
- `node --check companion-desktop/shell/tools-page.js` 通过。
- `npx vitest run tests/shellCapabilityCardSchema.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts` 通过，3 个测试文件，7 条测试。
- `npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts` 通过，10 个测试文件，84 条测试。

下一步：

- 继续 P8-001，清理旧分裂入口和说明文案，让创建、验证、发布入口统一指向能力包。
- 继续 P8-002，把更多工具页/连接页重复的卡片字段、空态和对象对话入口收敛到共享模型。

## P8-003 更新记录

更新时间：2026-08-10 12:18:00

完成内容：

- `ac.companion.host_bridge.*` 工具目录已统一标记为 legacy recovery/debug，并设置 `deprecated: true`。
- 旧 host bridge 创建、校验、安装、探测、卸载、启动、关闭、运行中探测等入口继续保留为回收池和调试入口，但不再作为普通用户对话创建软件连接的默认推荐路径。
- Codex brain prompt 增加软件连接创建规则：添加 Photoshop、Maya、Blender、Spine、Unity、Unreal 等软件连接时，优先调用 `ac.capability.create_draft`，并使用 `type=software_connection`。
- Body MCP server instructions 和 quickstart 增加同样规则，明确 `ac.companion.host_bridge.*` 仅是 legacy recovery/debug。
- 更新 `tests/agentToolSchema.test.ts`，固定所有旧 host bridge 工具必须 deprecated 且带 legacy 说明。
- 更新 `tests/codexBrainAdapter.test.ts`，固定 Codex prompt 必须引导软件连接创建走能力包主线。

验证结果：

- `node --check companion-desktop/agent-tool-schemas.cjs` 通过。
- `node --check companion-desktop/brain-adapters/codex.cjs` 通过。
- `node --check companion-desktop/agent-body-mcp.cjs` 通过。
- `npx vitest run tests/agentToolSchema.test.ts tests/codexBrainAdapter.test.ts tests/agentP2L2.test.ts tests/agentP2.test.ts` 通过，4 个测试文件，43 条测试。

下一步：

- 继续 P8-001，检查 UI 文案和 Copilot 面板上下文中是否还有默认指向旧 Host Center / host bridge 创建的入口。
- 继续 P8-002，把工具页和连接页更多重复状态字段收敛为共享能力包视图模型。

## P8-004 更新记录

更新时间：2026-08-10 12:25:00

完成内容：

- `companion-desktop/shell/capability-card-schema.js` 新增 `view(pkg, opts)`，统一产出能力包卡片的 `title`、`subtitle`、`description`、`status`、`tags` 和 `actions`。
- 连接页卡片改为消费 `schema.view(pkg, ...)`，标题、描述、状态、标签和动作不再各自散落在页面内部判断。
- 工具页卡片改为消费 `schema.view(packageLike, ...)`，工具标题、版本副标题、描述、标签和动作开始收敛到同一套能力包视图模型。
- 工具页云端提交动作从固定 `isAdmin: true` 修正为真实 `this.isAdmin`，避免普通用户误显示/误触发管理员治理入口。
- `tests/shellCapabilityCardSchema.test.ts` 增加共享 view 验收，覆盖 software_connection 和 tool。
- `tests/shellConnectionPageUi.test.ts`、`tests/shellToolCloudPublishUi.test.ts` 从页面直接 `tags/actions` 断言更新为 `schema.view(...)` 断言。

验证结果：

- `npm run local-companion:typecheck` 通过。
- `node --check companion-desktop/shell/capability-card-schema.js` 通过。
- `node --check companion-desktop/shell/connection-page.js` 通过。
- `node --check companion-desktop/shell/tools-page.js` 通过。
- `npx vitest run tests/shellCapabilityCardSchema.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts` 通过，3 个测试文件，7 条测试。
- `npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts` 通过，10 个测试文件，84 条测试。

下一步：

- 继续 P8-002，将工具页/连接页剩余重复的空态、对象对话入口和云端版本 picker 行为进一步收敛。
- 继续审计普通用户、管理员、云端版本、旧 legacy 回收池四类入口是否全部符合能力包主线。

## P8-005 修复记录

更新时间：2026-08-10 12:27:00

问题：

- 用户点击左侧“连接”后实际显示工作台页面。

原因：

- 渲染进程和 HTML 已经注册 `connections`，但主进程 `normalizeShellViewName()` 仍未把 `connections` 加入视图白名单，导致 `shell.setShellView('connections')` 被归一化回 `workbench`。

修复：

- `companion-desktop/main.cjs` 的主进程 shell view 类型和 `normalizeShellViewName()` 已加入 `connections`。
- `tests/shellConnectionPageUi.test.ts` 增加主进程白名单断言，防止连接页只在前端注册、主进程却回退到工作台。

验证结果：

- `node --check companion-desktop/main.cjs` 通过。
- `npx vitest run tests/shellConnectionPageUi.test.ts tests/agentToolSchema.test.ts` 通过，2 个测试文件，11 条测试。
- 已执行 `npm run restart:local-companion`。

## P8-006 更新记录

更新时间：2026-08-10 12:31:00

完成内容：

- `companion-desktop/shell/capability-card-schema.js` 新增 `versionOptions(pkg)`，统一云端版本列表的 id、semver、说明、日期、当前版本标记和展示 label。
- 连接页 `chooseCloudVersion(pkg)` 改为通过 `schema.versionOptions(pkg)` 生成 prompt 列表，不再自己拼接版本行。
- 工具页 `showVersionPicker(entry, versions)` 改为通过 `schema.versionOptions({ ...entry, type: 'tool', cloudVersions: versions })` 生成弹窗列表项，同时保留原始 bundle 给后续激活/安装逻辑。
- 共享版本模型支持用 `active`、`semverLocal`、`cloudVersion` 或 `version` 判断当前版本，避免工具页和连接页当前版本标记规则继续分叉。
- `tests/shellCapabilityCardSchema.test.ts` 增加 `versionOptions` 验收，覆盖连接和工具两类能力包。
- `tests/shellConnectionPageUi.test.ts`、`tests/shellToolCloudPublishUi.test.ts` 增加页面消费 `schema.versionOptions(...)` 的断言。

验证结果：

- `npm run local-companion:typecheck` 通过。
- `node --check companion-desktop/main.cjs` 通过。
- `node --check companion-desktop/shell/capability-card-schema.js` 通过。
- `node --check companion-desktop/shell/connection-page.js` 通过。
- `node --check companion-desktop/shell/tools-page.js` 通过。
- `npx vitest run tests/shellCapabilityCardSchema.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts` 通过，3 个测试文件，7 条测试。
- `npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts` 通过，10 个测试文件，84 条测试。

下一步：

- 继续收敛版本选择 UI 本身：连接页仍是 prompt，工具页是自定义弹窗；后续应统一为同一套能力包版本 picker，避免交互体验分叉。
- 继续审计连接页启动默认视图、对象对话入口和云端治理门禁。

## P8-007 更新记录

更新时间：2026-08-10 12:36:00

完成内容：

- 新增 `companion-desktop/shell/capability-version-picker.js`，提供共享能力包云端版本选择弹窗 `ShellCapabilityVersionPicker.pick(entry, versions, opts)`。
- `index.html` 已在能力包 schema 后、工具页和连接页前加载共享 version picker。
- 连接页 `chooseCloudVersion(pkg)` 已从 `window.prompt` 改为共享版本弹窗，和工具页保持一致体验。
- 工具页 `showVersionPicker(entry, versions)` 优先委托共享 `ShellCapabilityVersionPicker.pick(...)`，旧局部 picker 暂时保留为兜底。
- 更新 `tests/shellConnectionPageUi.test.ts`，确认连接页不再使用选择版本 prompt，且消费共享 picker。
- 更新 `tests/shellToolCloudPublishUi.test.ts`，确认工具页也优先使用共享 picker。

验证结果：

- `npm run local-companion:typecheck` 通过。
- `node --check companion-desktop/main.cjs` 通过。
- `node --check companion-desktop/shell/capability-card-schema.js` 通过。
- `node --check companion-desktop/shell/capability-version-picker.js` 通过。
- `node --check companion-desktop/shell/connection-page.js` 通过。
- `node --check companion-desktop/shell/tools-page.js` 通过。
- `npx vitest run tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts` 通过，3 个测试文件，7 条测试。
- `npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts` 通过，10 个测试文件，84 条测试。

下一步：

- 删除或进一步瘦身工具页旧局部版本 picker 兜底，减少重复 UI 代码。
- 继续审计对象对话入口和云端治理门禁，确保工具和连接完全围绕能力包对象运行。

## P8-008 更新记录

更新时间：2026-08-10 12:40:00

完成内容：

- 删除 `companion-desktop/shell/tools-page.js` 内旧的 `ensureVersionPickerStyles()` 和 `.tools-version-*` 弹窗样式。
- 删除工具页 `showVersionPicker()` 内旧的本地 DOM 版本选择兜底。
- 工具页版本选择现在只委托 `ShellCapabilityVersionPicker.pick(...)`；如果共享 picker 未加载，会提示重启或更新本地伴侣。
- `tests/shellToolCloudPublishUi.test.ts` 更新为确认工具页不再包含旧 `tools-version-*` 实现，并确认共享 picker 拥有 `capability-version-dialog`。

验证结果：

- `npm run local-companion:typecheck` 通过。
- `node --check companion-desktop/main.cjs` 通过。
- `node --check companion-desktop/shell/capability-card-schema.js` 通过。
- `node --check companion-desktop/shell/capability-version-picker.js` 通过。
- `node --check companion-desktop/shell/connection-page.js` 通过。
- `node --check companion-desktop/shell/tools-page.js` 通过。
- `npx vitest run tests/shellToolCloudPublishUi.test.ts tests/shellConnectionPageUi.test.ts tests/shellCapabilityCardSchema.test.ts` 通过，3 个测试文件，7 条测试。
- `npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts` 通过，10 个测试文件，84 条测试。

下一步：

- 继续审计对象对话入口：工具和连接打开后是否都进入 `CapabilityPackage` 对象上下文，而不是旧全局会话或页面私有上下文。
- 继续审计云端治理门禁：普通用户不可提交/切换云端版本，管理员动作必须使用统一门禁。
## P8-009 更新记录

更新时间：2026-08-10 12:45:18

完成内容：
- 工具页打开工具窗口后，改为优先读取 `/v1/capability-packages/:id/context`，再打开 `type=capability` 的对象对话。
- 工具页旧的 `toolCopilotContext` 私有上下文已收敛为 `fallbackToolCapabilityContext`，兜底文案也围绕工具能力包对象，而不是页面私有工具上下文。
- 连接页打开对象对话时优先使用 `session.sessionId`，工具页和连接页都更靠近 `CapabilityPackage` 自身的稳定会话语义。
- 清理工具页版本选择中的重复 `if (!bundle)` 分支，减少 P8 清理阶段的冗余代码。
- 更新测试断言，防止工具页退回 `type=tool` / `toolCopilotContext` 的旧对象对话入口。

验证结果：
- `node --check companion-desktop/shell/tools-page.js` 通过。
- `node --check companion-desktop/shell/connection-page.js` 通过。
- `npx vitest run tests/shellToolCloudPublishUi.test.ts tests/shellConnectionPageUi.test.ts tests/copilotSettingsUi.test.ts` 通过：3 个测试文件，26 条测试。
- `npm run local-companion:typecheck` 通过。
- `node --check companion-desktop/main.cjs`、`capability-card-schema.js`、`capability-version-picker.js`、`connection-page.js`、`tools-page.js` 通过。
- `npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/copilotSettingsUi.test.ts` 通过：11 个测试文件，105 条测试。

下一步：
- 继续审计工具页、连接页、Copilot 面板和 main 进程中残留的旧对象会话入口，逐步把普通用户主线统一到 `CapabilityPackage` 生命周期和对象上下文。
- 继续检查 legacy host bridge 是否只作为 recovery/debug/reference 暴露，避免重新成为连接创建主入口。
## P8-010 更新记录

更新时间：2026-08-10 12:51:10

完成内容：
- Copilot 对象会话入口支持显式 `sessionId`，不再只能把 `type/id` 重新拼接成页面私有会话。
- 连接页和工具页打开能力包对象对话时传入 `session.sessionId`，让 UI 会话与 `/v1/capability-packages/:id/context` 返回的能力包会话保持一致。
- 工具运行失败自动修复链路从旧 `type=tool` / `tool-${toolId}` 改为读取能力包 context，并写入 `capability:tool:${toolId}` 会话。
- 自动修复直接发送给 Agent 的消息会附带能力包 `contextPrompt`，避免绕过 Copilot 面板时丢失对象上下文。

验证结果：
- `node --check companion-desktop/main.cjs` 通过。
- `node --check companion-desktop/shell/copilot-panel.js`、`tools-page.js`、`connection-page.js` 通过。
- `npx vitest run tests/copilotSettingsUi.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts` 通过：3 个测试文件，26 条测试。
- `npm run local-companion:typecheck` 通过。
- `node --check companion-desktop/main.cjs`、`copilot-panel.js`、`capability-card-schema.js`、`capability-version-picker.js`、`connection-page.js`、`tools-page.js` 通过。
- `npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/copilotSettingsUi.test.ts` 通过：11 个测试文件，105 条测试。

下一步：
- 继续清理主线 UI 中 legacy bridge 的隐藏加载和文案，确保普通用户不会把旧 Host Center 当成连接创建入口。
- 继续审计 Workbench 文本资产工具与能力包创建工具的边界，降低工具被误建为文本资产的概率。
## P8-011 更新记录

更新时间：2026-08-10 12:56:00

完成内容：
- 旧桥接管理 `tools-bridges.js` 不再随本地壳首页默认加载。
- 工具页不再默认 `bind` legacy `ShellToolsBridges`，避免旧 62 宿主中心在普通工具主线初始化。
- 保留 legacy bridge 文件与隐藏调试入口；只有显式触发隐藏调试入口时，才动态加载 `tools-bridges.js` 并绑定调试模块。
- 更新 `shellHostCenterUi` 测试，固定“legacy 代码仍可作为恢复池存在，但主线 HTML 不预加载、工具页不默认绑定”的边界。

验证结果：
- `node --check companion-desktop/shell/tools-page.js` 通过。
- `npx vitest run tests/shellHostCenterUi.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts` 通过：3 个测试文件，7 条测试。
- `npm run local-companion:typecheck` 通过。
- `node --check companion-desktop/main.cjs`、`copilot-panel.js`、`capability-card-schema.js`、`capability-version-picker.js`、`connection-page.js`、`tools-page.js`、`tools-bridges.js` 通过。
- `npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/copilotSettingsUi.test.ts tests/shellHostCenterUi.test.ts` 通过：12 个测试文件，107 条测试。

下一步：
- 继续审计 Workbench 文本资产创建与能力包创建之间的边界，确保“创建工具/连接”请求更稳定地进入能力包主线。
- 继续清理旧桥接相关文案，让普通用户只看到“连接”与能力包对象，而不是旧 Host Center 术语。
## P8-012 更新记录

更新时间：2026-08-10 13:04:30

完成内容：
- 强化 `ac.workbench.create_text_asset` 执行端误路由防护，新增真实 UTF-8 中文关键词识别。
- 当 Copilot 误把“创建插件 / 创建工具 / 创建 Maya 随机选择插件”等请求送到 Workbench 文本资产接口时，会在执行端拒绝，并返回 `ac.capability.create_draft -> ac.shell_tool.authored_upsert` 的正确下一步。
- 保留普通文本资产创建能力；只拦截工具、插件、脚本工具、可安装 utility 等创建意图。
- 新增真实中文回归测试，避免只覆盖历史乱码样本。

验证结果：
- `node --check companion-desktop/agent-workbench-client.cjs` 通过。
- `npx vitest run tests/agentWorkbenchClient.test.ts tests/agentToolSchema.test.ts tests/agentP2L2.test.ts` 通过：3 个测试文件，40 条测试。
- `npm run local-companion:typecheck` 通过。
- `node --check companion-desktop/agent-workbench-client.cjs`、`agent-body-host.cjs`、`agent-tool-schemas.cjs`、`brain-adapters/codex.cjs` 通过。
- `npx vitest run tests/agentToolSchema.test.ts tests/agentWorkbenchClient.test.ts tests/codexBrainAdapter.test.ts tests/agentP2.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts tests/shellToolAuthored.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/copilotSettingsUi.test.ts tests/shellHostCenterUi.test.ts` 通过：12 个测试文件，108 条测试。

下一步：
- 继续把“创建连接/工具”的执行端结果和连接页 UI 做更直接的刷新/提示联动。
- 继续补真实宿主连接样本，把测试草稿逐步替换为可真实 probe 的软件连接。
# P8-013：Copilot 创建能力后刷新对应页面

## 完成内容

- Copilot 面板会识别 `ac.capability.create_draft` / `ac.capability.draft_create` 的成功结果。
- 成功创建 `software_connection` 或 `tool` 后广播 `assetcutter:capability-created` 事件。
- 连接页收到 `software_connection` 创建事件后自动刷新连接卡片。
- 工具页收到 `tool` 创建事件后自动刷新工具架。
- 已保留基础测试连接草稿 `basic-connection-test`，用于验证连接一级页面与对象对话；该草稿 `testOnly=true`，不会被当作真实连通成功。

## 验收

- `node --check companion-desktop/shell/copilot-panel.js`
- `node --check companion-desktop/shell/connection-page.js`
- `node --check companion-desktop/shell/tools-page.js`
- `node --check companion-desktop/main.cjs`
- `npx vitest run tests/copilotSettingsUi.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts`

# P8-014：workflow 能力包草稿底座

## 完成内容

- 新增 `workflowPackageAdapter`，让 `workflow` 不只停留在 schema 枚举，而是能生成合法 `CapabilityPackage(type=workflow)`。
- `POST /v1/capability-packages/drafts` 现在可持久化 workflow 草稿，拥有独立 `capability:workflow:<id>` 对象会话。
- 能力包 context 对 workflow 增加“运行器未接入”的边界说明，并避免带入软件连接专属 heartbeat 门禁。
- `ac.capability.draft_create` / `ac.capability.create_draft` 的 schema 支持 `workflow`。
- Copilot 统一创建入口会把“工具流/工作流/流程/自动化/workflow/flow/pipeline/automation”意图归入 workflow，不再误建为连接或工具。

## 验收

- `npm run local-companion:typecheck`
- `node --check companion-desktop/agent-body-host.cjs`
- `node --check companion-desktop/agent-tool-schemas.cjs`
- `npx vitest run tests/capabilityPackages.test.ts tests/agentToolSchema.test.ts tests/agentP2L2.test.ts`

# P8-015：软件连接启动/关闭进入能力包生命周期

## 完成内容

- `runCapabilityLifecycle()` 新增 `launch`、`close`、`discover_running` 动作。
- 软件连接能力包可通过 `ac.capability.lifecycle_run` 调用安全宿主进程控制，不再只能走 legacy `ac.companion.host_bridge.launch_host/close_host/discover_running_host`。
- 启动动作仍复用 `hostAppProcess` 的白名单和 exe 名称校验，不允许任意路径任意命令启动。
- 启动/关闭/识别结果会写入该能力包对象事件，例如 `launch_failed`，方便对象对话继续修复。
- 软件连接 context 增加 process lifecycle 提示，说明可用 `launch/close/discover_running`，同时保留真实 probe 门禁。

## 验收

- `npm run local-companion:typecheck`
- `node --check companion-desktop/agent-body-host.cjs`
- `node --check companion-desktop/agent-tool-schemas.cjs`
- `npx vitest run tests/capabilityPackages.test.ts tests/agentToolSchema.test.ts tests/agentP2L2.test.ts tests/hostAppProcess.test.ts`

# P8-016：连接页暴露能力包进程生命周期动作与基础测试连接

## 完成内容

- 连接能力包卡片动作增加 `discover_running`、`launch`、`close`，与 `install/probe/uninstall` 一起作为软件连接的主线生命周期入口。
- 连接页新增“识别运行中 / 启动 / 关闭”按钮，统一调用 `/v1/capability-packages/:id/lifecycle`，不回退到 legacy `/v1/bridges`。
- 创建本地测试连接草稿 `basic-connection-test`，类型为 `software_connection`，会话为 `capability:software_connection:basic-connection-test`，用于验证连接一级页面和对象对话。
- 该测试连接 `manifest.testOnly=true`，只能作为页面和生命周期入口测试，不会被当作真实软件连通成功。

## 验收

- `node --check companion-desktop/shell/capability-card-schema.js`
- `node --check companion-desktop/shell/connection-page.js`
- `npm run local-companion:typecheck`
- `npx vitest run tests/shellCapabilityCardSchema.test.ts tests/shellConnectionPageUi.test.ts`
- `npx vitest run tests/capabilityPackages.test.ts tests/agentToolSchema.test.ts tests/agentP2L2.test.ts tests/hostAppProcess.test.ts tests/shellCapabilityCardSchema.test.ts tests/shellConnectionPageUi.test.ts`

# P8-017：连接页支持拖入软件快捷方式创建连接草稿

## 完成内容

- 连接页支持拖入 `.lnk` 快捷方式或 `.exe` 文件，自动解析为本机软件连接草稿。
- Windows 快捷方式解析放在主进程，通过 `shell.readShortcutLink` 获取真实目标路径。
- 渲染进程只负责拖拽交互和调用 `/v1/capability-packages/drafts`，不会回退到 legacy `/v1/bridges/drafts`。
- 草稿 manifest 记录 `inputPath`、`shortcutPath`、`executablePath`、`exeName`、`targetKind`，已知软件会附带 `hostId`，后续启动仍遵守现有白名单。
- 修复连接卡片按钮中文乱码，新增测试防止“对话/安装/探测/版本/提交云端/删除草稿”等文案回退为乱码。
- `install/probe/uninstall` 生命周期补充结构化事件记录，支持 Loop Engine 的失败 -> 对话修复 -> 再验证对象事件流。

## 验收

- `node --check companion-desktop/preload-shell.cjs`
- `node --check companion-desktop/main.cjs`
- `node --check companion-desktop/shell/connection-page.js`
- `node --check companion-desktop/shell/capability-card-schema.js`
- `npm run local-companion:typecheck`
- `npx vitest run tests/capabilityPackages.test.ts tests/shellConnectionPageUi.test.ts tests/shellCapabilityCardSchema.test.ts`

## P2-004 执行记录

时间：2026-08-10 18:58
状态：completed

改动文件：
- `companion-desktop/shell/connection-page.js`
- `tests/shellConnectionPageUi.test.ts`

完成内容：
- 补齐连接页本地 `connectionState` 兜底判断，让 Blender 相关连接在列表数据暂未带后端 `connectionState` 时也能识别为 `bridge_supported`。
- 兜底识别关键词覆盖 `blender`、`blender_http`、`blender_startup`、`python_http`，避免拖入 `blender.exe` 后 UI 误显示为“模板待接入”。
- 固化主进程拖拽推断证据：`CONNECTION_DROP_EXE_HOSTS` 包含 `blender.exe -> hostId=blender, name=Blender`。

验证：
- `node --check companion-desktop/shell/connection-page.js`：通过。
- `npx vitest run tests/shellConnectionPageUi.test.ts`：通过，1 个测试文件，6 条测试。
- `npm run local-companion:typecheck`：通过。
- `npx vitest run tests/capabilityPackages.test.ts tests/hostAppProcess.test.ts tests/hostBridgeInstallers.test.ts tests/shellConnectionPageUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/agentToolSchema.test.ts tests/agentP2L2.test.ts`：通过，7 个测试文件，140 条测试。
- `node --check companion-desktop/shell/capability-card-schema.js`：通过。
- `node --check companion-desktop/agent-tool-schemas.cjs`：通过。
- `node --check companion-desktop/agent-body-host.cjs`：通过。

人工验收：
- 已完成真实 Blender 端到端验收：本机 Blender 5.1.2，路径 `C:\Program Files\Blender Foundation\Blender 5.1\blender.exe`。
- 启动脚本已安装到 `C:\Users\ZYF\AppData\Roaming\Blender Foundation\Blender\5.1\scripts\startup\assetcutter_blender_bridge_startup.py`。
- `blender-real-acceptance-20260810` 的 capability context 已出现 `connectionState.maturity = connected`，事件包含 `probe_passed`，消息为 `Blender bridge connected (5.1.2)`。

遗留风险：
- 当前轮完成了自动验证和真实软件探测验收；连接页面的鼠标拖拽视觉流程仍建议在桌面壳窗口中再手动点验一次，确认用户侧拖入动作和卡片刷新体验符合预期。

下一任务：
- 完成计划总审计，并重启本地伴侣运行态。

## P2-001 执行记录

时间：2026-08-10 18:36
状态：completed

改动文件：
- `local-companion/src/capabilities/capabilityLifecycle.ts`
- `local-companion/src/capabilities/softwareConnectionState.ts`
- `tests/capabilityPackages.test.ts`

完成内容：
- 将 Blender 接入统一能力包生命周期，`install/probe/uninstall` 不再被误判为 `template_missing`。
- `install` 调用真实 `installBlenderBridge()`，把 startup 脚本写入用户选择的 Blender `scripts/startup` 目录。
- `probe` 调用真实 `getBlenderBridgeStatus()`，只接受 Blender startup 脚本协议对应的本机 HTTP `/health` 信号，不能用安装记录冒充 connected。
- `uninstall` 调用真实 `uninstallBlenderBridge()` 清理 startup 脚本。
- 连接成熟度判断补充 Blender，使 Blender 卡片可进入 `bridge_supported / bridge_installed / connected` 路径，而不是模板待接入。
- 新增能力包生命周期测试：安装 Blender startup 脚本、未启动 health 时 probe 失败、启动真实 HTTP health 后 probe 成功并变为 `connected`、卸载后脚本消失。

验证：
- `npm run local-companion:typecheck`：通过。
- `npx vitest run tests/capabilityPackages.test.ts tests/hostAppProcess.test.ts tests/hostBridgeInstallers.test.ts`：通过，3 个测试文件，108 条测试。

人工验收：
- 未执行；仍需要在真实 Blender 安装环境中拖入 Blender 快捷方式或 exe，点击 Copilot 处理，安装 startup 脚本，重启 Blender 后真实 probe 成功。

遗留风险：
- P2 自动侧已证明统一生命周期可跑通 Blender，但真实 Blender UI 内入口和用户机器上的版本路径仍需人工验收记录。
- P3/P4 的页面级人工验收仍需在工作台中复核。

下一任务：
- P2-002：执行真实 Blender 人工端到端验收，记录软件版本、安装路径、脚本写入位置、probe 成功证据和失败重试记录。

## P2-002 执行记录

时间：2026-08-10 18:50
状态：completed

改动文件：
- `local-companion/src/bridges/templates/hostBridgeTemplates.ts`
- `tests/hostBridgeInstallers.test.ts`

完成内容：
- 使用本机真实 Blender 做端到端验收：`C:\Program Files\Blender Foundation\Blender 5.1\blender.exe`，启动输出版本为 `Blender 5.1.2`。
- 通过统一能力包生命周期创建并验收连接对象 `blender-real-acceptance-20260810`。
- 安装目标选择 Blender 5.1 安装目录，安装器自动映射到用户 startup 目录：
  - `C:\Users\ZYF\AppData\Roaming\Blender Foundation\Blender\5.1\scripts\startup`
  - 写入文件：`assetcutter_blender_bridge_startup.py`
- 首次真实 probe 在 Blender 未保持运行时失败，事件写入 `probe_failed`，错误为 `Blender bridge is not reachable on 127.0.0.1:7011: fetch failed`。
- 真实验收发现 Blender 5.1 对 startup 脚本要求 `register()`；补充模板的 `register()/unregister()` 兼容。
- 第二轮真实验收发现 `unregister()` 会导致 startup HTTP 服务立即关闭；调整为 startup 桥接不在 `unregister()` 中主动 shutdown，由 Blender 进程退出时自然结束 daemon 线程。
- 使用临时 Blender 后台保持脚本启动真实 Blender 后，probe 成功，连接对象进入 `connected`：
  - `connectionState.maturity = connected`
  - `publishEligible = true`
  - 最新成功事件：`probe_passed`，消息 `Blender bridge connected (5.1.2)`

验证：
- `npx vitest run tests/capabilityPackages.test.ts tests/hostBridgeInstallers.test.ts`：通过，2 个测试文件，100 条测试。
- `npm run local-companion:typecheck`：失败；失败点为 `local-companion/src/workflows/runtime/workflowFixtureRunner.ts(107,11)` 的 `audit.scopes` 类型为 `string[] | undefined`，属于 workflow 迁移中的无关阻塞，不属于本次 Blender 连接链路。

人工验收：
- 通过。
- 软件名称和版本：Blender 5.1.2。
- 安装路径：`C:\Program Files\Blender Foundation\Blender 5.1\blender.exe`。
- 创建方式：通过能力包生命周期创建真实验收连接对象 `blender-real-acceptance-20260810`。
- 是否成功启动：通过后台 Blender 进程启动并保持运行，startup 脚本打印监听日志。
- 桥接写入位置：`C:\Users\ZYF\AppData\Roaming\Blender Foundation\Blender\5.1\scripts\startup\assetcutter_blender_bridge_startup.py`。
- 软件内入口位置：Blender startup 自动加载脚本；当前样板为自动 HTTP health 桥接入口，未额外添加可见菜单项。
- probe 信号类型：Blender 内 startup 脚本启动本机 HTTP `/health`，端口 `127.0.0.1:7011`。
- probe 成功证据：能力包 context 最近事件包含 `probe_passed`，消息 `Blender bridge connected (5.1.2)`，连接状态为 `connected`。
- 失败重试记录：同一对象保留了 `probe_failed` 到 `probe_passed` 的连续事件。

遗留风险：
- Blender 样板已完成真实端到端验收，但 UI 中“拖入 Blender 快捷方式/exe -> Copilot 处理 -> 安装 -> 重启 -> 探测”的完整手点流程仍建议再走一遍产品验收。
- 全量 typecheck 当前被 workflow 迁移类型问题阻塞，需要单独修复或在 workflow 迁移任务中收口。

下一任务：
- P3/P4 页面级人工验收复核，确认连接页展示、Copilot loading、模板草稿展示和无旧 Host Center 痕迹。

## P3/P4 自动复核记录

时间：2026-08-10 18:54
状态：auto_verified / manual_ui_pending

复核范围：
- 连接页脚本语法。
- 共享能力包卡片 schema。
- Copilot 工具 schema 与 body host。
- 连接页 UI 单测。
- 模板草稿展示单测。
- Copilot P2L2 路由与能力包上下文隔离单测。

验证：
- `node --check companion-desktop/shell/connection-page.js`：通过。
- `node --check companion-desktop/shell/capability-card-schema.js`：通过。
- `node --check companion-desktop/agent-tool-schemas.cjs`：通过。
- `node --check companion-desktop/agent-body-host.cjs`：通过。
- `npx vitest run tests/shellConnectionPageUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/agentToolSchema.test.ts tests/agentP2L2.test.ts tests/capabilityPackages.test.ts`：通过，5 个测试文件，56 条测试。

人工验收：
- 当前工具环境没有可直接接管工作台窗口的浏览器控制入口，未能真实点击工作台连接页。
- 待手点确认项：
  - 连接页卡片展示成熟度、最近事件、下一步和能力可用性。
  - `Copilot 处理` 有 loading 和 inline 结果。
  - 模板草稿区能展示 `connection_template_draft_created` 事件内容。
  - 页面没有旧 Host Center / 62 宿主列表入口。

遗留风险：
- UI 自动测试已覆盖结构和渲染规则，但真实桌面窗口中的视觉状态仍需用户或后续可控浏览器环境复核。

## P2-003 执行记录

时间：2026-08-10 18:53
状态：completed

改动文件：
- `local-companion/src/workflows/runtime/workflowFixtureRunner.ts`

完成内容：
- 修复 workflow fixture client 的 `audit.scopes` 类型收敛问题，将可选 scopes 归一化为固定数组。
- 该修复不改变 workflow 执行语义，只消除 workflow 迁移中暴露的 typecheck 阻塞，让连接产品化验收可以继续使用全量本地伴侣类型检查作为门禁。

验证：
- `npm run local-companion:typecheck`：通过。
- `npx vitest run tests/capabilityPackages.test.ts tests/hostAppProcess.test.ts tests/hostBridgeInstallers.test.ts`：通过，3 个测试文件，108 条测试。

人工验收：
- 无需单独人工验收；这是类型收口修复。

遗留风险：
- 连接页的手点产品验收仍需在工作台 UI 中复核。

下一任务：
- P3/P4 页面级人工验收复核，确认连接页展示、Copilot loading、模板草稿展示和无旧 Host Center 痕迹。

## P0-004 执行记录

时间：2026-08-10 17:19
状态：completed

改动文件：
- `companion-desktop/shell/connection-page.js`
- `tests/shellConnectionPageUi.test.ts`

完成内容：
- 连接页列表合并后为每个 `software_connection` 注入 `connectionState`，优先使用后端字段，列表接口暂未提供时使用页面轻量镜像兜底。
- 连接卡片新增“状态 / 阻塞 / 能力 / 下一步”可见字段，展示成熟度、阻塞原因、可用动作摘要和下一步建议。
- 搜索覆盖 `connectionState.maturity`、`label`、`blockedReason`、`nextAction` 与 `availableActions`。
- 统计中的已连接数量改为以 `connectionState.maturity === connected` 为准，不再用零散事件冒充连接成功。

验证：
- `node --check companion-desktop/shell/connection-page.js`：通过。
- `npx vitest run tests/shellConnectionPageUi.test.ts`：通过，1 个测试文件，6 条测试。

人工验收：
- 未执行；P0-004 自动验证已覆盖页面字段与 legacy bridge 不回退约束。

遗留风险：
- 列表接口仍需要后续统一返回后端 `connectionState`，当前页面镜像只作为兼容兜底。

下一任务：
- P0-005

## P0-005 执行记录

时间：2026-08-10 17:23
状态：completed

改动文件：
- `local-companion/src/capabilities/capabilityLifecycle.ts`
- `local-companion/src/capabilities/capabilityContext.ts`
- `companion-desktop/shell/connection-page.js`
- `tests/capabilityPackages.test.ts`
- `tests/shellConnectionPageUi.test.ts`

完成内容：
- 对没有真实桥接模板的软件连接，`install/probe/uninstall` 返回结构化 `error: template_missing`。
- `template_missing` 返回体包含 `message`、`nextAction`、`supportedActions`，明确当前仍可对话、启动、关闭、识别运行中和导出。
- 事件写入 `kind: template_missing`，不再只写泛化的 `install_failed/probe_failed/uninstall_failed`。
- 对象上下文在 `template_missing` 时明确说明：当前可 launch/close/discover/export/conversation，真实 bridge install/probe/uninstall 尚未接入，Copilot 应围绕当前 CapabilityPackage 生成模板接入方案。
- 连接页通过 `connectionState.availableActions` 让模板缺失场景主入口保持为 `Copilot 处理`，不暴露不可用的直接安装/探测按钮。

验证：
- `npm run local-companion:typecheck`：通过。
- `node --check companion-desktop/shell/connection-page.js`：通过。
- `npx vitest run tests/capabilityPackages.test.ts tests/shellConnectionPageUi.test.ts`：通过，2 个测试文件，28 条测试。

人工验收：
- 未执行；需要真实打开连接页拖入未知 exe 后观察卡片状态，留到 P0-006 重启后手测。

遗留风险：
- 尚未做真实 UI 点击手测；当前自动测试证明后端响应、事件、上下文与页面约束均已落地。

下一任务：
- P0-006

## P0 汇总记录

时间：2026-08-10 17:24
状态：P0 implementation complete / runtime restart pending

完成内容：
- `ConnectionState` 已成为连接对象的统一产品状态来源。
- `connected` 只由真实 `lastProbe.ok=true` 产生，安装记录、文件存在和卡片存在都不能冒充真实连接。
- 连接对象 context、共享卡片 schema、连接页 UI 都已消费 `connectionState`。
- 模板缺失被产品化为“模板待接入”，并把下一步交给对象级 Copilot loop，而不是表现成坏掉的按钮。

验证：
- `npx vitest run tests/capabilityPackages.test.ts`：通过。
- `npm run local-companion:typecheck`：通过。
- `node --check companion-desktop/shell/capability-card-schema.js`：通过。
- `npx vitest run tests/shellCapabilityCardSchema.test.ts`：通过。
- `node --check companion-desktop/shell/connection-page.js`：通过。
- `npx vitest run tests/shellConnectionPageUi.test.ts`：通过。
- `npx vitest run tests/capabilityPackages.test.ts tests/shellConnectionPageUi.test.ts`：通过。

下一任务：
- P0-006：重启本地伴侣并记录结果。

## P0-006 执行记录

时间：2026-08-10 17:25
状态：completed

改动文件：
- `docs/连接改造执行状态.md`

完成内容：
- 追加 P0-004、P0-005 和 P0 汇总证据。
- 按计划重启本地伴侣，使 `local-companion` 与桌面壳连接页改动进入当前测试环境。

验证：
- `npm run restart:local-companion`：通过。

人工验收：
- 未执行；连接页打开和未知 exe 拖入验证可在后续 P1/P2 手测中一起完成。

遗留风险：
- 当前工作台窗口如果缓存旧 preload/UI，需要刷新或重新打开窗口后再手测。

下一任务：
- P1-001

## P1-001 执行记录

时间：2026-08-10 17:27
状态：completed

改动文件：
- `companion-desktop/agent-body-host.cjs`
- `tests/agentP2L2.test.ts`

完成内容：
- `ac.capability.connection_loop_run` 现在会先读取对象 context 中的 `connectionState.maturity`，再生成本轮 `plannedSteps`。
- `template_missing` 和 `probe_failed` 不再盲目执行 install/probe，而是写入事件并打开对象对话。
- `bridge_installed` 执行 `connection.probe`，`bridge_supported` 执行 `bridge.install`，`path_ready/process_ready` 执行 discover/launch。
- 返回结构新增 `maturity`、`plannedSteps`、`missingPermissions`、`finalContext` 和 `nextAction`。
- 未授权动作不会执行，只记录缺失权限；所有动作继续走 capability lifecycle/context/events 受控入口。

验证：
- `node --check companion-desktop/agent-body-host.cjs`：通过。
- `npx vitest run tests/agentP2L2.test.ts tests/agentToolSchema.test.ts`：通过，2 个测试文件，20 条测试。

人工验收：
- 未执行；P1-001 为 Agent body host 行为和单测任务。

遗留风险：
- 真实 Copilot 会话中的提示还需固定，避免它绕回旧 host bridge 或 mock probe；P1-002 处理。

下一任务：
- P1-002

## P1-002 执行记录

时间：2026-08-10 17:29
状态：completed

改动文件：
- `companion-desktop/brain-adapters/codex.cjs`
- `companion-desktop/shell/connection-page.js`
- `tests/codexBrainAdapter.test.ts`
- `tests/shellConnectionPageUi.test.ts`

完成内容：
- Codex 全局提示明确：已有软件连接修复必须优先读取当前 CapabilityPackage 的 context/connectionState，并使用 `ac.capability.connection_loop_run`。
- Codex 全局提示明确不切回 legacy 62-host catalog 或 `ac.companion.host_bridge.*`。
- 连接页 `Copilot 处理` 发送对象提示时附带当前成熟度、阻塞原因和下一步。
- 连接页对象提示明确：不要把文件存在或安装记录当作连接成功，不要 mock probe 成功。

验证：
- `node --check companion-desktop/brain-adapters/codex.cjs`：通过。
- `node --check companion-desktop/shell/connection-page.js`：通过。
- `npx vitest run tests/agentP2L2.test.ts tests/shellConnectionPageUi.test.ts tests/codexBrainAdapter.test.ts`：通过，3 个测试文件，24 条测试。

人工验收：
- 未执行；P1-002 为 prompt 和页面对象提示约束任务。

遗留风险：
- loop 写回事件仍偏泛化，下一任务改成明确 passed/failed，方便持续修复。

下一任务：
- P1-003

## P1-003 执行记录

时间：2026-08-10 17:36
状态：completed

改动文件：
- `companion-desktop/agent-body-host.cjs`
- `tests/agentP2L2.test.ts`
- `tests/capabilityPackages.test.ts`

完成内容：
- `ac.capability.connection_loop_run` 在本轮计划动作后写入明确事件：
  - `connection_loop_passed`
  - `connection_loop_failed`
  - `connection_loop_template_missing`
- `bridge_installed` 成熟度会执行 `connection.probe` 后写入 loop summary 事件。
- `connected` 可写入 passed summary；`template_missing/probe_failed` 写入失败或具体模板缺失事件并打开对象对话。
- 对象 context 可读取 `connection_loop_failed`，并把它纳入 `latestFailure` 与 `contextPrompt`，方便继续对话修复。

验证：
- `node --check companion-desktop/agent-body-host.cjs`：通过。
- `npm run local-companion:typecheck`：通过。
- `npx vitest run tests/capabilityPackages.test.ts tests/agentP2L2.test.ts`：通过，2 个测试文件，36 条测试。

人工验收：
- 未执行；P1-003 为 loop 事件和对象上下文测试任务。

遗留风险：
- P2/P3/P4 尚未细化；需要按文档规则继续拆分下一阶段任务。

下一任务：
- P2-001：按文档拆分下一阶段连接产品化任务

## P3-001 执行记录

时间：2026-08-10 17:41
状态：completed

改动文件：
- `companion-desktop/shell/connection-page.js`
- `tests/shellConnectionPageUi.test.ts`

完成内容：
- 连接卡片新增卡片内操作结果行 `connection-card-result`，生命周期动作执行前显示处理中，成功/失败后留在对应卡片上。
- 连接卡片新增最近事件折叠区 `connection-card-events`，展示最近 5 条安装、探测和对象事件，失败事件使用失败样式突出。
- 能力展示从 raw action id 改为中文可用性 chip，覆盖 Copilot、识别运行、启动、安装桥接、探测、发布等关键能力。
- `launch/discover_running/close/install/probe/uninstall` 不再用 alert 作为主反馈，改为 inline status + 卡片结果；删除、卸载确认仍保留。
- 保持连接页不回退 legacy `/v1/bridges` 和旧 62 宿主 catalog。

验证：
- `node --check companion-desktop/shell/connection-page.js`：通过。
- `node --check companion-desktop/shell/capability-card-schema.js`：通过。
- `npx vitest run tests/shellConnectionPageUi.test.ts tests/shellCapabilityCardSchema.test.ts`：通过，2 个测试文件，10 条测试。

人工验收：
- 未执行；需要打开连接页点击安装/探测确认卡片内反馈和最近事件视觉效果。

遗留风险：
- 导入、导出、发布等非生命周期治理动作仍保留部分 alert；本任务只覆盖 P3 要求中的生命周期动作主反馈。

下一任务：
- P3-002：搜索无结果空状态和页面产品化细节继续收敛

## P3-002 执行记录

时间：2026-08-10 17:49
状态：completed

改动文件：
- `companion-desktop/shell/connection-page.js`
- `tests/shellConnectionPageUi.test.ts`

完成内容：
- 搜索时 summary 增加“筛选 N”统计，用户能看清当前列表是筛选结果。
- 搜索无结果改为中性空状态 `connections-empty-result`，文案为“没有匹配的连接，换个关键词试试。”，不再复用错误样式。
- 保持搜索为空显示全部，且无结果状态不回退旧 Host Center 或 62 宿主列表。

验证：
- `node --check companion-desktop/shell/connection-page.js`：通过。
- `npx vitest run tests/shellConnectionPageUi.test.ts tests/shellCapabilityCardSchema.test.ts`：通过，2 个测试文件，10 条测试。

人工验收：
- 未执行；需要打开连接页输入无匹配关键词观察空状态。

遗留风险：
- P3 的视觉人工验收仍待执行，包括多个连接下的搜索筛选、卡片内反馈、最近失败展示和 Copilot 处理 loading。

下一任务：
- P4-001：新增模板草稿类型和对象事件保存，避免污染正式 bridge definition

## P4-001 执行记录

时间：2026-08-10 18:00
状态：completed

改动文件：
- `local-companion/src/capabilities/softwareConnectionAdapter.ts`
- `companion-desktop/agent-tool-schemas.cjs`
- `companion-desktop/agent-body-host.cjs`
- `tests/capabilityPackages.test.ts`
- `tests/agentP2L2.test.ts`
- `tests/agentToolSchema.test.ts`

完成内容：
- 新增 `ConnectionTemplateDraft` 结构和 `buildConnectionTemplateDraft()`，用于承载未支持软件的连接模板草稿。
- 模板草稿固定 `status=draft`、`productionDefinition=false`，包含 hostId、appName、软件类型、待生成文件、用户目录、真实 probe 信号和安全边界。
- 新增 Copilot 工具 `ac.capability.template_draft_create`。
- 该工具只把 `connection_template_draft_created` 事件写入当前 CapabilityPackage 对象，不写入 `local-companion/src/bridges/definitions/`，也不改变“已支持/可发布”状态。
- 工具返回结构明确 `notProductionDefinition=true`、`publishBlockedUntilRealProbe=true`。

验证：
- `npm run local-companion:typecheck`：通过。
- `node --check companion-desktop/agent-tool-schemas.cjs`：通过。
- `node --check companion-desktop/agent-body-host.cjs`：通过。
- `npx vitest run tests/capabilityPackages.test.ts tests/agentP2L2.test.ts tests/agentToolSchema.test.ts`：通过，3 个测试文件，45 条测试。

人工验收：
- 未执行；需要拖入未知软件后让 Copilot 调用 `ac.capability.template_draft_create`，观察对象事件。

遗留风险：
- P4 后续还需要让 Copilot 根据软件形态自动生成更具体的计划内容，并在页面中更直接展示模板草稿。

下一任务：
- P4-002：Copilot 根据未知软件类型生成模板接入计划

## P4-002 执行记录

时间：2026-08-10 18:20
状态：completed

改动文件：
- `local-companion/src/capabilities/softwareConnectionAdapter.ts`
- `local-companion/src/capabilities/capabilityContext.ts`
- `companion-desktop/agent-tool-schemas.cjs`
- `companion-desktop/agent-body-host.cjs`
- `tests/capabilityPackages.test.ts`
- `tests/agentP2L2.test.ts`

完成内容：
- `ConnectionTemplateDraft` 根据 `kind` 自动补齐模板接入计划：
  - `executable`
  - `script_dcc`
  - `project_plugin`
  - `command_port`
  - `heartbeat`
  - `unknown`
- 自动生成待创建文件、需要用户选择的目录、真实 probe 信号和安全边界。
- `ac.capability.template_draft_create` 不再强制要求手写 `probeSignal/files/requiredUserDirs/safetyBoundaries`，只要 Copilot 判断出软件类型即可生成默认计划。
- `template_missing` 对象上下文明确提示调用 `ac.capability.template_draft_create`，并列出可选 kind。
- 仍保持 `productionDefinition=false`，不写生产 bridge definition，不绕过真实 probe 或云端发布门禁。

验证：
- `npm run local-companion:typecheck`：通过。
- `node --check companion-desktop/agent-body-host.cjs`：通过。
- `node --check companion-desktop/agent-tool-schemas.cjs`：通过。
- `npx vitest run tests/capabilityPackages.test.ts tests/agentP2L2.test.ts tests/agentToolSchema.test.ts`：通过，3 个测试文件，45 条测试。

人工验收：
- 未执行；需要用未知软件连接对象触发 Copilot 处理，确认其根据软件形态调用 `ac.capability.template_draft_create`。

遗留风险：
- 模板草稿仍主要通过对象事件进入上下文，连接页尚未把接入计划作为一等块展示。

下一任务：
- P4-003：连接页展示模板接入计划草稿

## P4-003 执行记录

时间：2026-08-10 18:29
状态：completed

改动文件：
- `companion-desktop/shell/connection-page.js`
- `tests/shellConnectionPageUi.test.ts`

完成内容：
- 连接页卡片会从对象事件中读取最近的 `connection_template_draft_created`。
- 卡片新增“模板草稿”区域，展示：
  - 模板类型
  - 真实 probe 信号
  - 需要用户选择的目录
  - 安全边界
- 模板草稿仍来自对象事件，不改变连接成熟度，也不让未验收软件显示为已支持。

验证：
- `node --check companion-desktop/shell/connection-page.js`：通过。
- `npx vitest run tests/shellConnectionPageUi.test.ts`：通过，1 个测试文件，6 条测试。

人工验收：
- 未执行；需要用未知软件连接生成模板草稿后观察卡片显示。

遗留风险：
- P2 非 Photoshop 真实连接样板和真实软件人工验收尚未完成。

下一任务：
- P2-001：Blender 真实连接样板自动侧审计与补齐

# P8-026: PI-style connection agent loop

## Done

- Added `ac.capability.connection_loop_run` as the first bounded autonomous loop for software connections.
- The loop is scoped to one `CapabilityPackage` object and accepts explicit permissions only: `context.read`, `process.discover`, `process.launch`, `bridge.install`, `connection.probe`, `event.write`, `conversation.open`.
- The loop still calls the existing governed lifecycle endpoints, so Copilot gets more product-level autonomy without gaining arbitrary shell command or arbitrary filesystem access.
- Probe success remains gated by the real software signal. The loop can record evidence and failures, but it cannot turn file existence, card existence, or install records into a connected state.
- Codex brain routing now prefers this loop for existing connection repair/install/launch/verification instead of treating each action as an unrelated button click.

## Verification

- `node --check companion-desktop/agent-tool-schemas.cjs`
- `node --check companion-desktop/agent-body-host.cjs`
- `node --check companion-desktop/brain-adapters/codex.cjs`
- `npx vitest run tests/agentToolSchema.test.ts`

## Next

- Surface this loop in the Connection page UI as a product-level "Copilot 继续处理" entry with inline progress, recent event evidence, and next action hints.
- Add richer backend capability metadata so unsupported hosts clearly say "can launch/discover now, bridge install/probe template not connected yet" instead of failing like a broken product.

# P8-027: Connection page product pass

## Done

- Added a real search box to the Connection page. It filters by connection id, name, description, status, app name, host id, executable path, shortcut path, input path, and tags.
- Added a compact status summary for total connections, connected connections, pending connections, and cloud-backed connections.
- Connection cards now show product-facing operational facts:
  - target path or shortcut path
  - latest event/probe/install result
  - next action hint
- Added a first-class `Copilot 处理` action for software connections through the shared `ShellCapabilityCardSchema`.
- `Copilot 处理` opens the bound CapabilityPackage object conversation and sends a scoped instruction that asks Copilot to use `ac.capability.connection_loop_run` with bounded permissions.
- The page still avoids old `/v1/bridges` product paths and keeps Host Center hidden from the connection product surface.

## Verification

- `node --check companion-desktop/shell/connection-page.js`
- `node --check companion-desktop/shell/capability-card-schema.js`
- `node --check companion-desktop/agent-tool-schemas.cjs`
- `node --check companion-desktop/agent-body-host.cjs`
- `npx vitest run tests/shellConnectionPageUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/agentToolSchema.test.ts`

## Next

- Replace alert-based lifecycle feedback with inline per-card progress and result rows.
- Add backend capability metadata for each software connection so the UI can distinguish "can launch", "can install bridge", "can probe", and "needs Copilot to design a bridge template".

## P0-001 执行记录

时间：2026-08-10 17:09
状态：completed

改动文件：
- `local-companion/src/capabilities/softwareConnectionState.ts`
- `tests/capabilityPackages.test.ts`

完成内容：
- 新增 `ConnectionMaturity` 与 `ConnectionState`。
- 新增纯函数 `deriveSoftwareConnectionState(pkg)`。
- 覆盖 `draft`、`path_ready`、`template_missing`、`bridge_supported`、`bridge_installed`、`probe_failed`、`connected` 的成熟度判断。
- 明确 `connected` 只有 `lastProbe.ok=true` 才 `publishEligible=true`。
- 明确缺少真实模板的软件不暴露直接 `probe` 动作，而是保留 `agent_loop`、启动和识别运行能力。

验证：
- `npx vitest run tests/capabilityPackages.test.ts`：通过，20 条测试通过。
- `npm run local-companion:typecheck`：通过。

人工验收：
- 未执行；P0-001 为纯函数和单测任务。

遗留风险：
- `connectionState` 尚未接入 capability context 和连接页 UI，下一任务处理。

下一任务：
- P0-002

## P0-002 执行记录

时间：2026-08-10 17:12
状态：completed

改动文件：
- `local-companion/src/capabilities/capabilityContext.ts`
- `tests/capabilityPackages.test.ts`

完成内容：
- `buildCapabilityPackageContext()` 对 `software_connection` 返回 `connectionState`。
- `contextPrompt` 增加 `connectionState` 和可读的 `Connection maturity` 摘要。
- 工具和 workflow context 不返回 `connectionState`，避免对象上下文串线。

验证：
- `npx vitest run tests/capabilityPackages.test.ts`：通过，21 条测试通过。
- `npm run local-companion:typecheck`：通过。

人工验收：
- 未执行；P0-002 为后端 context 和单测任务。

遗留风险：
- 连接页和共享卡片 schema 尚未消费 `connectionState`，下一任务处理。

下一任务：
- P0-003

## P0-003 执行记录

时间：2026-08-10 17:15
状态：completed

改动文件：
- `companion-desktop/shell/capability-card-schema.js`
- `tests/shellCapabilityCardSchema.test.ts`

完成内容：
- 共享卡片 schema 在 `software_connection` 存在 `connectionState` 时优先使用成熟度状态。
- `status` 使用 `connectionState.label`。
- `tags` 增加成熟度标签。
- `actions` 优先使用 `connectionState.availableActions`。
- `canPublish` 在存在 `connectionState` 时尊重 `publishEligible`，避免未真实连接时显示发布入口。
- `template_missing` 场景不显示直接安装/探测/发布动作，保留 Copilot 处理和启动/识别类动作。

验证：
- `node --check companion-desktop/shell/capability-card-schema.js`：通过。
- `npx vitest run tests/shellCapabilityCardSchema.test.ts`：通过，4 条测试通过。

人工验收：
- 未执行；P0-003 为共享 UI schema 和单测任务。

遗留风险：
- 连接页尚未为列表数据注入或展示 `connectionState` 细节，下一任务处理。

下一任务：
- P0-004

# P8-024: remove local workflow product surface and defer runs to ScriptHub

## Done

- Removed the local shell first-class workflow page from the sidebar and view whitelist.
- Deleted the local `workflow-page.js` UI module and the local `workflowRunner.ts` implementation.
- Kept `CapabilityPackage(type=workflow)` only as a ScriptHub-facing projection/cache for validation, object conversation, cloud publish, and cloud version switching.
- Changed workflow lifecycle `run` to fail explicitly with `workflow_runtime_owned_by_scripthub` and record `workflow_run_blocked`, so local Companion cannot fake workflow run success.
- Removed the workflow card `run` action from shared card schema.
- Updated Copilot lifecycle schema guidance so workflow runs are routed to ScriptHub Workflow Runtime instead of local manifest step execution.

## Verification

- `node --check companion-desktop/main.cjs`
- `node --check companion-desktop/shell/capability-card-schema.js`
- `node --check companion-desktop/agent-tool-schemas.cjs`
- `npm run local-companion:typecheck`
- `npx vitest run tests/capabilityPackages.test.ts tests/agentToolSchema.test.ts tests/agentP2L2.test.ts tests/shellConnectionPageUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/shellToolCloudPublishUi.test.ts`

## Next

- When workflow UX is needed, integrate ScriptHub `WorkflowSkill`/`WorkflowRun` as the source of truth instead of rebuilding a parallel workflow page/runtime inside Local Companion.

# P8-025: tools page keeps only the tool rack

## Done

- Removed the tools page secondary navigation.
- Removed the hidden Host Center / bridge-management panel from the tools page DOM.
- Removed the tools-page dynamic loader that could still open `tools-bridges.js`.
- Kept the legacy bridge implementation files and backend tests untouched for migration/reference, but they are no longer a product surface inside the tools page.

## Verification

- `node --check companion-desktop/shell/tools-page.js`
- `node --check companion-desktop/shell/capability-card-schema.js`
- `npm run local-companion:typecheck`
- `npx vitest run tests/shellHostCenterUi.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts tests/shellToolExportSavePath.test.ts tests/shellToolWindowUi.test.ts`

# P8-021：工具流进入一等能力包页面

## 完成内容

- 新增左侧导航一等页面“工具流”，不再让 workflow 只停留在后端 schema 和 Copilot 路由里。
- 新增 `companion-desktop/shell/workflow-page.js`，只读取和显示 `CapabilityPackage(type=workflow)` 草稿与云端包。
- 工具流卡片复用 `ShellCapabilityCardSchema`，支持对话、验证、导入、导出、管理员提交云端和选择已有云端版本。
- 工具流对象对话优先读取 `/v1/capability-packages/:id/context`，保持 `capability:workflow:<id>` 独立会话。
- 页面不显示 `run` 动作，避免在 workflow runner 尚未接入时伪造运行成功。
- 主进程视图白名单加入 `workflows`，切换该页时与工具/连接一样卸载嵌入网页视图。

## 验收

- `node --check companion-desktop/shell/workflow-page.js`
- `node --check companion-desktop/main.cjs`
- `node --check companion-desktop/shell/capability-card-schema.js`
- `npm run local-companion:typecheck`
- `npx vitest run tests/shellWorkflowPageUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/shellConnectionPageUi.test.ts`
- `npx vitest run tests/capabilityPackages.test.ts tests/agentToolSchema.test.ts tests/agentP2L2.test.ts tests/shellWorkflowPageUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/shellConnectionPageUi.test.ts tests/shellToolCloudPublishUi.test.ts`

## 下一步

- 继续补 workflow 的真实 runner/节点执行能力，让工具流从“对象级创建与治理”进入“可真实运行与日志修复”的 Loop Engine 验收。
- 继续清理连接页和旧文件中遗留的中文乱码，优先处理用户可见 UI。

# P8-022：workflow 最小运行器与对象日志闭环

## 完成内容

- 新增 `local-companion/src/capabilities/workflowRunner.ts`，workflow `run` 不再只是保留动作。
- `runCapabilityLifecycle(id, 'run')` 现在会按能力包类型分派：`tool` 继续走本机工具运行，`workflow` 走最小 workflow runner。
- workflow runner 只执行 `manifest.steps` 中声明的安全步骤：
  - `checkpoint/manual/note`：记录确定性检查点；
  - `capability/capability_lifecycle`：调用另一个能力包的生命周期动作；
  - `tool/shell_tool`：调用已注册本机工具。
- workflow 没有可运行步骤时返回失败并写入 `workflow_run_failed`，不把空工作流当成功。
- workflow 每次运行会写入 `workflow_run_started`、`workflow_step_passed/failed`、`workflow_run_passed/failed`，对象对话可直接读取最近失败和运行日志。
- 工具流页面新增“运行”按钮，统一调用 `/v1/capability-packages/:id/lifecycle` 的 `run`，不开放任意命令。
- Copilot lifecycle 工具说明补充 workflow run 规则：只能执行声明步骤，必须写入 workflow step/run 事件。

## 验收

- `npm run local-companion:typecheck`
- `node --check companion-desktop/shell/workflow-page.js`
- `node --check companion-desktop/shell/capability-card-schema.js`
- `node --check companion-desktop/agent-tool-schemas.cjs`
- `npx vitest run tests/capabilityPackages.test.ts tests/shellWorkflowPageUi.test.ts tests/shellCapabilityCardSchema.test.ts tests/agentToolSchema.test.ts tests/agentP2L2.test.ts`

## 下一步

- 继续把 workflow runner 的步骤协议暴露给 Copilot 创建提示，让用户通过对话创建工具流时能自动生成可运行 `manifest.steps`。
- 继续把工具流运行失败后的“修复建议”做得更具体，例如定位失败 step、建议补哪个连接或工具参数。

# P8-023：修复连接页拖入快捷方式读取不到路径

## 完成内容

- 修复连接页拖入软件快捷方式后弹出“没有读取到拖入文件路径”的问题。
- 原因是页面把 `FileList` 从主世界传给 preload 后，文件对象跨隔离上下文时可能丢失真实路径。
- `preload-shell.cjs` 现在会在拖拽事件捕获阶段用 `webUtils.getPathForFile()` 先缓存最近拖入路径。
- 连接页调用 `companionShell.droppedFilePaths(files)` 时，如果跨上下文读取不到路径，会回退使用 preload 缓存路径。
- 仍然保持后续校验：只允许 `.lnk` 快捷方式或 `.exe`，并继续走 `/v1/capability-packages/drafts` 创建 `software_connection` 草稿。

## 验收

- `node --check companion-desktop/preload-shell.cjs`
- `npx vitest run tests/shellConnectionPageUi.test.ts`

## 注意

- preload 变更需要重新加载桌面壳窗口后生效；如果当前窗口仍报旧错误，关闭再打开本地壳窗口后重试拖拽。

# P8-018：能力包对象上下文提供失败摘要和下一步提示

## 完成内容

- 能力包 context 增加 `latestFailure` 与 `nextStepHints`，对象对话打开后可直接看到最近失败和下一步建议。
- 拖拽创建的未知软件连接会明确提示：需要补齐 `hostId`、安装方式和真实探测方式，不能把 exe 存在当作连接成功。
- 已知 hostId 的软件连接会提示启动/关闭/识别运行中必须走 `ac.capability.lifecycle_run`，并继续遵守白名单。
- Photoshop 探测失败时，对象上下文会提示先在 Photoshop 菜单运行 AssetCutter 连接脚本，再重新 probe。
- workflow 上下文继续提示运行器未接入前不能伪造 run 成功。

## 验收

- `npm run local-companion:typecheck`
- `npx vitest run tests/capabilityPackages.test.ts tests/agentP2L2.test.ts`

# P8-019：云端版本治理覆盖软件连接、工具和 workflow

## 完成内容

- 补强能力包云端版本测试，验证 `software_connection`、`tool`、`workflow` 共用同一套 cloud version store。
- 验证软件连接发布仍要求真实 probe；工具和 workflow 不要求真实软件 probe。
- 验证 workflow 本地失败事件不会被带入云端版本，避免本地草稿历史污染团队版本。
- 验证普通用户不能发布 workflow，也不能切换 workflow 云端版本。
- 验证管理员可发布 workflow v1/v2，并只能切换到云端已有版本。

## 验收

- `npm run local-companion:typecheck`
- `npx vitest run tests/capabilityPackages.test.ts`

# P8-020：能力包传输 JSON 与连接页导入导出

## 完成内容

- 新增能力包传输格式 `assetcutter.capability.transfer`，用于把连接/工具/workflow 以能力包 JSON 发给别人。
- 导出时剥离本机路径字段、安装记录、probe 状态和本地事件，避免把本机历史污染到他人环境。
- 导入后重新成为本地能力包草稿，并保留独立对象会话；导入方需要在自己的机器上重新安装、启动和真实探测。
- 新增 HTTP 入口：`GET /v1/capability-packages/:id/export` 与 `POST /v1/capability-packages/import`。
- 连接页新增“导入连接”按钮；连接卡片新增“导出”按钮。
- 导出会弹出保存位置选择，不默认写入插件目录。

## 验收

- `node --check companion-desktop/preload-shell.cjs`
- `node --check companion-desktop/main.cjs`
- `node --check companion-desktop/shell/connection-page.js`
- `node --check companion-desktop/shell/capability-card-schema.js`
- `npm run local-companion:typecheck`
- `npx vitest run tests/capabilityPackages.test.ts tests/shellConnectionPageUi.test.ts tests/shellCapabilityCardSchema.test.ts`
