# 已归档文档

默认不要读本目录。当前真源只在 `docs/` 根上那几份：错题本、交接、大楼宪章、未收口清单、`adr/`、多模型计划、工作流时间线、AI 执行路由审计。

这里是历史计划、改造清单、旧规格和原型。考古时再打开。原「店仓菜单」宪章在 `架构宪章-店仓菜单.md`；原则已并入 `docs/多模型可运营改造计划.md` §1.4。

例外：被要求接「整栋做成游戏 / 虚拟人桌宠 / 东家养成」时，读 `虚拟人桌面伴侣-东家养成设想.md`。那是设想纪要，不是大楼宪章，默认不开工。
