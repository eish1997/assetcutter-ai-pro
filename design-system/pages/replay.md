# Override: 技能房间

相对 `MASTER.md` 的偏差只在这里。

- 房间名「技能」。空态一句下一步：「跟管家说：把刚才那套整理成技能」。
- 标题行下工具条对齐工具架：搜索 + 全部/我的/本地/云端（复用 `.tools-search` / `.tools-filter-chip`）。
- 卡：标题 + 一段描述 + 动作钮（执行；货架可移除/上云；仅云端可安装）。执行钮底 `tungsten`、字近 `ink`。不要预检清单、表单、历史面板，也不改成工具架穿孔铭牌。
- 最近一次成功/失败可以一行次要字，不要第二套高饱和色。
