# Override: 桌面壳（标题栏 / 房间 / 送出）

相对 `MASTER.md` 的偏差只在这里。

- 标题栏 30px、`slate` 底、`white/6%` 底边，与现在一致。
- 当前房间：字色 `paper` + 底部或左侧 tungsten 细条；不要整颗蓝胶囊。
- 顶栏「送出」是唯一实心按钮：底 `tungsten`、字近 `ink`。次要按钮仍是 compose 透明环。
- 管家栏没有选中货时不要自己发光。
- 设置页继续 `tone="settings"` 实色边，不要强行玻璃化。
