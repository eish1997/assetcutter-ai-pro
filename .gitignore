# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.data/

# Desktop companion：打包产物体积大，由本地构建或 GitHub Releases 分发，勿入库
# 本机 SAM 权重/venv/缓存（见 scripts/setup-sam-local.mjs），勿入库
SamLocal/
SamLocal-release/

# Desktop companion：打包产物（统一 dist/ 子目录，见 companion-desktop/README）
companion-desktop/dist/
# 自定义输出根（COMPANION_BUILD_OUTPUT_ROOT=dist-out-xxx）与历史 release 目录
companion-desktop/dist-out-*/
companion-desktop/release/
companion-desktop/release*/
# 小工具包发行 ZIP（npm run pack:shell-tool → dist-out-shell-tools/<id>-<semver>.zip）
dist-out-shell-tools/
companion-desktop/local-companion-bundle/
companion-desktop/build-constants.json
companion-desktop/sam-local-bundled/

# auth-api 运行时登记（首条上传时创建，含环境与用户相关元数据）
server/data/companion-artifacts.json
server/data/ai-gateway-provider-keys.json
server/data/ai-gateway-provider-keys-events.json
server/data/ai-gateway-provider-keys.online-mirror.json
server/data/*.bak-*
server/data/auth-db.test.json
server/data/agent-cli/
*.local
.env
.env*.local

# Cursor 本地配置（规则、skills），可不提交；允许提交 Agent Autopilot hooks
.cursor/*
!.cursor/hooks.json
!.cursor/hooks/
!.cursor/hooks/**
# Autopilot 完成状态仅本地，避免协作冲突与误提交
.cursor/hooks/loop-state.json
.cursor/hooks/autopilot.runtime.json

# Dev log tip cache (R2 is source of truth)
.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# C11 — generated at build
public/healthz
public/healthz.json

