# 本地 npm run dev 与线上一致：走 Render gemini-proxy（开发时由 Vite /__ac-bulk-forward 同源转发，无需本机 9002）
# 若需调试本机 gemini-proxy，在 .env.local 覆盖为 same-origin 并 npm run dev:gemini-proxy
# 开发默认走 Render gemini-proxy（与 production 一致）；本地同源生图请在 .env.local 覆盖：
# VITE_BULK_IMAGE_API=same-origin
# VITE_BULK_IMAGE_API_VERTEX=same-origin
# 走 Render 生图时请在 .env.local 设置 VITE_AUTH_API_BASE_URL（与 .env.production 一致），并重新登录
VITE_AI_WORKER_PROXY_API=https://assetcutter-ai-worker-proxy.onrender.com
VITE_AI_WORKER_PROXY_API_VERTEX=https://assetcutter-ai-worker-proxy.onrender.com
VITE_BULK_IMAGE_API=https://assetcutter-ai-worker-proxy.onrender.com
VITE_BULK_IMAGE_API_VERTEX=https://assetcutter-ai-worker-proxy.onrender.com
